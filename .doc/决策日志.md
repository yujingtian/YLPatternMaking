# 决策日志（演进史归档）

> 本文件只记**过程**：每次口径变更的日期、决定、理由、废弃了什么。
> **现行规格一律以 [python工程设计.md](python工程设计.md) §十（实现态速查）与代码为准**，那里不重复历史。
> 新条目规则：规格文档只写现状（不带日期叙事），日期属于本日志与 git commit。

---

## 一、腰头裁片（v0.5 → v0.9）

- **v0.5 两标量参数化合成**：弯腰头下口线由两个标量参数拼合 → 废弃（对 curve_sag 响应失真）。
- **v0.6 cap_sag 限幅**：给 sag 加上限防变形 → 废弃（cap 限 sag 破坏弯曲总量传递，被拒）。
- **v0.7 圆顺窗长**（`waistband_blend_cb/side`）+ **`waistband_front_drop`**：分段圆顺 → v0.8 随整根拟合删除，两键走 from_dict 一次性垫片（stderr 告警后丢弃）。
- **v0.8 `fit_fair_cubic` 数值拟合**：忠实保留拼合链「平后段+侧缝肘弯」结构 → v0.9 删（后段本就近直，小 sag 时视觉与均匀弧无差，拟合复杂度不值）。
- **v0.9 整根均匀圆弧**（现行）：三锁（弧长=净长精确 / 总转角=链末切向角 / 起端切向沿 X̂ 保后中镜像 C2）+ 一匀（曲率恒 1/R）；端点位置派生（独立带状裁片端点无装配语义）。`g2_blend` 一并删除。
- **`_auto_drop` 同侧旋转拼合**：旧拼合把前弧绕侧缝腰点旋转同侧放置 → 删（退化口径，对 curve_sag 零响应）；改**跨后片侧缝线反射**（物理平摊缝拼合）。
- **2026-08-30 `waistband_fly_extension` 默认 3.5 → 0**：有些款搭门量加在 left_end 缝份上而非外延净样；>0 仍可用（弯腰头随弧端斜出 ~40° 转角时 3.5cm 使左端低 ~2.3cm，属设计非 bug）。
- **2026-08 新增 `waistband_shrinkage_warp/weft`**：腰头原是唯一直读全局率、无专用覆盖的主面料裁片，与机头/门襟等七片同口径。

## 二、前片裆尖角部（四轮目检定稿）

- **默认限长 miter 静默回退阶梯角**：尖裆内角 ≈75°、miter ≈1.64·缝宽 > 1.5 限长，角部留「竖一刀+斜一刀」台阶（用户目检发现，金标 M 尺寸角度缓未暴露）→ cutter 增显式 `"miter"` 取值绕过限长。
- **圆弧抹角家族全部废除**（2026-08 用户三轮目检）：等距圆弧 `"round"`、数值半径大圆弧、浪尖相切圆角——「净样裆尖就是尖的」，缝边只能是尖的。`_round_join_points`/`_circle_seg_cross`/`_arc_points`/`_tip_round_points` 整体删除；能力后又在 cutter 恢复（`"round"`），但前片裆尖不再使用。
- **轮廓延伸态**（`front_piece_crotch_contour`，前浪净边 C2 延续越出裆尖交下裆缝缝份边界）→ 删：弯腰头形态前浪曲率大，C2 延续越摆越远，缝边翘成高尖角且非真等距。
- **两偏移切线内切圆**→ 删：75° 尖角处内切圆深嵌角内，弧顶距净尖仅 ≈0.3cm，缝份塌陷破坏不变量。
- **尖角跟随净样 + 显式 miter**：中间态。
- **「等曲率延续弯向角点转向 + G1 单曲率弧重建过渡链」**：解决拉直与鼓包 → 被用户手改为纯贝塞尔多项式自然外延（`_natural_join_sharp`）并目检通过。
- **2026-08-24 定稿切线直线延伸 miter**（现行 False 态，`"miter_line"`）：多项式外延在锐角下交点远飞、缝边顶点高出裆尖 ~1.8cm；端切线直线延长交点近而有界（= sa/sin(θ/2)，实测恰高 1.0）。多项式求交解保留作后片 False 态。
- **同日「钝角平顶」**（`_blunt_join` 单边多项式外延 + `front_piece_crotch_lift` 固定抬高截断）→ 上线即废，选项一并回收。
- **mirror 折角键序误置**（("inseam","rise")，折轴错成下裆缝）：裆尖内角 ≈87° 近直角时镜像退化 ≈miter，角点「向内平切」不报错（用户目检发现）→ 更正 ("rise","inseam")。
- **2026-08-19 mirror 改真反折角构造 + 补轴穿越点 X**：旧实现只镜像方向、锚点不动，斜角翻折短缺（机头 (bottom,side) 角 0.66）；且直接连偏移链自然端点到 M 的弦，被镜像边缝边到不了角点等高处（后片内缝短 0.22，用户目检）。新构造经 X 中转后翻折像与被镜像边毛缝边界全线重合（金标：翻折像距线 == 0）。

## 三、DXF 刀口/定位孔表示（历经五轮实测修订）

- **「POINT+TEXT 成对」**→ 废：定位孔被误渲染成放码刀口（`_add_mark_text` 已删）。
- **「层 3 纯 POINT」**→ 废：混淆放码点/轮廓顶点层与刀口层。
- **「层 8 真实 CIRCLE r=0.5mm」**→ 废：过小完全不可见。
- **现行口径**：刀口 = 层 4 POINT + 组码 30（Z 深度）/50（开口角度）；定位孔 = 层 13 单纯 POINT。
- **第五轮（ET 顶点吸附）**：ET 按裁切折线顶点吸附挂刀口符号，段中间的刀口点不显示（机头延长线交缝边点集体不显示发现）→ `_with_notch_vertices` 把落在 CUT 折线上且严格段中的刀口点共线插入为顶点。角顶点命中的刀口切线不可判、渲染十字孤立点（门襟刀口报障）→ 刀口生成端保证落点在段中或共线顶点、不取法向均值。
- **净样层 14 闭环**（逆向 5336 大货样本）：标准 AAMA 净样在层 8，但 ET08 的层 8 只是普通内部线——净样落 8 白色散线不被识别致毛样净样重叠 → 净样/含缩水净样整圈链成一条闭合 POLYLINE 落层 14。

## 四、Web 端交互

- **主流程重构：3D 主视图 + 导出中心 + 高级编辑（2026-09-11）**：平铺结构（左全量参数 + 右 tabs + 顶部 8 按钮工具条）重排为「调参 → 3D 看效果 → 生成 → 勾选导出」主流程。四项产品决策（用户拍板）：①「生成」只重算 3D 试穿（fitting），2D 整版/裁片按需再算——进入高级编辑或导出时 `ensureSheet/ensurePieces` 自动补算（缺失或 stale 才重跑，新鲜直用）；②导出多产物逐个文件串行下载（不引入 zip，Chrome 多文件授权弹一次，150ms 间隔）；③左栏核心参数 = 基础测量 8 + 款式开关组（含 pocket_type/fly_type 虚拟下拉）+ 腰头类型，其余进「全部参数」页签（原面板原样）；④高级编辑 = 全屏 2D 工作台（右栏 3D 整体替换，左栏保持可见保拖拽高亮闭环）。
  - **导出口径变化**：由「预览新鲜才许导出」改为「导出时自动补算」——DXF/TOML 走后端重算天然最新（不再受本地快照新鲜度门控）；SVG/报表源自内存快照（downloadBlob 直存，本地引擎可出零后端），stale 必先 ensure 再取。
  - **手动生成语义**：Fitting3DView 删 800ms 自动重试穿与快捷滑杆卡（与左栏重复）；保留首挂载自动生成一次；stale 提示 = 舞台 Tag + 生成按钮 StaleFlag 角标。
  - **Toolbar 解散**：生成/导出/高级编辑 + 引擎 Tag → 左栏底部 action-bar；错误/警告 Alert → IssueStrip；撤销上次拖拽 → 高级编辑工具行。PreviewPane 由 AdvancedEditor 取代（删 fitting3d tab，2D 工作台化）。
  - **useDraft 微调**：单 version 口径不动（applyAdjust 竞态丢弃依赖它）；generate* 改 ref 取参（measRef/optsRef/versionRef，回调 deps []）+ 返回快照本体供按需补算直接消费；download 失败由吞掉改为 setErrors 后重抛（导出队列按项落账，void 消费处须自带 catch）。
  - 硬边界：引擎/后端端点/Pyodide 包零改动，全部前端组合层。
- **过期提示三轮定稿（2026-09-07）**：①PreviewPane 横幅条件渲染——显隐改变 flex 高度，绘图区被顶上顶下（把手一按一松连跳）；②Alert 恒挂载 + visibility:hidden 占位（nbsp 撑行高，普通空格被 HTML 折叠塌零）——常驻空行突兀被否；③终版：Toolbar 按钮右上角 StaleFlag 角标 + Tooltip，绝对定位不占宽。
- **custom 编辑器二轮（2026-08-27）**：上移/下移按钮无用 → 预览角点直接拖拽取代（pointer capture + getScreenCTM、0.1 圆整、命中半径自适应、表格行高亮联动）。
- **预览弧线与整版不符（用户报障）**：初版二次贝塞尔 Q、控制点只偏移 bulge，弧高仅 0.5·bulge，与引擎 8/3 三次的 2·bulge 差 4 倍 → 预览必须复刻引擎控制点构造而非「顶点近似」。
- **袋布预览底部节点被裁（用户报障）**：自适应 bbox 的 pad 四方向 maxY 误写减号，盒底向内推进 → 四边同向外扩。
- **推板档差口径二次拍板（2026-08-27）**：首版「统一与上一码、空格恒在首码」在基码居中场景反直觉（基码前插码后首码格空、基码格反可录）→ 基码锚定：基码行恒「—」，上方录与更大相邻码之差、下方录与上一码之差。
- **fly/fly_separate 双真 warning 移除（2026-08）**：fly_type 虚拟下拉已强制互斥，双真仅模板载入出现，引擎 fly_separate 优先口径不变。
- **schema gate 两坑修复**：组级 visible_if 指向组内自己的开关 → 循环隐藏整组（开关须归常显组）；参数级 gate 误用组级全真语义 → 口袋缩水参数永不显示（改任一真即显示）。
- **422 归因键序依赖修复（2026-08）**：逐键累加对关系型参数对有键序依赖（shape=custom 先于 custom_points 时前缀构造角点仍是默认空元组误报「得到 0 个」）→ 整体先试全量构造，失败才逐键归因。
- **waistband_front_drop 误归整版「腰头绘制」组（2026-08-27 修正）**：整版腰口弧度实由 front/back_waist_curve_sag 控制，front_drop 仅裁切链消费 → 移裁片段 craft_waistband。
- **getScreenCTM 平移方向坑（2026-08-25 修复）**：user→屏幕 px 方向矩阵，平移位移换算须取倒数 1/hypot(a,b)，直接用 hypot 则跟踪速率随缩放级漂移。
- **两步生成 + 拖拽调版上线（2026-08-25）**；本地 Pyodide 引擎上线后拖拽节流 100ms→50ms。

## 五、agent 边界（2026-09-03，一日两版）

- **初版（同日上午）**：「agent 薄壳、extract 零搬移」+「五不变量论证物理搬移不可行」——隐含前提是 `ylpattern.cli extract` 子命令必须留核心。
- **用户指出前提崩塌**：「ylpattern 就是单纯的引擎代码，所有大模型相关的都应该放到 agent 里」——CLI extract 子命令本身就是大模型功能、随包迁走，反向依赖与 wheel ImportError 两论据同时消解 → 当日整体迁移 12 模块。

## 六、参数层与口径

- **2026-08-25 删死参数 `side_intake_k_waist`**：定义后从未被消费；k_waist 唯一正解是 `waist_balance`。旧尺寸单传该键被 from_dict 拒绝（防呆）。
- **2026-08 `__post_init__` 拆分**：460 行单体拆 14 个分特征 `_check_*` + 4 个模块级助手，报错文案逐字一致（658 项全绿验证等价）；多字段同时非法时首报错顺序因分组重排略异。
- **2026-08-29 前浪裆弯单参数 → 三参数**：`front_rise_handle_ratio` 取代为 `front_rise_alpha/beta` + `front_rise_exit_angle`，默认 (1/3, 1/3, 0°) 与旧口径逐字节等价。
- **2026-08-29 `watch_pocket_offset_from_top` 4.0 → 3.0**：吃省默认改 0 后袋贴内边上端左移 ~2cm，4.0 时袋口顶边射线与内边弧线临界不相交。
- **2026-08-29 `front_pocket_dart_width` 默认改 0（不吃省）**：需要吃省时显式录入 1.5~2.5。
- **2026-09-01 腰长不变量实施**：纸样腰长 = 成品目标 + 边缘省口合计；`front/back_waist_dart` 收窄为纯腰长调节量（键名保留）。行为变更：开省款缝后腰围变长 ~Σ省宽，属修正本意。测试夹具 p2_drop 7.5→7.0 适配。
- **2026-08 缩水乘法 → 除法口径**：旧 ×(1+率) 以净样为基准、洗后偏小率²量级；改 ÷(1−率)（缩水前毛坯为基准）。
- **三个总开关落地**：`show_labels`（出口层显示控制）、`show_seam_allowance`（出口层显示控制；遗留死参数 `seam_allowance: float = 1.0` 保留但无实际作用）、`shrinkage_enabled`（计算级跳过，与前者性质相反）；解析统一收敛 `o.shrinkage_rates(专用warp, 专用weft)`，替换 flow 里七处手写回退。
- **2026-08 cutter 缝边公差固定 sample(32) → 公差驱动自适应**（`_offset_points_adaptive` 弦高 0.1mm，紧弧自动加密）；与 `_dxf_base.FLATTEN_TOL_CM` 同值锚定（依赖方向禁核心层 import exporters，注释锚定改公差两处同改）。同期发现缝边尖点曲率峰在端点不在中点（κ(0)≈1.9 vs κ(0.5)≈0.10），判据须全程扫 κ。
- **2026-08-27 贴袋预设/袋布预设收敛公式层**：`formulas/patch.patch_net_vertices` / `formulas/pouch.pouch_chain_preset`，前后贴袋 steps 与 web seed 共用单一来源。
- **collect_pieces 替换 api/cli 双份平行分支**：原「仅设某片 SVG 时只 build 该片」的惰性 → 全收集（开销可忽略、裁片集合一致）；cli 8 组逐片 elif 警告收敛为外层一条。

## 七、推码（多尺码）

- **架构路线拍板**：尺码表进、逐码参数化重打版——不做点位移生成几何，跨码唯一变量是 Measurements；「勿开逐码选项」与该路线冲突。
- **层 3 放码点云与 cutter 计数漂移发现**：发射点数随码漂移破坏跨码对应（ET 放码每码重采样实测；我们 front/back 也漂移+微段）→ 层 3 + 步 2.5 契约移二期预留，v1 先做多码核心。
- **v1 落地 2026-08-27**：SizeRun schema + collect + 多码 DXF + api/cli 入口；输出口径拍板：svg/trace/report 只出基码（版师只调基码）；毗围不收敛报告而非失败。

## 八、reverse/（工厂 DXF 反解析）

- **2026-08-30 袋贴内边用户纠偏**：「片内两弧：内部 = 袋口缝线、边线 = 袋贴弧」——袋贴内边 = 前代净边底部大弧（此前误取内部弧）。
- **2026-08-30 表袋二次纠偏**：5015 表袋件是自定义多边形非袋贴相交 → custom 净样原样提取；同日首则「无具体位置就自设，只保证上部不被面板藏住」。
- **2026-08-30 后贴袋三定则**（用户定则）：机头下方 / 对齐后腰中点 / 袋底保持毗围线上方。
- **2026-09-02 袋口弧深工厂定标**：旧 bulge 0.3/0.4/0.5 渲染仅 0.6~1.0cm，比工厂 5015 实测真弧高 3.36 浅 3~4 倍（用户复核「弧度和照片差距很大」发现）→ 真弧高 1.5/2.5/3.5 三档 ÷2 发射。
- **已知边界**：毗围闭环留残余 ΔW ≈ −2.7cm（工厂目标 = 导线弦，引擎默认形状系数下重打版偏大）——层 4 边界，报告披露不阻塞。

## 九、extract/（照片参数提取）

- **VLM 换型**：glm-5.3-flash（4v-flash 失明绕过，max_tokens≥8192 防思维链截断）；延迟修复 = 流式 SSE + thinking 开关（数值调用默认关，4.7s）。
- **2026-09-02 22 键模板注意力淹没发现**：模型只报整件级特征，局部件键需判据量化锚 + prompt「必看项」指令定向才报（实测目测 26% 与独立读数 25%~30% 一致）。
- **2026-09-03 必看项须覆盖同族全部枚举键**：首版只定向 mouth_depth，低腰喇叭款实照复核发现 mouth_mode 仍落默认 conf 0.40 未做视觉判读（恰好碰对）→ 必看项扩为「先判形态三选一、再量弧深」。
- **2026-09-02 枚举判据「唯一分辨点」口诀入册**：低腰喇叭背面盾形底被误判 angular（conf 0.75）——盾形本就是五边形；判据须给几何分辨点（数底部：斜线交底中一点=baker_shield / 底中留水平段=angular），勿只给风格形容词。
- **families 绕行小表袋引擎缺口**：`p1_dist=10.0` + `facing_side_w=3.5` + `p2_drop` 按腰位分档（低 5.5/中低 6.5/中+ 7.5）——引擎自适应定位缺口修好后两值可回归带中值。
- **2026-09-02 `front_pouch` 不进先验表**：内部件照片看不到、描述侧无判据词，未分析到不发射（用户口径）。
- **2026-09-03 CLI 进度回调**：`extract_from_input(progress=)` 阶段回调 + stderr `[agent] {秒}s {消息}`（stdout 留给产物路径）；HTTP 服务缺省 None 静默。
- **2026-09-03 迁至 agent/extract/**：见「五、agent 边界」。

## 十、杂项口径定型

- **2026-08 弯腰头 + 有省量时 P1/P1′ 垂直延长至上腰头线**（袋贴/袋布消费同源）。
- **2026-09-02 小表袋 facing_intersect 深度定标**（用户定标）：深度被袋贴内边锁死是口径特征非缺陷（下半段入袋不可见，顶部对齐即可）；extract G 表 `offset_from_top` 3.0→1.0、`width` 7.0→5.5。
- **刀口口径多次纠偏收敛为家族口径**：净口角点「每角 2 刀、打在缝边上」（入边延长线交出边缝份 + 出边反向延长线交入边缝份）；后贴袋曾按文档字面做成净线端点竖直外投（用户目检否决）、小表袋曾误读为两偏移线交点单刀（用户纠正）。
- **脚口刀口两轮纠正**：旧 `point_at_y(sa_hem)` 打在卷边起折推断位（与净脚线错位 ~2.7cm，废）；中间版保留净样位不外扩（打在净样角点非缝边，废）；定稿 = 内外侧缝 ∩ 净脚口线角点、pinned 限定载体边打在缝边上。
- **后片毗围线坑**：按水平线截断复制在 d=0 时与横裆线完全叠影（用户目检「毗围线没画出来」）→ 端点本在净边上的参考线一律 1:1 拷贝。
- **后片横裆线不画**（用户口径）：毗围线即其测量基准、横裆水平线冗余，高度交点亦不打口。
- **2026-08 后片上边界角点十字标记废除**：打在缝边顶点上无用 → 双射线刀口（每角沿两邻边延长线各一刀）取代。

## 十一、3D 人台试穿（2026-09-09 一期）

- **立项动因**：用户口径——整版 2D 太抽象，看不到「穿在身上的效果」；行业通行 3D 人台试穿，调参到满意再导出。导出链路零改动（stale 门控照旧）。
- **方案选型（用户拍板）**：轻量物理仿真（自研 PBD 纯 TS，新依赖仅 three + delaunator）；参数化人台（前端代码生成，无外部模型资产）；一期前后片布料缝合 + 腰头环带，小裁片只画结构线不做独立仿真。
- **体型围度与成衣尺寸分离（用户纠偏，本条为一期最重要口径）**：尺寸单围度是成衣量（含松量/调节量），直接当人台围度则松紧效果是假的（永远显示先验松量）。BodyProfile 独立前端 state + localStorage，不进引擎 Measurements/DraftPayload、不 bump 参数版本、不影响打版；默认从成衣按先验松量反推估计并标注「估计值」，用户可录真实测量值。引擎零体型先验（改先验/体型不用重打引擎 zip）。
- **序列化坐标**：备选裁片局部系（与裁片 SVG 一致）→ 弃，取**整版全局系 cm Y-up**：初始摆位直接拿全局 Y 当身体高度、marks 零换算、金标可写「hem 边所有点 y==脚口线高」强断言。唯一例外腰头（旋转局部系不可逆推）只随标量导出。
- **origin 补法**：备选 flow 往 local ctx 塞 NamedPoint → 弃（破 test_front_piece 元素计数金标）；取 `PatternPiece` 尾部默认字段 origin/frame，3 处重建点（with_shrunk/with_gross/apply_shrinkage）透传，金标钉死。
- **fitting 快照不走「先画后裁」门控**：试穿是探索视图、引擎独立重打版；整版/裁片/DXF 新鲜度语义不变。3D 内快捷滑杆写回同一参数 state（版本照常 bump → 整版/裁片 stale），debounce 800ms 自动重试穿；DXF 下载仍由整版门控把守。
- **腰口 = 视觉环带 + pin，不做腰头布片仿真**：腰头近无弹性，仿真只引入高刚度→小步长稳定性代价；top_chain 钉在各自 drafted 高度体表环（yoke 开启时 back 顶链天然钉在机头缝高度，与环带间缺口记为一期已知简化）。
- **摆位支撑半径而非射线求交**：初态半径取各覆盖管支撑函数最大值（凸上界，保证不穿体、无腿间缝隙退化），比射线求交便宜；终态贴合交给 PBD 收敛。
- **GS + substeps 选型**：Gauss-Seidel 原地投影（不用 Jacobi 并行批处理——单线程 JS 无收益）；3 substeps×6 iterations 为 60fps 预算内的稳定点；碰撞每子步一次（约束迭代内不碰撞，省 pow 开销）。
- **发散兜底阶梯**：NaN/飞点 → 恢复上一好帧（阻尼×0.9、迭代×2）→ 连败 3 次冻结当前帧 + 「仿真已暂停」角标（静态包裹摆位兜底仍可看效果）。
- **辅助功能（用户全选）**：应变热力图（红紧/绿贴/蓝松，约束数据顺手算）、视角快捷按钮 + OrbitControls 自由拖（用户确认必须支持鼠标拖动）、透明度/隐藏人台、截图 PNG、快捷滑杆自动更新、体型管理三件套（内置预设 5 个 + 自定义管理 + JSON 导入导出）。
- **一期显式非目标**：自碰撞（裆部互穿二期焊合+局部加密）、小裁片独立缝合、腰头布片仿真。二期候选：小裁片缝合、自碰撞、求解迁 Worker、静态 yoke 带。
- **2026-09-09 互嵌接合方案裁决（消除骨盆/大腿分体感）**：三方案评审，胜者「两段独立采样 + 闭式 containment clamp」——骨盆上段 11 环与腿下段站点表逐位不变（接合带之外零漂移），81 环 k=0.6419 恰饱和 (1−m)aP−cx、48 顶点最坏骨盆范数 0.9104<1 顶盖零外露（源码级复刻验证）。**否决单表追加站点**：CR 前视锚改变使骨盆 82 环围度 88.8→91.28（a 漂 +0.42cm）、腿 78 以下环漂移 ~0.5cm，「可见腿型不动」不成立；其基线亦有误读（y 超范围的管是 continue 跳过非夹取端环，「前中支撑下降」不存在）。**否决外露鳍片式微收（0.96 截顶）**：留 1.06cm 外露鳍片 + 盖处支撑台阶 1.06cm + 后侧 1.81cm 平盖带，其「腿顶盖不可完全藏入」被方案 1/2 常数直接证伪。吸收其两个独家发现（见下条 collide 缺陷 + 入口守卫清单）；其楔环专设第四截面档不采纳（y<yHip−1 自动落 hip 档即可得已验证的会阴支撑值）。
- **2026-09-09 collide 存量缺陷发现（互嵌评审的意外收获）**：collideOne 把未归一化 (dx,dz) 直灌 radiusAt——其返回「向量倍数」语义（|dx|=L/2 返回 2.0 非真半径），穿透判据退化为 dist≥R/dist+skin、均衡点 ≈√R（骨盆 3.79cm），布料可沉入体表 ~10cm；旧单测（粒子停在 a/2=7.17，limit 仅 2.3 根本未推出）与集成两行断言同用弱度量全部空转，radiusAt docstring「未归一化也可」是错的。修复：collideOne 归一化后取 limit + docstring 改口 + 单测/集成断言同步改强度量（防再次空转）。互嵌三管交叠带使顺序投影首次非幂等（单遍残差最坏 14.7cm、双遍归零），SOLVER_PRIOR.collideSweeps=2 随行落地。
- **2026-09-09 三管接合形态：平口对接改互相嵌入**：一期三管裆线平口对接呈分体——共面端盖（骨盆平底盖与双腿平顶盖同处 y=crotch，z-fighting）+ 腿圆超出骨盆半宽；改为互相嵌入：骨盆下延楔填裆、腿管上延内收插入骨盆、顶环 containment 解析保证平顶盖藏体内。弃备选：单管花生形截面（破超椭圆星形凸假设）、贴装饰环遮缝。
- **2026-09-09 互嵌二轮对抗审查修复（2 high，复测 63/63 全绿）**：① **tubeMesh 全管绕向内翻**（一期存量缺陷：法线朝内、FrontSide 下看到的是管内壁——「分体感」与共面盖可见皆此症状，藏盖 containment 被整体击穿）→ 墙面与顶底盖绕向翻转 + 补绕向/水密/盖朝向金标；教训：遮挡发生在三角绕向层，环参数层 27/27 全绿曾放过它。② **合法病理体型压破 containment 守卫**：waist=130/hip=70/thigh=90（全过逐字段校验、无跨字段校验）在 rise Δ∈[5.5,8] 使 k<0.3 抛 Error → 3D tab 静默空白 → 改优雅降级「放弃上插只保楔」（z-fighting 消除不依赖上插成立）；不可反向钳大 k（k 是 containment 最大收缩系数，放大即穿壁）。随行删两处按臀站点算出的「恒正余量」假注释（裆上受力环段实为负，containment 全靠 k 收缩兜底）。
- **2026-09-09 三轮：tubeMesh 三管装配体被 SDF 蒙皮整体取代删除**：用户反馈互嵌只做到几何连通——交线 C0 硬折痕、剪影髋-腿过渡凹角、明暗交线两侧突变，「视觉仍是搭积木」；逐面装配体在原理上圆不出 C1 交线，换 SDF 隐式曲面（skin.ts：三管 smin 软并集场 + 自写查表 marching cubes 单张水密网格，环参数仍唯一来自 buildMannequin；MC 选型裁决——否决 three examples addon（单位立方体 N³ 网格对 40×130×35cm 细长 bbox 浪费 ~10 倍节点、addBall 元球 API 无逐点场回调、直出三角汤无法共享顶点做水密金标/光滑法线）与 marching tetrahedra（三角 ×1.7 烧穿 3 万预算），查表 MC + 共享栅格边顶点懒分配保证闭合流形、歧义胞只夹拓扑不破水密）。tubeMesh 删除（grep 证实唯一消费者 Fitting3DView 与 1c 金标），绕向/水密金标整体迁移 skin.test.ts 并由「三角几何法线·∇F>0 ≥100 采样」继承强化（管参数层全绿不代表渲染正确的一期教训在此闭环）。碰撞/摆位零改动（seams/pbd 仍走 sectionAt/radiusAt 解析路径），解耦论证 = 融合带采样 R_skin−surfaceRadius ≤ k/4+0.05（实测 +0.247）< garmentGap（该 k/4 论证与 surfaceRadius 参照四轮均证伪，重造为解析并集双测度，见下条①②）。规格两处按代码现实修正并已在 §10.11 披露：① 穹顶场原文「dy≥L 返回 dy−L」有侧向断崖 + 两踝下方 smin 压 k/4 桥出「脚底幻影膜」→ 改 max(d₂, dy−L)；② thigh 站裆侧回退按 min(r_exit, ring+k/4) 会注入固定 +2% 假漂移使 ±2% 容差空转 → 改环回退（exit ≤ ring+k/4 取 exit 否则回 ring，实测 +1.88%）。随行踩坑：three addon 源码 MC 表负数排版为「- 1」（空格分隔），提取须剥空白否则 parseInt NaN 致三角表全坏（760 万三角、20.7s）——表格按 Bourke 值逐行校验后才写死。规格金标两处纠偏：(0,75,0) 实为骨盆楔内（场负，会阴被填带属预期）非规格所断言 >0；(0,70,0) 才是不桥接证据（+0.176）。复测 76/76 全绿（skin.test.ts 14 例含 5 病理体型水密降级）。
- **2026-09-09 四轮：SDF 蒙皮对抗审查修复（7 findings 全实证，78/78 全绿）**：① **R1-F1（HIGH）单 k=2 融合鼓包证伪**——「k/4=0.5<garmentGap 故布料不可见」论证把**场值下压上界**当成了**表面位移上界**：裆区骨盆楔前壁与对侧腿前内壁近切向接触带两壁梯度近对消（|∇F|≈0.3），k/4 的场凹陷被放大数倍，实测 165/66A 腿心 y=75 θ≈295° 射线外凸 +1.66（评审更细角度采样 +1.94）超 garmentGap=1.2；归因实验拆分双对——病理粗腿鼓包（130/70/90=1.50、hip75×thigh80=1.62）是腿↔腿对驱动、瘦小/滑杆极值是骨盆↔腿对驱动 → **融合半径分对**：`F=smin(F_骨盆, smin(F_左腿,F_右腿,kLL), kPL)`，kPL=`blendKPelvisLeg`=0.75（kPL=1 瘦小已 1.04、1.25 达 1.24 超限）/ kLL=`blendKLegLeg`=1（kLL=2 粗腿内壁 1.62），7 夹具双测度实测最坏 0.81/0.76（余量 ≥33%）；评审三候选（分对 k/梯度归一化/膨胀补偿）取分对 k——最小改动且紧支 smin 族锁死不变。② **解耦金标参照整体重造**：原「R_skin−surfaceRadius ≤ k/4+0.05」的 surfaceRadius=逐管凸支撑取 max 恒 ≥ 并集轮廓（系统性低估外凸），且轴心原点 15° 稀疏采样永不命中腿心尖峰方向——参照改**解析并集本体**（新增导出 `unionField`=三管单管场严格 min，即碰撞/摆位解析路径的几何本体）双测度：并集表面点法向位移 −F/|∇F| ≤ gap + 轴心/腿心 1° 射线首穿−并集末次出射 ≤ gap；smin≤min 单侧性另立独立断言（蒙皮永不陷入并集内部，融合只向外鼓）。注：评审的「+1.94 已吞布料」遮挡推断偏保守——surfaceRadius ≥ 并集轮廓，布料初始摆位实际在并集轮廓+gap 之上，尖峰方向并未真吞；但外凸界断言为假是实打实的，按诚实保守口径修。③ **R1-F2 thigh 站 maxExcess ≤ k/4 恒真断言删**（构造使然：回退尺则把 >ring+0.5 的方向全换回 ring，非回退子集逐向外凸 ≤0.5 由构造恒成立）——改钉「回退方向数 ≤120」（实测 91，钉住楔接管带弧宽，楔变宽/腿内移即红）+ 漂移 ≤2%（实测 +1.95%），真实融合外凸上界交解耦金标。④ **R1-F4/R2-F2 预算循环**：单次 ×1.15 重跑不闭合（滑杆上限 120/130/80/60 单档后仍 32780>30000）→ 循环逐档放粗至多 5 档（`budgetMaxRetries`），病理 6 夹具断言 ≤ maxTriangles 收敛。⑤ **R2-F1 围度滑杆 200ms trailing debounce**：默认估计体型下腰/臀/腿/膝滑杆与围度直连（estimateBody 1:1 反推），每 0.5cm tick 触发蒙皮+PBD 同步重建（实测 ~150-240ms/标准、~500ms/病理）卡主线程——girths 先 debounce 再喂 useFittingSolver（warm-start 语义不变仅降频；与 payload 的 800ms 重生成链路独立）。⑥ **R1-F3 穹顶 max 连续性注释纠偏**（连续只在零点集邻域，端平面深部有 ≤L 幅度跳变但符号保持、零点集不受影响——勿对该场做线搜索求根/Lipschitz 假设的优化）。⑦ **R2-F3**：grid.material 泄漏（GridHelper.dispose() 释放 geometry+material）+ 删 isBody/isCloth 写后即死标记。§10.11 四段（smin 分对/预算循环/站点围度/解耦双测度）同步重写为现行规格；SKIN_PRIOR 头注记四轮证伪史（k/4 场值上界≠表面位移上界）。
- **2026-09-10 五轮：人台人体化（腿管外缘连续 emergence 替代藏头 containment，78/78 全绿）**：用户反馈「两个大腿和躯干拼合、长得奇怪」。数值诊断（标准夹具 165/66A）确认根因在 buildMannequin 三管骨架而非蒙皮层：①全身最宽点在大腿顶（腿在裆位即达全尺寸，外缘 19.06 > 臀站 15.56——真实人体最宽在臀、大腿外缘与臀线连续）；②containment 藏头使 emergence 一环跳变（81 环 a 4.38 → 78 环 9.16 一环翻倍，58° 陡壁）；③腿心恒 ±0.11×hip 不随高度内收（真裆 ±7.5→膝 ±7）；④膝→脚口等粗柱、无小腿肚；⑤骨盆 hip→crotch 仅收 0.94 比直筒。重构（引擎零改动）：删 containment 整链（embedKOf/容纳预检降级），腿环统一构造式 `cx = max(a+gap(y), min(outer(y)−a, 0.55×W_h))`——外缘目标梯子（×W_h：thigh 0.97/knee 0.78/hem 0.70/ankle 0.62）管剪影、内缘 gap 下限梯子（cm：crotch −1.5/crotch−12 处 0.3/knee 1.5/hem 1.3/ankle 1.2）管两腿分离，cx 不进 CR（分段线性梯子 Lipschitz 界可证）；头带 (crotch, min(crotch+7, hip−2)] 按 outerHead 三锚凸坡直接构造（贴盆内壁→2cm 陡降→放量至外缘，盆壁穿越角 ≥40°）；calf 站膝下 7cm×knee1.08；截面改圆（waist/臀 e 2.3/2.15、腿 default/knee depth 1.0/1.05）；crotchGirthRatio 0.94→0.85。剪影金标：腰 11.652→臀 15.889→大腿带 15.95（超臀 0.06 连续）→膝 12.25→小腿肚 13.29→脚口 10.99→踝 9.89，无台阶（Lipschitz 实测 0.47 ≤0.7）。弃案：纯调参不删 containment（台阶只能缓解，藏头与「腿从盆里长出来」语义互斥）；cx 按站点锚比例（thigh/hip 比 → 腿围漂移复发）；中线等距双柱（仍无臀腿连续）。病理体型（胖腰粗腿 130/70/90 等 6 夹具）不再有降级路径：粗腿 gap 下限绑定 = 「腿确实比盆宽」的诚实降级（cx ≥ a−1.5 恒正，实测 min 5.25）。
- **2026-09-10 五轮随行：kLL 0.6 / collideSweeps 3 / 解耦②作用域 / 集成 −0.1 容差**：①设计大腿接触带（gap 梯子 68~78）出口两内侧壁切向擦碰 |∇F|≈0.14 放大 k/4，kLL=1 时 (0,68,0) 点法向外凸 1.774 超 garmentGap=1.2 → kLL 1.0→0.6（1.065 预算内；解耦①是全局最终裁决）。②解耦②轴心原点收窄到 y≥70 会阴楔区：y<70 的轴心 θ≈0° 射线落设计接触带腿间凹谷（并集凹区，实测外凸 1.4-1.9 超预算），布料按凸支撑 surfaceRadius+gap 摆位永不进谷（该方向支撑=腿 bF≈9.2 远在谷外）、谷内外凸由①法向测度把守——非为过测调作用域，是布料真实可达域的诚实口径；腿心原点全域实测 0.000。③三管共覆盖 y 段扩为两条（腿头带×骨盆 + 大腿接触带两腿互叠），collideSweeps 2→3（2 遍最坏残差 0.13、3 遍 0.09）；接触带内缝焊合约束把缝线粒子拉向中线与碰撞推出互抗的力平衡残差（y 66~71、全部粒子仍在真实表面外 ≥0.21cm、零可见穿模）由集成测试 −0.1 容差收纳（沿 2026-09-09 先例口径），集成测试时长 33-36s 需 60s 超时。④金标重写：fitting3d.test.ts 删「嵌入环 containment 金标」换「人体形状不变量」9 条（Lipschitz/最宽点≈臀/站点锚/膝踝比例/小腿肚/接触带 ≤10.5/|cx|≥5/P.a 单调/头带贴盆）+ 接合支撑金标新锚（SR(79)=14.941/SR(82)=P.a/SR(76)=8.446）；skin.test.ts 头注重写、9.9 字面量改 sectionAt 派生、(0,68,0)<0 接触带符号、穹顶楔底锚点移 (0,70,6)。§10.11「互嵌接合」段重写为现行「人台人体化」规格。
- **2026-09-10 裤子渲染临时屏蔽（3D tab 保留）**：用户口径「先不在人台上传上裤子，只显示人台」——人台形状视觉验收期不渲染裤子。Fitting3DView 顶部 `RENDER_GARMENT=false` 单开关：布料四半片/腰头环带/结构线不建不挂，热力图/布料透明度/重新试穿/解算状态/frozen 角标等布料相关控件同步隐藏，3D 场景只显示人台 + 视角/截图/体型/松量读数；useFittingSolver 契约不动（照常解算，man/buildVersion 仍驱动人台随体型/围度滑杆重建），fitting3d 模块、引擎 fitting 出口、78 例金标全部保留，验收通过改回 true 即恢复。首版误屏蔽整个 PreviewPane「3D 试穿」tab，按用户纠偏收回（tab 是体型抽屉唯一入口，验人台恰恰需要它）。
- **2026-09-10 六轮：人台形体塑形四件套（矢状 S 曲线 + 单调样条梯子 + 环棱线消隐 + 脚盒/肚脐；代码注释标「第四轮人体化」，现行规格权威 §10.11「形体塑形」节）**：动因——emergence 重写后人台仍是「直筒背/等粗腿/腰线弱/无脚无脐」，VLM 侧面解剖核对判腰椎内凹、小腹、腘窝均「看不出」：环模型按围度站点放样只约束周长，矢状剖面（前后半深比）与环间过渡形状无控制。前置二轮微调：fb/bb 矢状偏置收窄骨盆壁 + ham 锚 + worstLip 阈值 0.7→1.05（语义=台阶检测非斜率美学——收窄后头带凸坡陡段+裆环 girth 增长的真实斜率 1.016@78.5；软化坡会压低骨盆×腿穿越角威胁 kPL 外凸预算，按实测放阈值而非改形状），接合金标随校 SR(79) 14.941→14.580、SR(82) 14.787→13.342、SR(76) 8.446→8.597。环棱线根因（两源同批删）：环间线性插值 ~2-4cm 折点被 MC spacing 1.0 如实画出 + kind 档位跳变（ring map 二值切换）——形状参数改 y 向 monoSplineAt 连续梯子、sectionAt 环间 a/bF/bB/e 单调样条（cx 保持线性）、骨盆上段采样步 4→2。monoSplineAt（Fritsch–Carlson 型：局部极值锚平导数、|m|≤3×min 割线——无过冲且外缘 Lipschitz 界仍构造可证）+ sectionShape bias 形参周长自标定（bias 只改形不改围：改 SAGITTAL 不破围度金标，但 a 漂移需随校接合锚）。腰最窄肋锚（腰+9、1.06×腰）+ torsoTopRatio 0.88（躯干上段双向展开，腰成全局最窄）；X 站姿 gap 重调（knee 1.5→0.8/calf 1.6/hem 1.8/ankle 2.2，**cx 不进样条红线未破**）；外缘梯子样条化 + calf 显式锚 0.80。Fritsch–Carlson 双端零斜率最平插值教训：裆→膝 36cm 长直段把曲率摊薄、腘窝 dip@膝上 2 仅 0.07cm 任何分辨率不可见——ham 锚+腘窝上沿 rim 锚（膝上 4cm）把「ham 饱满→陡降入谷」集中，dip 0.07→0.28→0.47，回归守卫 lb(44)−lb(42)≥0.2。脚/脐随行修复三处：①smin +Inf 哨兵必须显式短路（多项式代数式 h=1 处 Inf−Inf=NaN，未短路带外层全 NaN、MC 出空网格）；②脐场 k0>3 早退（否则 skinField(0,86,0)≠骨盆场破单侧性金标）；③bbox 脚盒界 X/Z 漏盒心偏移 |c|（前盒 ≈12.75）把脚尖切出 ~2cm、悬挂边破水密（病理夹具实测「边恰共享 1」46~54 条）。踩坑：脚盒心 y 必须局部系（地面+半高−踝高；footField 以 origin 平移后求值，存世界 y 把脚沉地面下 |yAnkle|）。红线遵守：seams.ts/pbd//collide.ts 零改动、kPL/kLL/kLF 全程未超预算未调、garmentGap 1.2 不动。金标 fitting3d 26/26 + skin 16/16 + integration 3/3（PBD 最坏侵入 skin 余量带 0.283→0.082 @y66.0，集成容差 −0.1→−0.15 收回、全部粒子仍在真实表面外 ≥0.218cm，collideSweeps 维持 3）。
- **2026-09-10 六轮验收：VLM 视觉验收循环（四轮解剖核对 + 五轮腘窝推深）+ 裤子渲染恢复**：验收 harness 口径（复用）：Playwright 截图（localStorage 注入 ylpattern.draft.v1/ylpattern.body.v1；antd 两字 CJK 按钮标签渲染插空格「侧 面」需 exact 匹配；addInitScript 收单个包裹参数对象）→ 本地 PNG 经 Read 上传 CDN 签名 URL → analyze_image（仅收远程 URL）；VLM prompt 必须声明已知简化（无头/无臂/两段圆角盒脚/单色无纹理/~1cm 网格/地面格网）防假缺陷报告；vitest 吞 console.log，数值采集用探针测试 throw new Error(JSON.stringify(vals))（用后即删）。四轮收敛：腰椎 0.73→0.70→0.67（两轮「看不出」→符合，谷深 8.06→7.77→7.48）、小腹 1.18→1.24→1.28（「接近直筒」→符合）、腘窝 0.85→0.80 仍不可辨（dip 0.283cm≈1.2px @~4px/cm——几何存在但画不出）→ 第五推 1.08/0.96/0.85→1.12/1.00/0.80（dip 0.471≈2px）→ 判「有内凹（轻中度）·收—凹—鼓节奏可辨·本轮膝窝项目可通过」，腰椎/小腹/小腿肚/臀无回退。**渲染分辨率天花板结论：几何凹陷 <~0.3cm 在当前渲染分辨率（~4px/cm）不可辨，推形状而非改渲染。** side_low 低角度机位两轮不可用（Playwright 拖拽把相机怼进躯干局部）——判 harness 相机问题非模型缺陷：其核对项（贴地/外八/双脚落地）已被正/侧/病理视角全覆盖（p1_front 病理 130/70/90 正面 6 项全过不炸），停止迭代。验收清单全过：腰凹/臀峰位/臀底褶/小腹+脐/腘窝/小腿肚/跟腱/腰最窄/双脚落地 + 病理不炸。五推重校仅 sr76 →8.597、lb74 →8.602，其余锚零漂移。裤子渲染恢复：RENDER_GARMENT 改回 true + rebuild，布料四半片/腰头环带/结构线/热力图全量恢复，目检通过（正 8 项/侧 6 项全过；斜 45° VLM 曾报「右腿纵向裂口」，纯侧视定向复核证伪——近侧腿外缝全程连续无缺口，系斜视角两腿间暗带被误读为裂口，视角伪影非几何缺陷）；§10.11 临时屏蔽句同步改为已恢复口径。
- **2026-09-10 七轮：下半身全面打磨（明显缝隙/自然圆润臀峰/腰髋衔接/小腹-裆曲线，5a/5b/5c 三子轮演进，79/79 全绿）**：用户四点反馈（①正面无盆骨感、腰侧连接大腿深凹 ②大腿粘合应明显缝隙+小腹到裆应微凸曲线 ③侧面臀凸太离谱、未顺滑延伸到大腿 ④整体再找优化点）+ 三拍板（缝隙=明显：裆下 ~5cm 内分开、大腿中段缝 ≥2cm、膝 ~1.2、踝 2.4；臀峰=自然圆润；范围=下半身全面打磨，肩/胸/颈/头不动、裆位/腿身比由尺寸单锁定）。根因（dump 实测到锚）：(a) 腰侧深凹 = 矢状峰锚 glute 1.42/belly 1.28 保围缩 a@82 谷 13.34 + 头带挖盆 1.2 + 盆→腿外缘接管点陡交角 + kPL 内凹圆角，四因叠 y80-84；(b) 腿粘连 = gap 过零 y68、68~78 接触带微缝 0.3-0.8cm 在 kLL 桥接与 MC 可辨阈之下视觉焊死；(c) 小腹-裆台阶 = 盆 bF@78 10.08 vs 腿 8.45（差 1.63）+ belly 单锚尖峰「孤立乳头状凸起」；(d) 臀峰 = 1.42 架状 + 峰位臀线下仅 2cm + 臀底盆/腿 bB 台阶 1.21（腿首锚硬编码 1.0）。**否决盆-腿壁运行时混合机制**（破单原语站可证性/cx Lipschitz/与 smin 职责重复/金标 mass re-dump）——沿用「先验锚点 + 金标回归网」。常数演进（→5b→5c 终值）：gap crotch −1.5→−2.5、新 gapOpenBelowCrotch 5（过零锚 68→73）、midThigh 0.3→1.4、knee 0.8→1.2、ankle 2.2→2.4、新 midThighGirthRatio 0.85（开缝联动收腿管）、headSteepDepth 1.2→0.8（停止挖盆壁）、wedgeDrop 5→3、crotchGirthRatio 0.85→0.83、glute 1.42→1.30→1.22、glutealBelowRatio 0.45→0.50（峰位臀线下 4cm 真人位）、lumbar 0.67→0.77（浅谷）、crotchBack 0.92→0.88、belly 1.28→1.18→1.16→1.10、crotchFront/wedgeFront 0.96/0.90→0.80/0.80、新 thighTopBack 1.20（裆首锚，后壁交接差 1.21→0.25）、poplitealRim/Bias 1.00/0.80→0.97/0.84（环缢减半 dip 0.47→0.30，≥0.2 守卫存活）、calfBack 1.12→1.18→1.15→1.12、achilles 0.92→0.95、calfBelowKnee 7→5（解剖位）；5b 新增：outerAtThigh 0.97→0.955（0.97 与臀 15.70 夹 78-84 段谷成双峰）、gapOpenGirthRatio 0.89、fillBelowHip 5/fillGirthRatio 0.995 臀下填充锚（独立前瞻表手法同楔表，a@82 14.05→14.9、腰髋谷深 1.65→0.8 自然 hip dip 级）、bellyUpperRim 0.15/1.06 与 bellyRim 0.65/0.97 双沿锚；5c 新增 calfTaperBelow 12/calfTaperGirthRatio 0.93/calfTaperBias 1.11（小腿峰下快-慢凹降）。方法论三教训（已沉淀 §10.11 形体塑形节）：①**monoSlope 平顶带**——峰锚与端锚均被钳 0 导数时两段 Hermite 双端零斜率拼成「平台-升-平台-落」水平棱线（小腹屋檐三轮根因），解法=峰两侧插非极值沿锚摊开曲率+压峰幅；②**CR 平顶切向鼓包**——[crotch,thigh] 54/54 平顶使 CR 在 74 环鼓出 53.5、gap 绑定下 outer@74≈15.9 反超臀带成次峰（VLM 正面「下鼓包@大转子」），解法=收口锚 0.89；③**近线性直落**（calf→hem 围度直锥）解法=峰下收拢围度站。金标有意重校清单：缝隙带 ≤10.5→≤6.0 + inner 锚重设（@72≥0/@66≥1.3）、SR(79) 14.941→14.580→14.902、SR(82) 13.342→15.36、SR(76) 8.597→9.627、lb74 8.602→9.07、腰椎谷 ≤0.94→≤0.98、臀峰 ≥1.05→>1.01、outer 站锚裆 15.393→14.996/踝 10.54→10.685、skin 符号 (0,68,0)负 → (0,75,0)负+(0,72,0)正、解耦②轴心 y 列表 [70,72,73,74,76,78,79,81,84]、新增不变量盆-腿壁前后交接差 ≤0.6 / 腰髋沟上限 outer@82≥W_h−2.2 / inner@膝≥1.1。harness 踩坑两则：vitest toBeCloseTo(x,2) 容差=0.005 非 0.5（重校锚值时误以为半格容差）；解耦② 30s→90s、集成 60s→120s——全量并行 CPU 争抢下文件级时长翻倍是 harness 预算非几何断言（几何全过），按实测放超时防间歇假红。红线遵守：seams/pbd/collide 零改动、kPL/kLL/kLF/garmentGap 全程未调、cx/gapLadder/headLadder 线性未破。
- **2026-09-11 七轮验收：5b/5c VLM zoom 两轮复验 + 七图全套逐项验收通过**：5b zoom 复验四项裁决——小腹「屋檐有缓和仍在」→5c 压峰 1.16→1.10；小腿「下降段太直、鼓起段太短」→5c 峰下收拢站；腰髋沟「浅而连续」过；缝隙三段可辨过。5c zoom 复验双过（小腹微凸光滑收进裆底仅极浅软组织凹陷带、小腿纺锤长弧）。全套七图（std 正/侧/背/斜/default + 病理 正/侧）逐项验收全过：正面外轮廓 G2 连续、hip dip 浅谷自然、腿缝三段可辨、小腹-裆连续微凸、膝无环缢；侧面 S 连续、**臀底→大腿后壁平滑无台阶**（高清全身图交叉核对推翻 5c zoom 低倍「轻度台阶」读数）、腘窝浅凹可辨、小腿肚峰位膝下 4-5cm 纺锤长弧；背面臀两半对称、臀底平滑、腿缝全程可见趋势与 gap 梯子一致；斜视「第一眼基本像真人女性下半身」、裆-腿根-腿缝分离干净；病理双图优雅降级（无破面/尖刺/NaN、双腿两管、封口完整）。**VLM 判读甄别方法沉淀**：①像素 cm 估算 ~1.2× 通胀、膝锚定不可靠——结构性质判读为准；②低倍/斜视读数与高清主视向专项矛盾时以专项为准（小腿「锥形管感」vs 侧视「纺锤长弧」、臀底「台阶」两例均如此裁决）；③「臀凸 3-4cm 偏保守/饱满度不足」=「自然圆润」拍板目标态非缺陷（用户否决的正是 1.42 架状臀）；④analyze_image 并行 >3 个触发 429 速率限制——分小批 2-3 个发；⑤回归甄别用旧图对照：default「膝下布料喇叭散开+前片分开露肤」经第四轮已验收旧图同部位对照确认同现象早已存在（且集成缝合闭合 <1cm 绿、本轮零改布料代码）——判 legacy 布料垂坠+半透明渲染误读非人台回归，留档布料专项。残留留档（裁决接受不改）：耻骨上缘浅檐口=腹股沟褶皱区真人本有；腿缝直缝=gap 线性红线约束；腘窝 dip 0.288 处渲染天花板 ~0.3cm；小腿快缓转接亚 0.5cm 折角；std_back/iso「髋上缘浅水平带」= 肋弓外扩锚 y≈107 拐点（腰上躯干段本轮范围外、幅度像素级 ≈0.3-0.5cm）。
- **2026-09-11 换轨调研与两项拍板（环模型 → 真人网格）**：七轮程序化打磨观感到结构性天花板，用户发起换真人网格。21-agent 调研矩阵：数据源候选 MakeHuman（CC0 资产 + AGPL 代码——许可分离可用）/SMPL 系（研究许可禁商用，出局）/商业人体 API（订阅+数据出境，出局）；两项用户拍板：①许可红线 = 仅商用安全许可（CC0/MIT/Apache），MakeHuman **只借 CC0 数据、绝不移植 AGPL 代码**（本仓只解析 .obj/.target 数据格式）；②集成路线 = B（自研 TS 稀疏 morph 引擎，不引 MH 运行时），红线 seams.ts/pbd//collide.ts 零改动、引擎零改动、BodyProfile 4 键 + UI + localStorage 契约不变、garmentGap/collideSweeps/collisionSkin 不动。流程重排（S9）：七轮人台改动先 commit 留档 → 并存开关开发 → **VLM 验收通过后才退役**（原计划先删后验，验收不过只能 revert，改后置）。
- **2026-09-11 换轨落地（P0-P4）**：`scripts/vendor_makehuman.py`（makehumancommunity v1.3.0 commit 1f508f60，CC0 全文随数据进 git `vendor/makehuman/` + PROVENANCE.md sha256 + .gitattributes -text 禁 EOL 转换保哈希）→ `public/bodymesh/{base.bin, targets.json}`。vendor 三数据级坑（先探针 dump 定案再动手）：① base.obj 是 **A-pose 绑定姿势**（腿环质心裆 ±9.6 外撇到踝 ±22）——`desplay_legs` 每顶点 XY 平面旋转（pivot 中轴、y 向 smoothstep × |x| 向权重，会阴中带 ≤2 刚性防撕裂），**measure targets 增量随各自顶点同旋转**（morph 语义 = 旋转后场）；② 旋转腿微缩脚下探 → re-zero +3.1cm，height/裁切自检基准重算；③ 臀地标带 [裆+1,+9] 落裆叉喇叭带（叉环伪围度 115 > 真臀 98）上移 [裆+6,+14]。运行时 `fitting3d/bodymesh/` 六模块：load（promise 缓存）/morph（稀疏叠加+地标顶点索引重读）/align/**分段线性 y-warp**（sole-ankle/knee/crotch/waist 四节点精确过点+顶上斜率 1.0；原三点 LSQ 仿射把腰站映到髂骨带、weightClamp 逼 2.0 压 native 82→66，warp 后回 ~−0.2 量级）/calibrate（围度闭环查预标定 w=0/0.5/1 三点影响矩阵分段反演、阻尼 0.8、±1.5%）/heightfield（三管 R(y,θ) θ256×0.5cm、骨盆裆下钳位 3/腿头带 7/腿底 hem−2）/build（Tube 环 2cm + hf 柄）。适配层：`SectionParams.hf` 可选柄、`radiusAt` 短路查表返回**裸 R**，`surfaceRadius` 凸支撑近似角向起伏残差实测最坏 ~1.5cm 由 preRelax 200 收敛；渲染 sourceMesh BufferGeometry 直出。合成金标 bodymesh.test.ts（node 无 fetch/public）+ 真数据金标 Python 侧 test_vendor_bodymesh.py 9 例（skip-if-absent 先例）。
- **2026-09-11 裸台验收 + 旧模型退役执行**：裸台八图 VLM 逐项通过（std 正/侧/背/斜 + 病理「胖腰粗腿」「腿比盆宽」各正/斜；prompt 声明已知简化无头/无臂/腰上截断平盖/单色无纹理/真脚）→ 退役：skin.ts + skin.test.ts 整删（SDF 蒙皮）、mannequin.ts 环放样整链删（BODY_RATIO/SECTION_PRIOR/SAGITTAL/FOOT_PRIOR/NAVEL_PRIOR/SKIN_PRIOR + USE_BODYMESH 并存开关）、fitting3d.test.ts 形状金标段（worstLip/缝隙梯子/矢状 S/脚盒/脐/接合锚）删；继承口径：radiusAt 单位方向向量、collideSweeps=3、集成 −0.15 容差。tsc + vitest 60/60 全绿；引擎 pytest 照旧（引擎零改动）。
- **2026-09-11 退役当日回归：踝节点修正（脚进高度场污染脚口带）**：退役前着装回归发现初态穿透 263/5606 最坏 11.23cm 全在 y=0.6/1.9 θ≈0°——根因 = 纵向对齐 sole↔hem 恒等配对把真脚（mesh 腿短脚高 13.5cm）抬进腿高度场行内：脚趾在腿轴前方 ~16cm，按角度 splat 成 θ≈0° 假半径 15-18 污染脚口带碰撞把布推爆（旧环模型「脚在裤口下、布料不可达」不变量被对齐语义反置）。修法（仅适配层）：align 踝节点路径——踝 → hem−`legBelowHem`(2cm) 精确过点、脚底沿 slope 1.0 保真脚高、真脚整体落腿场底之下；ankle 地标缺省退化 sole↔hem（合成网格兼容）。修后初态残差 169/5606 最坏 1.51cm（腿肚/大腿角向起伏带 = surfaceRadius 原点支撑近似固有，非脚污染），集成初态容差 −2.0 收纳、终态 +skin−0.15 强断言把守。副作用：膝-踝段 y-warp 斜率 44/34.562≈1.273（小腿重拉伸，vs 环模型已验收 ×0.875 压缩）——裸台比例复核着装视补（见下条）。
- **2026-09-11 换轨收口：着装视 8/8 + 裸台比例复核裁决 + 验收 harness 全坑留档**：①着装视 VLM 逐项全过（std 正/侧/背/斜 4 视 + 病理「胖腰粗腿」「腿比盆宽」各正/斜 2 视；病理 prompt 声明「同一标准码版型穿病理体型，紧绷/合不上属预期」防假缺陷）——内缝/侧缝/后中缝/裆底全闭合，无飞布/穿模/撕裂，悬垂自然。②裸台小腿比例裁决 = **非缺陷**：节点侧实测膝站 y=41.5、踝(hem) y=0 → 小腿 41.5cm，正是引擎膝线公式 `(脚口0+立裆77)/2+3` 的固有结果（真人 165 膝-踝距 ≈37-38，+10% 偏置来自打版膝线惯例，引擎红线禁改）；人台忠实对齐 payload 站点（P2 契约、围度闭环在站点高度切网格）是正确行为，×1.273 重拉伸只是把 MH 原生小腿映到版型跨距的机械结果。VLM 侧视判正常、正视报「小腿偏长~5%/膝位偏高」（数字口径通胀）与该偏置同向自洽——正侧视矛盾时节点侧数字裁决（七轮验收甄别口径再立功）。③harness 坑全录：无头模式 rAF 完全挂起（fps=0，截图全是 t=0 初始态——std 斜视「纵向裂缝」假缺陷即此，节点侧缝合 max=0.000/406 对证伪）→ 系统 Edge 有头 + 防节流三 flag；解算状态文案在 `.f3d-status` span（按钮文本恒「重新试穿」，按按钮文本轮询永不中——曾误判解算挂起）；canvas 出现瞬间的 fps 探针恒 0.0 是同步构建阻塞主线程 >3s 探针超时的假象（事后恢复 60-73fps，勿误读为 rAF 挂起）；裸模式（RENDER_GARMENT=false）状态 span 不渲染 → 固定等待 15s；VLM prompt 解剖部位用临床措辞（服装 CAD 评审框架/骨盆区域下方/陷入人体网格内部/布料网格）——口语化裆底/穿模措辞触发内容过滤 400 code 1301。④清理：_debug_seam.test.ts（曾阻断 tsc）/ debug_seam.log / _diag.png / shot3d.mjs 临时 harness 全删，RENDER_GARMENT 恢复 true 重建。
- **2026-09-11 用户两报障修正：大腿缝隙过大 + 调大腿围变粗位置太靠下（vendor 数据级修复，runtime 零感知）**：根因（dump 实测）：① desplay_legs 只对直腿轴去外张、pivot 在裆上——裆下腿轴间距 ~19cm 原样保留（膝上内侧 gap 5.3~6.5，真人自然站立 ≤2）；② MakeHuman 原生 measure-thigh-circ 的 Δgirth 峰值带在 crotch−14（其 canonical 站），与 payload thigh 站 crotch−3 错位 11cm、站点灵敏度仅峰值 39%（比值 2.57）——围度闭环为凑站点围度把权重推爆、大腿中段撑粗根部反紧。修法（全在 vendor 数据链，TS 按 meta.targets 名消费、换 thigh± 内容即零感知）：**adduct_thighs** 逐 1cm 切片量右腿环内侧 gap，单侧共向平移 δ=max(0, gap−target)/2，分段目标大腿 2/小腿 3.5（膝线 crotch−32 ±2cm 线性过渡、踝上 6cm 渐入、脚部原生不动=自然外展站姿），δ 全程 1.6~2.2 保腿轴竖直；同高度共向平移无内部错动 → 水密/切片围度/地标/裁切面零影响。**derived_thigh_targets** 派生径向保形场以站 crotch−3 为中心（逐顶点相对该高度腿环质心的径向增量、cos² 窗半宽 18cm、峰值 0.8cm@w=1 站点 Δg≈4.15 与原生同量级、内侧 ×0.7 防病理权重两腿互穿、dy≡0 站高不漂）整替原生 thigh±，峰值/站比 2.57→1.02（峰值带 78.06 vs 站 77.6，错位 11cm→0.5cm）。守卫：金标 `test_thigh_gap_closed`（大腿主段 gap ≤3.2）+ `test_thigh_derived_band_alignment`（峰值带 ∈ 站 ±3.5 + 站点响应 ∈[3,8] + 腰/膝串扰 <0.3）；pytest 24 绿 + 前端 tsc/vitest 60 绿 + dist 重建。VLM 复验（截图实为着装视——RENDER_GARMENT 已恢复 true 的 dist，NUDE 模式只改等待策略；裸台保证由 Python 金标把守）：std 腿间缝隙 1~2cm 自然；legheavy（大腿围 80>臀围 75）增粗峰值在大腿根部且沿大腿单调收窄（旧「葫芦形中段膨出」消除）、病理着装 5/5（内缝/侧缝闭合、无陷入、紧绷带在大腿上段）；std 着装初审两疑点（两腿间「裂口」/裆部「破面」）聚焦复查确认是**背景色误读为裂口、褶皱明暗误读为破洞**（间隙内是浅色背景非肤色、暗区无网格缺失无皮肤透出），与 integration 数字金标（缝合闭合 <1cm、+skin−0.15 不穿透强断言）互证——「VLM 报几何缺陷先数字复算」甄别口径再立功。教训：**第三方 morph target 的 canonical 站与自家站点定义必须对位验证**——错位时闭环在错的位置推权重，站点围度读数「对了」但形状错。
- **2026-09-11 人台固定：估计体型从实时联动改一次性快照（用户口径「人台是不能动的，设置好多少就是多少，不可以随着裤子变化而变化」）**：原实现 estimateBody 随成衣参数实时重算、activeProfile 优先取实时值——拖快捷滑杆改腰/臀围时人台跟着变，用户否决（此前 Q&A 曾把该联动解释为「刻意的直连设计」，同日推翻）。改法：估计体型 = 首次出现有效尺寸（waist>0 守卫，防空面板 0 值固化）时快照进 `store.estimated`（localStorage 'ylpattern.body.v1' 既有字段，StoreShape 契约不变）持久化，此后成衣参数变化零联动；findProfile 搜索序 [预设, store.estimated, 自定义] 使快照天然胜出，实时估计仅作快照前兜底。显式刷新 = 抽屉按钮「用估计值」改「按成衣尺寸重估」（重估+落快照+激活一步）。连带：围度 200ms debounce 语义更新（只在切体型/重估时触发，保留防逐 tick 重建卡主线程）；松量读数 = 成衣围(随参数变) − 体型围(固定)，改参数时松量读数动、人台不动——语义更顺。组件层改动不进 vitest（无 jsdom 先例），estimateBody/bodyProfileStore 纯函数金标不变 60 绿 + dist 重建（浏览器刷新生效）。
- **2026-09-11 人台扭曲报障（快照固化极端体型 → 原生 morph 外推饱和）：calibrate 分档钳界**。用户截图报「人体形态扭曲」，node 数字复现（临时 _diag.test.ts 用真资产复刻 build 链）定位：人台固定改造后，首次有效尺寸快照恰在快捷滑杆拉满（腰 120/臀 130/腿 80/膝 60）时固化 → estimateBody 产病理估计围度 {腰 118.5, 臀 125, 腿 76, 膝 57} → calibrateWeights 把原生 target 推进 w>1 线性外推（预标定三点表只覆盖 w∈[0,1]）满钳 2.0——原生 morph 的 Δ 位移分布从未为 w>1 作者化，髋部大转凸包/膝部肿胀/大腿波浪鼓包与截图吻合（双侧对称系统性形变 = 参数化外推特征，非随机噪声）。**排除项**：thigh± 是派生径向保形场（vendor 侧 cos² 窗构造、w=2 平滑、legheavy VLM 验收过），非元凶；数字证据 est-extreme 腰+/膝+ 双双钳死 2.0、converged=false，对照 std-verified 全部 ≤1.32 收敛。**否决的替代方案**：① 估计围度钳到 w=1 可达域——mesh 臀底 89.1@w=1，瘦向（臀 70）体型无法表达、钳制会伪造松量读数，破坏双侧语义；② 原生钳 1.0——默认路径膝+ 1.297 会新引入不收敛。**落地**：`priors.ts` weightClamp 语义收窄为派生场外推钳 + 新增 `nativeWeightClamp: 1.5`；`calibrate.ts` 按站点分档（mk==='thigh' 宽钳 2.0，其余 1.5），外推上界砍半、残差披露照旧。金标：bodymesh.test.ts 合成资产加 thigh+ 替身 target（腿环 y∈(20,30) 绕腿轴 ×10% 解析可标定）+ 两档钳界测试（极端腰→waist+ 恰钉 1.5 欠量披露、极端腿→thigh+ 钉 2.0）；诊断复验 est-extreme 四权重 1.5/1.5/2.0/1.5、est-default 膝+ 1.297 收敛不变；vitest 61 绿 + dist 重建。**用户侧恢复**：已固化的病理快照不会自愈，需「体型设置」恢复正常滑杆后点「按成衣尺寸重估」或直接选预设（标准 165/66A）——代码修复只保证极端体型降级为自然大码形态 + console.warn，不 silently 篡改用户录入。
- **2026-09-11 二轮报障（默认路径也「不平整」→ 差分雅可比重构 + 原生钳 1.0）**。用户复报「没有解决，很不平整，是不是 MakeHuman 原样就坏」。诊断（_diag.test.ts 环剖面，用后即删）：基网格 w=0 平滑——|d2R0| 峰 1.07 即解剖曲率（小腿肚/膝/臀）、左右不对称精确 0.000、std≈基线曾被 VLM 验收，**MakeHuman 数据无罪**；鼓包全在 morph 权重层：① 默认路径（成衣 70/96/58/46 → 估计 68.5/91/54/43）膝+ 外推 **1.297**（一轮钳 1.5 没罩住默认路径——1.297 < 1.5）在膝带 y≈42-50 造 +1.2~1.5cm 半径局部鼓包带；② 预标定矩阵是**单 target 对角近似**（vendor 脚本逐 target 单独施加、在 canonical 站量测），测不出 cos² 窗重叠耦合——实测 knee+1.0 向 thigh 站溢出 ~+8.6cm 围度、thigh 场向 hips 站 ~2.5cm/w——对角迭代虽收敛但落在大对消权重上（std 曾 hips−0.68 + thigh−1.32、est-default hips−0.61 + thigh−1.58），把局部凹陷/凸带放大成用户可见的「不平整」。**落地两件套**：① `nativeWeightClamp` 1.5→**1.0**（作者化域 [0,1]；一轮否决「钳 1.0 破默认路径膝+1.297」的前提随联立求解消失——欠量 −4.7% ≈ 半径 0.35cm 视觉不可见、console.warn 披露）；② `calibrate.ts` 重写为**运行时前向差分雅可比**：当前权重点实测 J[j][i]=∂g_j/∂w_i（扰动方向=该站需求方向），Gauss-Jordan 部分主元联立解最小权重（奇异回退对角步进），`priors.ts` 加 `calibFdStep: 0.1`；三点表降级为诊断元数据（vendor 金标仍把守 target 对位）。**重构过程中踩坑（假阴性教科书案例）**：差分分母忘了带方向号——withSigned 在需求为负时等效改变的是 −h、除以 +h 让全部「要变小」的站斜率反号，求解器满舵冲 + 钳（全 +1.0/+2.0、残差 +21%~+32%）；合成测试目标全取正需求（×1.02/×1.05/×1.3）全绿测不出，真数据腰/臀全部小于基网格立刻爆——**负需求（缩小方向）用例须进联立求解器的测试意识**，现有合成金标以「耦合联立」测试守住正需求溢出路径、负需求路径由真资产 integration + 诊断复验把守。**复验数字**：std 收敛（残差 ≤0.6%，权重 {waist−0.33, hips−0.71, thigh−1.50, knee+0.08} 与旧解同族）；est-default {waist−0.10, hips−0.63, thigh−2.0 宽钳, knee+1.0 钳}——膝带外推鼓包消失（ΔR 二阶差分波浪度 d2max 1.656→1.229，且膝带残形是 w=1 域内作者化膝形 ±0.3 半径；躯干 0.836 低于 std 自身的 1.297）；est-extreme 全 + 钳诚实降级照旧（超网格可达域，warn 披露）。vitest 63 绿（新增「耦合联立」金标：合成 waist+ 场同时缩放 waist/hips 两站、hips+ target 不存在——差分雅可比量出溢出后由 waist+ 一并满足且 hips 权重保持 0；旧对角反演在 hips+ 行 null 时 hips 误差恒 5% 永不收敛，反向钉死重构必要性）+ dist 重建。
- **2026-09-11 三轮报障（腿部扭曲 + 大腿→膝台阶）：thigh 派生场非对称窗 + knee± 派生整替**。用户报障：① 腿部中段整体往中线收拢、视觉扭曲；② 调大腿围时大腿→膝应缓慢递减；上半身正常。VLM 读图定位：大腿上段外侧球状鼓包（与臀剪影脱节阶梯）+ 大腿中段~膝双腿贴拢/穿插；小腿/脚踝/躯干正常。node 逐顶点复算坐实三根因（全在 vendor 派生层——运行时无绕全局 X=0 缩放、morph 纯超叠加）：**RC1 死区**——对称 cos² 窗半宽 18、站 crotch−3 → y≤59.56 精确归零，膝站 48.06 上方 11.5cm 死区不受大腿围控制（Δg@w=1 实测 4.14@77.5→1.08@65.5→0@≤59.5），「上段鼓包+下段细」台阶即此；**RC2 原生 knee+ 内侧剪切（症状①主凶）**——原生站点窗 y∈[30,68.7] 上缘 y∈[57,66] 是纯内侧 −x 平移剪切边（dz≡0 逐顶点核实、内柱 dx=−0.543 外柱≈0），径向主体却在 y≈45-55；原生膝围 34.38、典型输入膝 43 → 权重 (43−34.38)/6.44≈1.34 常年钳 nativeWeightClamp=1.0 → 剪切边永久生效，膝间隙 2.00→0.01~0.3（adduct 该带 δ=0，实测排除）；**RC3 canonical 错位**——原生 knee± 峰值 y≈45.5 vs 我们膝站 48.06，且满钳 1.0 最大膝围只到 40.82 够不着常见输入。**修法（Fix A+B，全 vendor 数据链、TS 按名消费零感知）**：thigh 场改非对称窗（下半 half_dn = y_st−knee_y ≈29.5 在膝站精确归零、上半 half_up 15；站点响应 4.14 不变，根→膝 Δg 单调递减 = 用户要的「慢慢变小」）；knee± 派生整替（抽共享构建器 `_derived_radial_field`，站=膝标 48.06、half 15/15、amp0 1.25 ≈ 站点 Δg/w ~6.4、medial_att 0.30 = 间隙预算解析上界 (2.0−0.5)/(2×1.25×2.0)，满钳 w=2 间隙恰落 0.5 floor、可达膝围 ~47.2；remap 循环跳过 thigh+knee 两站；槽序 [waist±,hips±,thigh±,knee±] 不变、load.ts SLOT_ORDER 零改动）；calibrate 钳位分层 knee 并入宽钳（thigh/knee weightClamp 2.0，waist/hips nativeWeightClamp 1.0）+ 修正过时「thigh 场向臀站泄漏 ~2.5cm/w」注释（实测 ~0，腿块雅可比近对角、差分联立保留罩躯干耦合）。**否决 B2（径向投影改写原生 knee）**：投影不消上缘剪切边、也不解决满钳够不着——整替是唯一同时修 RC2+RC3 的路径。**代价裁决**：原生 knee+ 顺带的小腿 +2.78@y36.5 派生尾巴只 ~+0.8 → 高膝围档小腿每侧 ~0.28cm 变细，VLM 复验小腿正常，接受。**金标**：vendor 13 例（thigh 场扫描下界 −30→−34 + Δg@crotch−15 ≥2.0 先红后绿钉死死区修复 + 峰下单调；新增 knee 对位 test_knee_derived_band_alignment + 间隙 floor test_knee_gap_floor）pytest 26 绿；前端 bodymesh.test.ts 63 绿（thigh fixture 改锥形窗 + 新合成 knee+ target + 钳位分层 knee 极值入宽钳档）+ dist 重建。**node 数字复验（_diag 临时测试，用后即删）**：est-default 膝+ 1.60 不再满钳收敛（旧钳 1.0）、宽钳 w=2 间隙 0.62 ≥ floor、死带中点 Δg ≥1.0、thigh− w=2 平滑。**VLM 三档复验与甄别**：est（典型）5/5 全正常；legheavy（72/100/62/46）「髋侧鼓包/小腿不对称」读数 node 复算裁决为**布料伪影**（hips+ 权重仅 0.195 + 髋带围度严格单调无局部峰 bump=0.00 + 成衣 96/58 小于人体 100/62 布料绷紧，VLM 亦建议改版型松量）；std（66/90/54/35）「髋球包 + X 形」读数与像素近同构的 est 判读矛盾，node 三断言裁决为**误报/布料**（髋/大腿/小腿三带围度局部峰全 0.00、左右镜像 <0.1cm、大腿内侧间隙较基底最多收 0.221cm——hips− 收窄骨盆的合法效应，X 形需 cm 级收拢；成衣膝 46 vs 人体 35 富余 11cm 垂坠）。「VLM 报几何缺陷先 node 数字复算」口径三连立功。**harness 新坑留档**：① analyze_image 响应缓存按 CDN 路径 ~15min + 上传层内容去重（字节相同的新文件名回落旧对象）——同路径/同字节复读拿到旧分析（std 首轮真空态分析被缓存回放两轮误当真）；每轮分析必须全新文件名且字节必异，判读存疑先像素降采样网格（40×25 亮度字符图，零成本区分真空态灰块 vs 已渲染双腿柱）自查截图内容；② 冷启动时序实测 ~10s「计算中」+ ~38s 解算至「已稳定」共 ~48s，solver 收敛后**永不自动 reset**（「已稳定」与空态 overlay 先后观察到是截图时机/缓存问题非状态翻转；空态判据 `.fitting3d-loading` overlay 与「已稳定」互斥）；③ 近同构图 VLM 判读可矛盾（est 干净/std 报缺陷）——像素网格对照 + node 数字是终审，不追加 VLM 轮次。
- **2026-09-12 四轮报障（裸台「球包+腿往中线收拢」原样复发 = adduct 整段平移撕裂臀线；上轮修复层错位 + 资产缓存双根因）**。用户再发裸台正视图截图：「一点变化都没有，没有修复啊」——大腿外侧球状鼓包 + 髋-大腿交界凹口 + 双腿往中线收拢，与三轮报障前同貌。**两层根因**：① **修复层错位**——三轮修复全在 morph 目标层（thigh/knee± 派生场），而裸台形态由基底几何决定；_diag4 旧 vs 新 targets 在 legheavy 档裸台外缘最大差 **0.00cm** 数字坐实（morph 目标只影响调围后的形态，且围度类扫描对 x 向侧向鼓包天然盲区——球包不增周长，围度 bump 扫描测不到，三轮 VLM 验收的「无髋侧球包」结论即被此盲区误放行）；② **真根因（stage-trace 逐高度对照）**——`adduct_thighs` 共向平移 δ 在裆下即达全量 ~2.2cm：desplay 后外缘本是平滑锥线（18.32→16.90，每cm 降 ≤0.2），平移后 18.32→17.73→16.74→15.95（**3cm 内撕出 2.6cm 台阶，每cm 0.85~1.0**）=「球包+凹口+腿往中线收拢」三症状同源——大腿干被从臀线正下方整体拽进 2.2cm。**修法：δ 叉部 12cm 线性渐出**（`fork_ramp_cm`，δ × min(1, max(0, ((crotch−2)−y)/12))）——臀线锚定不动，gap 剖面变「裆下 ~6〔fork≈0 保留臀褶解剖自然分开〕单调渐收、裆−16 处到 ~2」（=环模型时代用户拍板「裆下 ~5cm 内分开、大腿中段缝 ≥2」），腿轴由竖直改自然内斜（股骨斜度 ~2.3°）；平移是刚体 → 围度/地标/水密/裁切面零影响不变。**自检层四连踩坑**：① 轴共线 0.8 阈值拍脑袋错红——pre-adduct 解剖本身弯 0.95、旧出货 1.8，改「散布 ≤2.5 + 方向守卫（低处不得更外 >1.0）」，金标随之 test_leg_axis_vertical→**test_leg_axis_natural**；② 自检4 方向写反（rises=gaps[i]−gaps[i+1] 量的是骤降），渐出带正常收敛被误报 0.90——**守卫公式先在已知好数据上验方向**；③ 自检3 `_frange` 不支持负步长 → 采样带空、cliff=0.0 **vacuous pass**（真值 0.35）——与手工复算矛盾暴露，升序采样改相邻差方向；教训「守卫必须证明自己活着（真值≠恒 0）」；④ gap 剖面「绝对单调不升」与臀褶解剖相悖（pre gap 裆−2→−4 本自然回升 ~0.9、fork≈0 处 post 忠实跟随）→ 改 **pre 相对口径**（post 回升 ≤ pre 回升 +0.3；δ 沿向下单调不减 → 结构上 post 回升恒 ≤ pre，唯 δ 回摆可红）；另注意 desplay 的 crotchRaw 估计与出货 meta landmarkHeights.crotch 相位差 ~3cm——in-pipeline 自检与出货网格同名 d 值不同，探针对照易假性矛盾，终审以出货 base.bin+meta 为锚。**金标**：test_thigh_gap_closed 带分流（主段裆−30..−16 ≤3.2 + 渐出带裆−8 起单调 + 顶部裆−6 ≥4.5 防过度内收 + 裆−16 ≤2.5）；新增 **test_outer_silhouette_continuity**（正视外缘每cm 骤降 ≤0.5 + 臀锚裆−1 ≥17.8）钉死球包回归；pytest 双文件 27 绿；vitest 63 绿 + dist 重建。**资产缓存击穿（「没变化」的另一半）**：base.bin 更新后浏览器 HTTP 缓存照常命中，用户端可能一直在看旧人台——vendor targets.json 新增 `baseSha256`（bin 内容指纹前 8 位），load.ts 先 `cache:'no-cache'` 拉 targets.json 再以 `?v=<sha8>` 拉 base.bin（URL 变即击穿；旧 meta 无指纹回落无参路径）。`RENDER_GARMENT` 由改源重编改 **URL 参数 `?garment=0`**（裸台核验免重编；裸台仍需合法 draft 参数——左栏 measurements 空时 fitting POST 422、人台不渲染，Playwright 需注入 'ylpattern.draft.v1' 种子）。**复验链**：node 复算出货 base.bin 外缘旧台阶行 18.33→17.97→16.99→16.01 消除（现 18.34→18.33→18.28→18.16→18.05→17.70，max 0.35/cm 平滑内斜）、左右镜像 0.00、大腿环围度刚体差 0.045cm；Playwright 裸台正/斜 45° 双视角截图（坑：antd 两字按钮 DOM 文本插空格，`text=/正\s*面/` 才中；默认取景裁掉臀部，需点正面预设+滚轮缩小；像素网格自查后再花 VLM 钱）→ VLM 五项（大腿外侧连续/双腿姿态/大腿→膝渐变/臀腿过渡/小腿脚踝）全过，总判「几何形态整体正常」。**方法论**：验收扫描口径必须匹配缺陷方向——围度（周长）对侧向形状盲区，正视轮廓 max|x| 才是同口径；用户报「没变化」先验证用户端拿到的是不是新资产（缓存击穿先于算法怀疑）；守卫阈值用 pre 测量锚定而非拍脑袋（0.8 vs 解剖 0.95）。
- **2026-09-12 五轮报障（四项解剖形态修正：软化大转子折角/重塑裆部体积/腹股沟结构线/腿部肉感；两轮实施 + 三视图 VLM 复验通过）**：用户携裸台截图四项指令（①消除大转子尖锐几何状突起、平滑过渡为脂肪肌肉包裹的自然圆润弧度 ②会阴扁平方正改三维体积感、大腿缝自然并拢形态而非生硬挖空 ③雕刻下腹-大腿根清晰自然的腹股沟凹槽 ④打破圆柱直筒腿型、按股四头肌/内收肌分布做上宽下窄软组织起伏 + 整体 smooth）。**一轮**（全 vendor 数据链、TS 按名消费零感知）：hips± 派生首版（纯 cos² 峰钉臀站）+ adduct v2（smoothstep 剖面、fork 渐出 12，主段目标 1.8）+ 四解剖 sculpt 首版（trochanter −0.38@裆+3.5 / groove 0.65 直线 / lobes 0.55/0.45/0.35）；工程门全绿但 VLM 复验 3/3/5/5 硬失败——「测试绿 ≠ 形态对」，四 sculpt 存活守卫（26~29）与 hips 对位守卫正是本轮补课（此前 no-op 能全绿穿过）。**二轮根因（逐顶点数字复算）**：① hips± 峰钉臀站 86.6 而 mesh 实际最宽带（转子穹顶）在裆−2..0 = 站下缘窗值 <0.2 处——标定 hips− 0.53 只削站带（−1.38）不削穹顶（−0.18），两者间挖出 ~2cm 凹槽 =「平台+陡崖」方形折角主凶（一轮 trochanter 主削 −0.38@裆+3.5 恰落在该凹槽上加深台阶，帮倒忙）；② adduct v2 主段实测 1.6~2.0 全程不闭合，VLM 读作等宽槽（「生硬挖空」）；③ 单轨跨相插值 + per_side 二值翻转：叉部 1cm 内 cx 8.3→0.05 跨相内插把中线顶点横推 −1.32cm、镜像对顶点相向位移 2.59cm/w 互穿。**二轮实施**：hips± 重写为**平顶窗 + 双相轨道**（core [裆+1, 臀+2] 窗值恒 1 使穹顶/站带同进同退；腿相〔右腿环镜像质心 + 内向 gap 预算〕与躯干相〔全环质心 (0,cz)，中线顶点纯 ±z 镜像天然对称〕独立采样、切换带 ±1.5cm smoothstep 交叉渐变；dx 内向总预算钳；0.5cm 加密采样）；adduct v3 五控制点剖面 (裆−1,4.2)→(裆−12,1.2)→(裆−20,0.9)→(裆−26,1.8)→(裆−38,3.5) + fork 渐出 12→7（裆−10 即满量收叉口）；groove 0.65→0.80 加深 + 法向外弓 0.8·sin(πt) 凸向腹侧（腹股沟韧带弧度，一轮直线读作「平直刻痕」）；lobes 0.65/0.55/0.45；thigh± amp0 0.8→1.0 + 上半宽 15→8（裆线台阶交 hips± 场接管）；trochanter 削移至穹顶轻量化（−0.20@裆−1 半宽 4 + 下翼 −0.22@裆−7）；gap floor 0.5→0.65（0.25cm sweep w2 实测 0.451 漏穿守卫的采样插值边际）；新守卫 25（叉带自裆−4 逐 0.25cm 上扫 w1/w2 gap ≥0.5——旧单点裆−4 落 pre-gap 5.7 宽处漏叉顶）/ 26~29（四 sculpt 存活幅度带）/ 30（fork 带边长归一剪切率 ≤1.5/cm——按率不按绝对差：跨叉弓长边上躯干相外推 vs 腿相内收方向反转是设计行为 ~1.05/cm，行级台阶 4~7/cm 一扫即红）。**验证**：probe 全绿（hips+ 站点 Δg 15.16 / thigh+ 5.21 / knee+ 5.12、叉带 w1/w2 min gap 0.55/0.58、外缘 cliff 0.47）+ pytest bodymesh 15/fitting 13 + vitest 67（_diag 用后即删现 66）+ tsc 0 错；金标锚 hips 99.19 / thigh 53.46（较一轮 hips +0.43 / thigh −0.29：转子主削自裆+3.5 移到穹顶裆−1，臀站窗值 0 不再削、大腿上段近肩带被轻削——构建期自检已锁）。**三视图 VLM 复验通过**：正面 6/5/7/7、3/4 侧 8.5/8/6.5/8、裆特写 7/8/6/6.5（①3/4 侧「平滑连续圆弧未见折角/平台」；②mons 7、下腹过渡 8「饱满自然无扁平塌陷」；③特写 8「清晰斜向、走向正确」；④「锥度比例合理、膨隆柔和自然」）。**甄别两则**：① 正面远景报沟「走向水平平直」与特写「斜向正确」矛盾——构造即斜线（(7.5,裆+7)→(1.8,裆−1) 与水平夹角 ~55°、深 0.80cm 在全身截图 ≈2px），判远景分辨率误读、构造数字裁决不改码（「VLM 报几何缺陷先数字复算」再立功）；② 特写报叉顶「双坑状」= 两条独立沟内侧端 + mons 隆突的真实几何（两沟中心相距 3.6cm 不重叠，真实腹股沟褶本就两条各自汇入会阴外侧），判正确解剖不迭代。**harness 增补**：裆部特写视图（正面 canvas clip 下半中段 ~2.2× 有效放大）进验收流程——0.5~1cm 级浅刻痕（沟/软组织檐口）在全身视图不可辨，专项特写才是该尺度的证据视图；analyze_image 偶发 1210 图片解析错误（同批签名 URL front 成功、quarter 失败）——重新 Read 铸新签名 URL 重试即过，属上传/CDN 瞬态非图片损坏。
- **2026-09-12 六轮根治（「160/64A 叉上结节+折痕」+ 165 正面「马鞍袋」放大；用户 interrupt 拍板「根治（推荐）」：vendor 侧重排派生场、消掉对消权重并拉宽过渡带）**。三轮 A/B 定案：**A 叉上陡崖**——thigh± 上半窗 8 在裆+1 硬截断而 hips± 平顶恰满幅，两场拼合 −2.1cm/2cm → thigh± 改 `_derived_torso_field` 双相（叉上躯干相 torso_scale 0.28 延续消崖、弱到不与 hips± 雅可比列共线）；**B 等百分比**——径向保形场近似加等绝对围度（ΔP≈2πδ），细环被收得 % 更狠：裆+1..+3 过收 −11.4% vs 需求 −8.2%、ramp 下缘戛然 → notch 折痕；hips± 改 proportional（幅值 × 局部环周长/站环周长，站带因子恒 1 响应不变）+ 躯干相下缘 taper 斜坡 0.55（平顶满强度送到叉过收 2~6pp、髋峰相对突出成台阶）；**C W 形+马鞍袋**——vendor lm.hip 实测在裆+5.5（非臆断 +20，叉→站 span 仅 8cm）：hips± 上窗 8 死于裆+15、waist− 原生够不着 → 裤+8..+18 无人收缩带（交付 −3.4% vs 需求 −9.7%）成 W 形谷@+8/球@+15（斜45° VLM 球状隆起 6/10 几何本体）→ 上 ramp 20 + 腰串扰守卫 0.3→0.8（护的是已退役对角三点表预算；运行时全 4×4 差分雅可比 0.5cm/w 耦合、对角 ~4.5 无害）；等 % 腿相在腿站过收 ~2 倍（环周长比 0.55 vs 站点真实需求比 160 0.24/165 ≈0——hips− 腿相 −8.4%/w × 0.71 = −5.95% vs 165 腿站真实需求 +1.0%）逼出 thigh+ 0.89 对消，其长下尾（half_below 29.5 到膝标）在 hips− 窗（裆−9 死）之外把裆−6..−12 马鞍袋带吹胀 +1.7~4.6% → 腿相乘 `_LEG_RATIO_CAP`（0.40 实测仍 +3.5%；按预标定表建模 thigh+(cap)=(w·cap·8.4+demand)/9.8 反解带内 ≤1.5% 定 0.12）。**教训三则**：① 标定诊断基准系——calibrateWeights 在 mesh 原生高度测（无 applyVertical），_diag 打印混对齐后 pos × 原生站高 = 垃圾数据（曾误判「闭环回归」，_diag_iter 逐迭代复刻求解器证明 2-3 轮收敛、it2 = 生产权重精确复现）；② VLM 判读三律——报缺陷先拍 base 直显对照（?bodymesh=base）定基线：本轮 base 裆部直显 4/10（MakeHuman 转子穹顶原生台阶）而 morph 5-6，残余 1-2 分 = 数据层真实差（hip 90/thigh 54 梨形比 0.60 vs MakeHuman 平均 0.55），最终剪影由 girth 目标决定、任何场形/基网格改动不改变总轮廓（求解器收敛同目标，「内缩基网格」对剪影无效）；同图重读 ±1.5~2 分 = 仪器噪声，读数差小于噪声不迭代（平顶 core A/B 与 cos² 判不出、按数字 −12 带 +1.6 vs +1.3 退回 cos²）；网格叠加 60px 像素网格既过内容过滤又稳定读数——对比必须同仪器（网格 vs 网格）；③ 守卫语义审查——旧预算守护已退役机制时该退役（三点表 0.3 挡住 W 修复）。**终态（sha 4a9e3e34，资产链确定性复现）**：165 解 {waist−0.321, hips−0.700, thigh+0.258, knee+0.110}、160 {waist−0.506, hips−0.813, thigh−0.083, knee−0.092}——对消结构消失（160 腿相翻负 = 真实需求 −2.7% 方向）；叉带 165 +1.2~1.3% / 160 −0.4~−1.2% ≈ base 对照、中带平滑单谷无 W；金标 pytest 28 + vitest 66 全绿（守卫零同步）；VLM：165/160 斜 45° 自然~轻度 1-3、160 裆轻度 2-3 ≈ base、165 裆 5-6 vs base 4（残余交用户目检裁夺）。
- **2026-09-12 裁撤试穿（用户裁决：先解决人台自然 + 自由调节尺寸，穿裤子/物理碰撞链整体退役，只留人台显示）**。前端删：seams.ts / mesh.ts（布料网格+delaunator 依赖）/ structureLines.ts / heatmap.ts / pbd/（collide+constraints+solver）/ useFittingSolver.ts / mannequin.ts（三管截面适配层 sectionAt/radiusAt，消费方全灭）/ bodymesh/heightfield.ts（三管高度场碰撞桥）；新增 useMannequin.ts（无 rAF 解算循环，静态构建）；build.ts 出口瘦身为 MeshMannequin {topY, bottomY, sourceMesh}（对齐→围度闭环→morph 链不动）；priors.ts 删 SOLVER_PRIOR/MESH_PRIOR 与高度场五常数（thetaBins/rowStep/ringStep/pelvisBelowCrotch/legAboveCrotch）；package.json 删 delaunator；CSS 删热力图图例/松量/透明度/冻结四组死类。**引擎侧零改动**：exporters/fitting.py 与 /api/draft/fitting 端点照旧——前端仍取 payload（body 站点 = 人台纵向对齐锚 + 标定测点换算依赖），pieces 字段闲置；恢复试穿 = 前端 revert 即可。**金标收缩**：fitting3d.test.ts 只留 bodyProfile/Store；bodymesh.test.ts 删 heightfield/fillThetaPits describe、整链契约去掉管环/hf 断言（morph 后站点围度断言保留）；integration 烟囱改人台整链（有限/顶底序/站点围度达标），**站点围度只断言 waist/knee**——thigh 标定测点钳在 mesh 坐标裆叉下（build.ts 口径），payload 侧重建该测点需复刻分段对齐，且 payload thigh 站恰与裆线等高处切片含裆桥（实测 57.8 虚高）正是钳位理由，钳位行为由合成金标把守（对齐恒等可直测）。pytest 28 + vitest 50 + tsc 全绿。

- **2026-09-12 二次裁撤（用户指令「把调节尺寸的功能也去掉，只用 three 显示 obj」：体型围度调节链整体退役，人台 = three 静态直显 MakeHuman 原始 obj）**。前端删：`bodymesh/` 运行时整目录（load/morph/align/calibrate/build/slice + 合成金标 bodymesh.test.ts）、`bodyProfile.ts`/`bodyProfileStore.ts`/`BodyProfileDrawer.tsx`（体型库 UI + localStorage `ylpattern.body.v1`）、`useMannequin.ts`、`priors.ts`（全表——EASE/BODY_MIN_RATIO/BODYMESH_PRIOR 消费方全灭）、`fitting3d.test.ts`/`fitting3d.integration.test.ts`/`_fixture_fitting.json`；**fitting payload 前端消费归零**——types.ts FittingResult 契约族、api/apiHttp postFitting、protocol `'fitting'` cmd、useDraft generateFitting/fitting 快照三件套、「生成」按钮 + StaleFlag + stale 角标链全拆（App/Fitting3DView 不再接收 fitting props）。人台现行链：`Fitting3DView`（无 props）→ `rawMesh.loadRawObj('bodymesh/raw.obj')`（原 ?bodymesh=raw 诊断路径转正：只取 g body 皮肤面、dm→cm、脚底落地；不裁切/不去臂/不 morph/不标定/不对齐）→ BufferGeometry 直显，机位/视角预设按 bbox 身高定标。**引擎零改动红线保持**：exporters/fitting.py、POST /api/draft/fitting、engine_glue._fitting、tests/test_fitting_payload.py + test_vendor_bodymesh.py + test_engine_glue.py 全照旧（无前端调用方，闲置）；public/bodymesh/{base.bin,targets.json} 保留（vendor 产物 + Python 金标把守，运行时闲置），raw.obj 为 git 内拷贝继续消费。恢复体型调节/试穿 = 前端 revert。回归：tsc 0 错、vitest 34/34（原 50，fitting3d 16 测随链退役）、`npm run build` 通过；引擎侧未触碰。

- **2026-09-12 三次调整（用户指令「把他的 target 生成滑块的调节控件，让我先试一试看效果」：静态 raw.obj 直显 → MakeHuman target 滑杆试验场）**。背景问答：targets 是 MakeHuman 官方 CC0 下载物（makehumancommunity v1.3.0，PROVENANCE sha256），即其 GUI 滑杆机制本体（稀疏顶点位移场，作者化域 [0,1]，定性不定量）——用户要求直接拿它做滑块试手感。实现：`bodymesh/` 最小复活（load/morph/types/slice 四文件 git show HEAD 恢复，头注去碰撞/闭环措辞），`Fitting3DView` 重写为试验场——loadBodyMesh（base.bin+targets.json）基网格上屏，四部位双极滑杆（腰/臀/大腿/膝 −1..+1，正推 site+/负推 site−）→ morphPositions in-place 写 position attr + computeVertexNormals（亚毫秒），`stationGirth` 四站围度实时读数（站高取 w=0 meta 值，native 场微移站高属已知口径偏差）；**无闭环/对齐/钳位**——裸权重放开 ±1 供试探（超作者化域外推风险自担，读数可见）。rawMesh.ts/raw.obj 直显随让位退役（raw.obj 资产保留）。金标：bodymesh.test.ts 最小集（parseBin 布局契约 + morph 恒等/叠加）；vitest 36/36、tsc 0 错、build 过。引擎侧照旧零触碰。


- **2026-09-12 四次转向（用户报障「为什么变成半身了，我要的是全身的，然后就用它的 targets 调节模型」：base.bin 滑杆 → 全身原生 target 滑杆试验场）**。三轮滑杆用 base.bin 基网格——vendor 裁切链产物（腰上 15cm 截断 + 去臂 + 最大连通域），渲染出来是半身无臂人台，非用户所愿。**新链路（bodymesh/native.ts）**：直接解析 `public/bodymesh/raw.obj`（= vendor base.obj，sha256 一致）——全部 v 行平铺**保留原始顶点号**（.target 索引即 base.obj 顶点号，与全身网格天然对齐零重映射；helper-*/joint-* 辅助顶点留在数组不渲染），面只取 g body 扇形三角化（与 vendor parse_obj 同口径），dm→cm ×10，body 面顶点最小 y 落地脚底 y=0；8 个官方 measure 场从 `public/bodymesh/targets/measure/` 原样拷贝（文本 `idx dx dy dz` dm 单位）。**裸原生口径**：base.obj 本身已是 universal female young average（DEFAULT_FEMALE="" 恒等），无 macro 烘焙；原生场固有缺陷（thigh 场膝上死区等）是作者化行为，原样呈现——派生场修复只存在于退役的裁切链。**A-pose 全身读数适配**：BIG=25cm 大环过滤剔除指段/小臂环；站高粗探测 = 自下而上逐 cm 切片扫 2→1 大环跳变定裆（膝=0.57×裆、大腿=裆−6、臀=裆+10、腰=裆+23 比例派生，探测失败静默置空）；readGirth body 站取最大环（躯干恒大于挂臂环）、leg 站取最右大环（右腿；初版 cx>0 过滤会被质心微偏正的大围度躯干环误选，当轮即修）。morph 签名放宽为结构 `{positions, targets}`；slice.ts 收缩为 sliceLoops 底座（stationGirth 求和口径随半身假设一并退役）。**退役**：bodymesh/load.ts（base.bin 前端路径二度退役，base.bin/targets.json 仍由 Python 金标把守闲置）；测试重写（parseBin → parseTargetText）+ 新增 native.integration.test.ts 真数据冒烟（raw.obj + 官方 target 上验证站高四站有序/基线围度解剖区间/索引域不越顶点数；devDep 加 @types/node，import.meta.dirname）。验：tsc + vitest 40/40 + build 全绿。

- **2026-09-13 五次调整（用户口径「姿势手术撕裂臀线」教训落地：base.bin 派生链重写为纯切割链——只做下半身切割，零姿势修改）**。用户在 Q&A 中确认臀大肌解剖上罩着大腿后上段（「臀部控制着一部分大腿，光调整大腿会形体扭曲」），与 2026-09-12 四轮报障（adduct 整段平移撕裂臀线）互证；裁决：vendor 脚本撤掉全部姿势/形态手术，只保留切割语义。**新链路（scripts/vendor_makehuman.py，~1700 行瘦至 ~560 行）**：解析（g body、dm→cm、+Z 前自检、脚底=0）→ 女性 macro 恒等占位 → 粗裁 0.73H + 最大连通域去臂 → 地标检测（姿势无关口径：裆=2环→1环拓扑合并、臀/腰=围度切片极值、膝/踝=右腿围度极小）→ 精裁腰+15 → 封盖/紧凑化/水密欧拉自检 → **官方 8 场全部重映射**（vmap→keep_map→remap 三级索引链，无姿势旋转、增量仅 dm→cm；thigh/knee/hips 派生场替换逻辑废除）。**退役清单**：desplay_legs（去外张旋转+phi 增量旋转）、adduct_thighs（v3 gap 剖面）、四解剖雕塑（soften_trochanter/sculpt_leg_muscles/sculpt_pelvis_front/sculpt_inguinal_groove 及其存活守卫）、三派生场族（_derived_radial/torso_field、叉带互穿/剪切守卫）、预标定影响矩阵（targets.json schema 2，闭环消费方已灭）、--dump 调参表。**金标重写（tests/test_vendor_bodymesh.py）**：锚值换新（地标 crotch 77.6/hip 83.6/waist 104.6/knee 47.1；V 4276/F 8548 水密欧拉 2；w0 围度 waist 69.27/hips 98.68/thigh 54.74/knee 34.59）；官方场方向响应 8/8 全锁（w1：waist+ +10.60/hips+ +19.36/thigh+ +6.39/knee+ +6.71 及负场，重映射错位即炸）；**A-pose 外张守卫**（右腿环质心 cx@裆−10 12.47 → @裆−45 18.38，差值 ≥3cm——姿势手术回归混入即塌缩）。验：pytest 22 过 + 前端 40/40 未受扰（前端走 raw.obj 全身链，base.bin 仍闲置）。

- **2026-09-13 六次转向（用户报障「为什么现在显示的还是一整个人，不是只有下半身」：前端显示切回 base.bin 下半身，全身原生链二度退役）**。五次调整只重生成了 base.bin 资产，前端仍停在 raw.obj 全身原生链——用户刷新后看到整人报障；显示口径就此落定「只显示下半身」。**前端新链**：新增 `bodymesh/bin.ts`（`parseBaseBin` 二进制解析：布局契约 + 尾部残留/索引越界/空槽/头部非法四处防爆 + 与 targets.json 计数对账（V/F/T 三处同步失配即炸）；`loadBodyMesh` 模块级 promise 缓存，fetch targets.json → 以其 `baseSha256` 指纹拉 `base.bin?v=<sha8>` 击穿浏览器缓存（2026-09-12「改了没变化」事故机制延续）；站高与裁切面高直接取 targets.json 的 **vendor 地标检测值**——detectStations 启发式站高探测（裆=2→1 环跳变 + 比例派生）随之退役，读数口径从近似转精确）。`slice.ts` 收编 `readGirth`（自 native.ts 迁入）：BIG=25cm 环过滤降级为防御性口径（下半身切割网格无臂无指、腰/臀恒单环），leg 站取最右大环（cx 最大确定性挑出，不受躯干质心微偏影响）。`Fitting3DView` 资产层换 `BodyMeshAsset`（8 官方场索引已对齐切割网格、四站 vendor 地标、height=裁切面高），初始机位按裁切面高定标（正观下半身），材质去 DoubleSide（水密网格）。**退役/删除**：`native.ts` + `native.integration.test.ts`（raw.obj 全身原生链，二度退役）；`public/bodymesh/raw.obj`（vendor/makehuman/base.obj 仍是 git 内原件，恢复全身 = 拷回 + 重建 native 链）；`public/bodymesh/targets/measure/` 8 个官方场拷贝（官方场已烘进 base.bin，运行时只消费 base.bin + targets.json 两文件）。**测试重写（bodymesh.test.ts）**：手搓 tinyBin 布局契约（含尾部残留/索引越界防爆）+ morph 恒等/稀疏叠加/源数组不可变 + **真产物冒烟**（盘上读现货 base.bin+targets.json：计数对账、8 场名、四站齐全、waist girth>55、官方 waist+ 自家站方向响应 >+2cm——vendor 重跑漂移即炸）；@types/node + `import.meta.dirname` 沿用。验：tsc 干净 + vitest 38/38（4 文件）+ build 全绿；Python 金标 22 过未受扰。

- **2026-09-13 小腿/踝引入（用户指令「将这小腿和脚踝引入到项目」：官方 calf/ankle measure 场并入，8 场 → 12 场、四部位 → 六部位滑杆）**。背景：Q&A 确认打版滑杆此前只有腰/臀/大腿/膝（knee 场作者化尾巴顺带影响小腿上段），上游 makehumancommunity v1.3.0 另有官方 `measure-calf-circ-{incr,decr}` / `measure-ankle-circ-{incr,decr}`（CC0 September 2020 显式释出，文件头自证）——正中「无小腿/踝独立调节」缺口。**下载坑**：raw.githubusercontent.com 在本网络不可达（curl 全败），走 api.github.com contents API base64 解码、按原始字节 LF 落盘保 sha256（`.gitattributes -text` 口径）；上游路径多一层 `makehuman/data/targets/measure/`（PROVENANCE.md 已记），naive `data/targets/measure` 404。**vendor 链**：`STATIONS`/`MEASURE_TARGETS` 各加 calf/ankle 两行；新地标**小腿肚 = 右腿围度在 [踝+2, 膝−2] 的局部极大**（解剖小腿最丰满处、膝/踝两极小间的唯一峰，带端各留 2cm 防蹭极小平台；`_girth_local_min` 泛化为 `_girth_extremum(mode)`）；`station_y` 增 calf/ankle，w=1 响应自检自动覆盖 12 场。实测 calf 34.0 / ankle 12.5（膝 47.1）、w=1 响应 calf+ +10.01/calf− −9.99、ankle+ +5.97/ankle− −5.97（近反对称、无 thigh 式 canonical 错位）。资产重生：网格拓扑不变（V 4276/F 8548，只 T 8→12），baseSha256 换新自动击穿缓存。**前端**：types.ts TargetName +4、bin.ts stations 类型/过滤 +6 站、Fitting3DView SITES 加 小腿/踝 两滑杆（初始/归零 state 同步扩）、bodymesh.test.ts 冒烟 12 场名/六站/calf+ 方向响应。**金标锚值更新（test_vendor_bodymesh.py）**：ANCHOR_LANDMARKS +calf 34.0、ANCHOR_GIRTH_W0 +calf 35.37/ankle 20.36、ANCHOR_RESPONSE +四行、leg_st 集合扩 calf/ankle、下行链有序断言（膝>calf>踝）。PROVENANCE.md 补 4 行 sha256。验：pytest 22 过 + tsc 干净 + vitest 38/38 + build 全绿。
- **2026-09-13 七转：站直姿势采纳（用户问「官方『造姿势三件套』能不能把腿张开的姿势改为模特一样站直」→ 探针验证通过 →「先方案再执行」→ 方案纠正「理论上臀/大腿/小腿都要改变角度」→ 逐关节版批准落地；「零姿势修改」口径退役）**。三件套 = `data/rigs/default.mhskel` 骨架（关节名→顶点索引组）+ `default_weights.mhw` 蒙皮权重（骨名→[[顶点,权重]]，139 骨）+ 姿势自 authored（角度从绑定几何直推，无需官方姿势文件）；两文件 JSON 内嵌 CC0 license，只借数据零 AGPL 代码（许可红线不破）。**探针证据（整腿绕髋一转简化版）**：θ=7.84°，踝落髋正下方偏差 1e-15cm、脚底水平 [0.00,0.00]、镜像最近点 0.000cm、w=1 骨内刚性偏差 0.006cm、混合带围长损失 ≤1.7cm、外缘包络零台阶（退役手术当年 3cm 内 2.6cm 骤降）；臀带（无骨骼参与）被 upperleg01 权重携带 mean 1.58/max 2.87cm——「臀部控制着一部分大腿」的官方蒙皮实现，正是自研窗口化手术四轮撕裂臀线、官方连续影响场能成的机理差。**正式版逐关节**（用户纠正整腿一转为逐关节）：θ_t=6.52°（髋→膝竖直化，绕髋）、θ_k=+2.79°（绕移动后膝补角）、θ_c=9.31°（膝→踝，小腿净线性角）、脚绕移动后踝反补 −θ_c 回水平；角度公式 θ=atan2(dx,dy)+π，L/R 由 atan2 自动反号；髋/膝/踝三点铅垂自检（kneeDx/ankleDx ≤0.5cm、角度域 [4°,12°]、soleLevel ≤0.1）。**实施坑三则**：① 关节组顶点全在 helper 几何组（8/8）——质心必须用 parse_obj 全量 verts_dm 表，~40% 腿/脚权重项挂 helper 顶点（5316 项按 vmap 过滤丢弃计数）；② 小腿合成角初版误用髋→踝角 7.84°，残差 1.47°×38cm=0.97cm 被 kneeDx 自检当场抓获（exit 12）——自检设计有效性的即席实证；③ 平整度口径：绑定脚底本有 0~0.4cm 自然坡（姿势前后逐点分布一致），带内展宽口径误否，改左右脚最低点差 soleLevel。**产物变化**：V/F 4276/8548→4311/8618（站直抬腰 ~1cm 使精裁面多切一层三角形）；地标 crotch/hip/waist/knee 各 +1.0（78.6/84.6/105.6/48.1，裆下切向拖拽+脚底重归一+1cm 检测网格量化）；hips w0 98.68→97.63（大腿内倾解剖回收）；hips+ 响应 19.36→9.38 减半（时误归因「场窗钉绑定帧髋高、站点上移落窗缘」——**当日纠偏证伪，真因=随帧旋转线性角未归一，见下条**）；官方 12 场增量按逐顶点累积角随帧旋转（upperleg*=θ_t、lowerleg*=θ_c、脚骨 0，R(−θ_c)R(θ_c)=I；先例 desplay 链同款）；精裁余量 1.0→0.5（功能约束只有精裁<粗裁）。**金标翻新（test_vendor_bodymesh.py）**：test_leg_axis_natural_splay → test_leg_axis_standing_vertical（cx@c−10 10.05 / c−30 10.80 / c−45 11.66：|踝段−大腿根|≤2.0 防 A-pose 回归〔回归时差 ~5.9〕与过度内收双向、|膝−大腿根|≤1.5 防「整腿一转」膝弓残留、cx@c−10>5.0 防并腿）+ 新 test_sole_flat_and_mirror（soleLevel ≤0.1、膝/小腿肚/踝 |cx_L+cx_R|≤0.3）+ pose meta 断言（名 standing、θ 左右镜像、域 [4°,12°]）+ 锚值全量重读。**视觉验收**：产物 bin 渲染正/侧/髋部放大三视图 VLM 全过（腿竖直、脚平底朝前、站位在髋下、髋-腿过渡干净、无撕裂/翻转/自交）；VLM 报「右髋平点/左髋略凸」数字终审否决——髋带/臀上带镜像最近点最大偏差 0.000cm、左右外缘 max x 逐位相等（光影感知伪影，规矩：VLM 报几何缺陷先数字复算）。**口径退役**：早间「零姿势修改」退役，`--pose apose` 为 A-pose 复现口径；vendor 脚本 2026-09-13 晚定型「切割+站直姿势链」。验证：pytest 1051 全绿 + vitest 38/38。

- **2026-09-13 同日纠偏：hips± 单侧翻转（用户报障「臀部调整尺寸时从正面看右边往内凹，方向搞反了」——随帧旋转的线性角未归一，vendor 侧 bug 非显示问题）**。现象量化：hips+ w=1 把 +x 侧髋带外缘**内拉 −2.83cm**、−x 侧外推 +3.41cm；waist± 顶点无腿权重（a=0）、thigh± 顶点多全权重（a≡θ，整周等价）故幸免——只有 hips（髋带部分权重 0.2~0.8）炸成单侧翻转。根因：`pose_standing` 的 θ=atan2(dx,dy)+π 落在 [0,2π)，L 侧（+x）外张角原值 ~353.5°≡−6.5°——**整周旋转对位置无损**（周期性：站直网格、三点铅垂、镜像 0.000 等全部位置守卫照过），但步骤 7 把它当**线性角乘权重**：a=w·353.5°，w≈0.5 → ~177°，官方增量被翻转半个平面。排障路径（对账制）：官方原始 hips+ 场 671 镜像对偏差 0.0000（数据无辜）→ 进程内复刻链逐顶点对账 707 项与产物逐位一致（映射/旋转实现/写出无辜）→ 唯一变量 pose_angles，实测 max 353.84° 定罪。修复三件：`_norm` 归一到 (−π,π] 再入变换与线性角（位置逐位不变、增量方向复原）；vendor 新自检 exit 13（pose_angles |a|>25° 拒产——此 bug 曾穿透全部既有守卫出货，守卫缺口实证）；金标新增 `test_official_targets_mirror_symmetric`（12 场镜像对基线 0.0000cm + 配对退化防御 ≥1000 对/场 ≥150 对）。锚值回读：hips+ 19.35 / hips− −9.65（与 A-pose 链 19.36/−9.66 一致——「站点上移落窗缘致减半」的语义漂移**不存在**）；thigh+ 5.95 / thigh− −3.92 / ankle± ±5.93 顺带复原（+x=L 侧部分权重顶点同病）；waist±/knee±/calf± 与地标/w0 围度不变（位置不变）。产物 baseSha256 d8570f0d→620dc7d1（位置不动、增量换血）。验证：pytest 1052 全绿。教训入册：**「线性量的周期性等价」只在全量成立、乘权重（取分数）后破灭**——任何供插值/加权消费的角度必须先归一。
- **2026-09-13 预设体型：官方身高 macro 场接入（用户需求「能不能搞几个预设体型，比如160，165身高的女性形体」；AskUserQuestion 拍板「四档芯片 + 身高滑杆」，ExitPlanMode 一次驳回后澄清身高**连续自由可调**——芯片只是快捷设值不限制滑杆）**。**测量驱动、约定无关**设计基石：不假设 MakeHuman macro 权重混合约定，vendor 步骤 2.6 在**切割前**站直全身网格上对 max/minheight 场 w=±1 实测 max y 响应 → targets.json `meta.height = {baseCm 167.36, plusCmAtW1 +72.02, minusCmAtW1 −36.69}`（exit 14 守卫 ΔH 方向/量级），预设权重 = 目标Δ ÷ 实测ΔH（155→−0.337 / 160→−0.201 / 165→−0.064 / 170→+0.037，回环精确）。**意外发现：官方 macro 域极宽**——±1 实测跨 130.7~239.4cm（卡通身高域），预设全落小权重区；滑杆 UX 随之裁决：**cm 单位钳常人窗 150~185（step 0.1）连续可调**（可停 162.3 等任意值）而非裸权重 ±1，芯片值全在窗内不受钳制影响。**合成顶点场插值修复（出荷前预防性修复，前端从未见过 bug）**：切割链的切缝交点（cut_below）与封盖质心（cap_loops）是网格新造顶点、原始 .target 场里没有它们的增量——身高场作用于全部 originals 时顶环缝不动 → 顶缘厘米级拉伸带（围度场影响域多不含顶环侥幸掩盖，身高场全场覆盖必然暴露）；修 = 切缝顶点按其边两端点**原始 .target 增量线性插值（参数 t）、封盖质心取环成员均值**，×10 dm→cm + 随帧旋转同工序（两端均不在影响域则该场不动它、单端在场退化取在场端）——14 场全受益，身高± 覆盖全部 4311 顶点（4158 orig + 152 seam + 1 cap）。**站点缩放近似**：围度读数站 y × `stationFactor` = heightCm/baseCm（身高场近似等比升降全身的比例近似口径，极端档位有轻微偏差，与「场会微移站高」caveat 同级）。**链路**：T 12→14（槽位 12 measure 后追加 height±，meta file 存相对路径 `macrodetails/height/…`）、MEASURE_TARGETS 值改 targets/ 相对路径；新前端文件 `bodymesh/height.ts` 纯函数（heightCm/weightFor/stationFactor）+ Fitting3DView 预设体型卡（155/160/165/170/默认芯片，当前身高回算差 <0.05 高亮）+ 第 7 行身高滑杆；身高宏目标入库 `vendor/makehuman/targets/macrodetails/height/`（api.github.com contents API + base64，raw.githubusercontent 不可达沿用小腿/踝先例；maxheight 420920B/minheight 444394B，sha256 入 PROVENANCE）。产物 baseSha256 620dc7d1→0b5d2c37。**金标**：Python 新 test_meta_height（基高域 + 预设可达性换算回环）+ test_height_field_direction（切割网格 w=1 maxy −30.93/+58.88、脚底 ≥ −1 锚地）+ 全场覆盖断言（身高场 count == vertexCount，合成插值缺席即炸）；前端 bodymesh.test.ts 14 名冒烟 + heightInfo 契约 + 身高场方向/脚底锚地 + height.ts 纯函数 describe（互逆/钳制/两支独立非反对称）。验证：pytest 1054 全绿 + vitest 41 + tsc 干净。
- **2026-09-13 试穿链二期复活（M0~M3 数值侧一气收口；方案 `.claude/plans/3d试穿二期-复活.md`，用户拍板五条：缝合配对在前端/payload schema v1 零改动、解算器原样复活旧 PBD、碰撞换 three-mesh-bvh、独立原则〔人台参数不改衣服、合体度只经热力图/松量读数表达、绝无自动调版/自动改体型闭环〕、直腰头=真实第 5 布片与前后片物理缝合〔用户口径「腰头一定要和前后片连在一起」，弯腰头回退视觉环带+提示〕）**。**非 git revert 而是重写复活**：garment/ 纯函数层（mesh/band/align/placement/seams/priors/ease/heatmap）+ worker/{solver,collide}——solver 移植 f0ce40d 旧 PBD 全套（verlet+Gauss-Seidel 3×6、preRelax 拉链渐进、发散兜底），碰撞从旧三管解析换 BVH（无界距离查询——maxThreshold=4×skin 会深陷失明，>1.2cm 粒子查无命中永不推出）；缝合 11 条配对按弧长参数（前后片整版并排摆放、锚点无全局坐标重合），pin = 腰头上沿 belt line **只钉 Y**。**M0-M1 十二轮参数实验全档留 priors.ts 注释**，要点：band rest 全保纸样矩形（按吸附构型重定跨行 rest 与环向纸样 rest 自相矛盾 66.8 vs 70.1 → 预应力喇叭+腰缝拔河）；band 行高 = 腰站锚+真布高 bandWarp（warp 顶段 1.75× 外推把 4cm 腰头钉到 7cm 肋外扩区→屈曲翼）；全体粒子 Y-pin 与面板悬垂 1.1cm 拔河；crotchHold=60 硬释放（40/180/240-fade 全差）；preRelax 300（450 拧进缠结吸引域）；启动强阻尼 0.9 适得其反——**启动序列对收敛 basin 敏感是总教训，勿盲调**。M1 验收：320 帧 settle（超 5s 端 6.7% 系统性偏差留档，上限 360）；11 缝 avg 0.06/P95~0.5、穿透 ≤0.15。**M2 丹宁增硬标定（500 帧参数探针）反直觉发现：适度增硬反而助收敛**——0.3 全局有腰下大腿蠕动模式、地板 ~0.8 擦 settle 阈值踩线（M1 的 320 帧本就踩线过）；0.45/0.5 稳收敛 313/314、地板 0.56/0.53；0.6 = 增硬环顶腿面磨转极限环地板 3.4 永不收敛；**span=4 窄硬环在软布中=铰链（环缘蠕动）不收敛、span=6 稳定窗、span=8 顶格 357**——定 denimBendStiffness 0.5/denimRegionSpan 6，M1 金标重跑更稳（314 帧、地板 0.53）。**M2 松量切片难点**：缝合对是物理约束不是共享顶点，bodymesh/readGirth 共享端点链法在缝上断链——ease.ts garmentLoops 端点距离容差 greedy 链（tol 1.5 覆盖 <0.5cm 缝残差、腿间 ≥2cm 不误链）+ Garment.triAll 全片合并三角形；挑环口径同 readGirth（body=最大环/leg=最右大环）、crotch 分叉站跳过。**M3**：重新试穿+重置解算双按钮、自动重穿开关默认关（600ms debounce 热启动；关=bodyStale 提示+手动 link——独立原则的交互面）、人台三态（实体/半透明/隐藏）、弯腰头回退提示上屏、fly 连裁拦截金标补齐。**残留（需用户截图配合，浏览器不可自动驱动）**：摩擦 0.5 与丹宁数值 VLM 八视角复调（继承旧 VLM 口径全套）。验证：tsc 0 错 + vitest 65/65（garment 16 + solver 2 + ease 6 + bodymesh 等）+ pytest 1054 恒绿（引擎零改动）。
- **2026-09-14 育克参与真实缝合（用户业务口径修订「一条完整的牛仔裤 = 腰头/育克/前片/后片裁片；前腰头缝前片，后腰头先和育克缝合再和后片，做程序要站在实际的业务场景」——修订二期红线「payload schema v1 零改动」）**。引擎：fitting payload 增第 4 片 back_yoke（back_yoke 开启且机头已上版；frame=rot180 整版倒置绘制保向反变换；有 yoke 时后片 top 改判 seam、育克 top 顶替腰口链 top_chain；无 yoke 三片不变向后兼容，金标/engine_glue 同步）。前端 16 缝装配链（无 yoke 仍 11 条）：侧缝链式拆分×4 + inseam×2 + rise/cb 镜像×2（后片+育克）+ 育克下口↔后片上口×2 + 腰缝×4（后侧改缝育克上口，环序 [yoke_L, front_L, front_R, yoke_R]）+ 腰头后中。**侧缝拆分大坑**：上段起点必须 1−fb（后片段占前片侧缝弧长比），勿按 fy（yoke 边长比）——fy+fb≈1.0061>1 把交界 0.6cm 条带双重预订到交汇点上下两侧（目标相距 ~1.2cm），四交汇点永久拔河=环向蠕动泵（avgSpeed 地板 4~12 随 basin 漂移永不收敛）；修复后 0.61% 长差按 M1 口径全段分数配对均匀吸收。**收敛战斗（未收口）**：修复杀死蠕动泵（漂移比=净位移/路径长 0.967→0.074）后暴露下一层——**整裤低频往复摆振**（avgV 平台 ~10.5：前片脚口内角领头、L/R 半边反相交替〔f300 时 L 半边 2~3 R 半边 8~12，f900 反转〕、99.8% 悬空粒子全体 8~14 均匀抖、缝距 0.034/穿透 0 全达标；当前假设 = pinYOnly 下整裤环向无锚 + 悬空无摩擦阻尼，damping 0.985 压不住约束投影〔GS 走带〕注入）。**已反证对策（勿重试）**：缝投影偶迭代反向扫描（杀蠕动但 M1 也坏成 1.44~1.58）、静摩擦阈值 0.05（杀蠕动但地板不动——游离粒子摩擦够不着）、摩擦 1.0（接触粒子切向冻结→塌陷假 settle 157/pen 19.5）、锚定单粒子（冻死穿衣旋转→塌陷假 settle 175）、育克整片 bend 增硬 0.5/0.9（无效）。**方法论两则**：①单跑地板比较无意义——Float32Array.fill(0.3) 与字面量 0.3 的浮点差经数百帧混沌放大分岔入不同 basin（同配置地板 4.3 vs 11.7），比较必双跑验确定性；②初摆位塌陷靠持续环向滑动穿衣（f0 凸包场计穿透 ~5400/6035 在会阴区两腿空隙、BVH 窄相无推力，f600 穿透 0.9/12）——任何冻旋转对策=冻在塌陷态假收敛；M1 的滑动是穿戴瞬态、泵的滑动是永久（判据=穿透清零后速度是否仍在）。**状态**：M1 直腰头用例照旧绿；yoke 16 缝用例红（480 帧上限不收敛，settledAt=−1）——damping 扫描实验已备（out/tmp_yoke_diag3_entry.ts：yoke 0.96/0.93/0.90 + M1 回归保护），接续口径/命令/失败表全录 `.claude/plans/3d试穿二期-yoke收敛交接.md`。
- **2026-09-14 同日收口：yoke 整裤摆振极限环根因定案 + collisionSkin 0.3→0.98 持续接触锚（solver/collide 零改动，纯 priors；vitest solver 3/3 + 全量 68/68 + pytest 1059 + tsc 全绿）**。**机制定案**：整裤 XZ 刚体模态（环向旋转/侧摆）**零刚度**——band pinYOnly 只锚 Y、重力对 XZ 无恢复力、dist/bend/seam 全是布片间约束原理上锚不住刚体模态，唯一世界锚=碰撞正压/摩擦；GS 固定顺序投影绕封闭缝环走带持续注入，与零刚度模态合成**持续极限环**。M1 单环拓扑弱泵靠 basin 运气收敛、yoke 双环带（腰缝环+育克下口环）强泵——即上一条「质量/约束拓扑差异」的力学展开。**判别实验（纯 priors 突变，shipped 零改动）**：①damping 0.96/0.93 不收敛、0.90 假收敛 558 帧冻在带穿透态（pen 2.184/206——低速蠕沉期 avgSpeed<0.8 触发 30 帧冻结、M1 同坏 585 帧/pen 3.46/188），damping 全档否决；②iterations 8/12 地板单调降 10.6→5.8→2.3（坐实投影注入是泵源）但 M1 反悬 0.96 贴阈值不结算（基线 it6=313），单 knob 死路（外推 iters≈40 才够、运行时 6.7×）；③friction=0 地板减半不死（10.6→5.5，摩擦踢喂约半泵能非全源）；④gravity=0「收敛」是退化假象——缝张力（rest=0 收口）把布拉穿身体（pen 14.6~17.0/1501~1748），重力+碰撞是抗收缩必要项。**突破=skin 抬厚**：0.6 即真收敛（613 帧、终速 0.285 远低于阈值）但 pen 0.722/10 超门——worst 恰=贴体静止点 hull 悬高 1.32−skin，**场保守性惩罚合法贴体**（布真穿在身上，penetrationStats 场口径比实际网格宽）；结构性矛盾：锚定需要贴体接触、场 pen 门惩罚贴体（overhang−skin>0.15 即爆）——解法=skin 抬到盖过最大静止悬高。**skin 窗口地形（basin 非单调）**：0.5 平台 2.8 / 0.6 收敛 613 / 0.8 平台 7.4 / **0.98 收敛 251 / 1.00 收敛 269 / 1.02 收敛 312** / 0.95 双破（yoke 5.3 平台、M1 悬 0.958）/ 1.05 双破（2.1/3.5 平台）/ 1.10 yoke 孤点 224（M1 未验）/ 1.2 悬 0.85——**双 fixture 联合窗 [0.98,1.02]**（M1 273/328/313），denimBendStiffness [0.45,0.5] 先例同量级；iters8 组合互斥（救 M1 328→281 但 yoke@1.0 坏成 3.0 平台）放弃。**定案 collisionSkin 0.98**（双余量最优：yoke 251 余 229、M1 273 余 87；1.0 的 M1 余量仅 32），priors 全标定注释（窗口地图/死路表/适用域警示）、solver.test 头注与行内实测值同步（320→273、yoke 251）。**方法论三则**：①「接触锚 vs 场 pen 门」矛盾识别——skin 必须盖过最大静止悬高，锚定与 pen 门才同时满足；②knife-edge 假象——±0.05 首探双破像刀尖标定，加密采样（0.98/1.02）见连续窗，basin 地形采样密度不足勿早判死；③**窗口只对本 fixture 对成立**——换体型/裁片 basin 重排需重验（生产无 settle 超时兜底，残留）。**残留（VLM 复调增项）**：skin 0.98 布离体 ~1cm 浮空感 + 腰头 snap 至 mesh+0.98，并入原「摩擦 0.5 与丹宁数值 VLM 八视角复调」。
- **2026-09-14 裁决转向：试穿穿台退役 → 悬挂展示（裤子撑开挂人台旁侧，晾衣架观感）**：用户四口径——纯静态筒形（不跑完整 PBD 穿台）/悬挂为唯一展示（穿台代码留档不触发）/不画晾衣架几何/**热力图退役**（裤子单独渲染不贴身，应变热力图失去意义）。背景：seams 侧缝链式拆分修复（消交界双重预订蠕动泵）后 yoke 整裤复发**整裤全局低频摆振**（avgV 平台 10.5、99.8% 粒子游离、L/R 半边反相绕前后轴扭转、领头者恒在前片脚口内角 v 27~50；机制假设 = band pinYOnly 环向无锚 + 游离无接触阻尼 + GS 走带注入，M1 同参数 313 帧收敛——差异在 yoke 环带质量/约束拓扑），收敛战线未竟，用户裁决转向绕开。**实现口径（前端零引擎改动）**：① worker 挂起分支（protocol `hang` 标志，默认 false 向后兼容）：不建 BVH、不传 snap、`NULL_COLLIDER`（worker/nocollider.ts；collide.ts 物理零改动）喂 createSim，preRelax 300（`HANG_PRIOR.relaxIters` 运行时写 SOLVER_PRIOR）跑完**单帧以 settled 回传**、不 startLoop——无动力学即无收敛判据、无摆振问题；无碰撞下缝闭合反而更快（300 即 avg 0.10/p95 0.37，优于穿台门槛 0.3/0.8；600/900/1500 收益递减且持续收缩裤筒包络，别加深）。② **关键认知：初始摆位是裙形**——placement 把前后片围成包住双腿的单筒，两腿形状靠 preRelax 拉链缝闭合才成形，纯静态 ≠ 直渲初始摆位；视图 Group visible=false 藏裙形闪现到松弛单帧揭幕（弯腰头回退环同）。③ 显示层偏移：worker 几何空间保持人台轴系（pins/crotch 目标自洽），裤子 Group `position.x = 2·maxRadius+garmentGap+clearance(6)`（`BodyField.maxRadius` 新 getter）平移到人台 +X 侧；相机 target/机位按 offset/2 差量平移居中双主体（幂等）+ flyTo 四预设同偏移。④ 热力图退役：heat state/Switch/onFrame 重算/setStrain 接线全删（heatmap.ts 与 render.setStrain 留档不触发）；松量读数保留（settled 单帧触发照旧）。⑤ UI 文案试穿改悬挂（重新悬挂/自动重挂/立即按新人台重挂等；代码标识符不动）。⑥ solver.test yoke 用例 **skip 留档**（M1 用例保留守物理健康，穿台复活再解封）；新悬挂金标 worker/hang.test.ts（双 fixture：缝 avg<0.2/p95<0.6、无 NaN、筒形包络守卫 x>24/z>20、钉 Y 漂移<0.5、单帧语义）。**已知限制**：悬挂无重力无自碰撞，裆部可能有小穿插褶皱（接受）；warm 重挂 runSig 匹配时从上次悬挂态出发（形态连续）。验证：garment vitest 4 文件 28 过 1 跳、全量 69 过 1 跳、tsc、vite build 全绿；引擎零改动（无需 build:engine）。穿台摆振诊断链/已试对策死路表见当日上一条与 `.claude/plans/3d试穿二期-yoke收敛交接.md`。
- **2026-09-14 悬挂纠偏（用户截图实证裤腿皱成一团后当日收口）：无碰撞单帧证伪 → 解算链回归穿台同源 + 帧数封顶兜底；yoke 收敛用例解封**。**证伪链**：①上一条的「无碰撞 preRelax 单帧」——缝虽闭合（avg 0.10）但**裤腿对折塌陷**：腿间空隙无碰撞体，内缝缝合对（前中 θ≈0 ↔ 后中 θ≈π）直接把布对折拉过腿间空腔，包腿圆度 wrapHem 仅 0.50（wrapFraction = 腿段粒子绕腿轴 12 扇区覆盖率，成筒 ~1.0/塌片 ~0.5）；**加碰撞的 preRelax-only 同样不行**（wrapHem 0.50~0.67、穿透 4600 粒卡腿间）——约束-only 松弛过不了局部极小值，包腿本就是动力学几百帧环向滑动完成的。②全钉腰头（pinYOnly=false 杀摆振根）——**灾难**：腰环钉死反而阻止滑移穿衣，穿透 18cm 卡死（穿衣时代「锚定冻旋转=假收敛」教训在悬挂同样成立）。**定稿口径**：悬挂 = 穿台解算链原样（collider+snap+60Hz stepSim，worker 空间裤子正常穿"隐形身体"成形圆筒）+ **显示层 Group 平移人台旁**（显示归显示、成形归成形）+ 帧数封顶 `HANG_PRIOR.maxFrames`=600 兜底（超限强制出 settled；形状 f150 已达标，防 basin 彩票卡显示）；成形过程可见（首帧起动画 ~4~5s，终态圆筒）。**yoke 用例解封**：上一条按交接文档 skip 的「不收敛」结论过时——交接文档系攻坚中途所写，决策日志当日「同日收口 251 帧」才是终态；解封实测 M1 274/yoke 252 帧收敛 3/3 绿（**教训：交接文档与决策日志冲突时以决策日志终态为准**）。**金标升级**：hang.test.ts 增**包腿圆度守卫**（wrapKnee≥0.65/wrapHem≥0.55，正是本次翻车该有而没有的断言）+ 缝/穿透/600 帧内收敛；HANG_PRIOR 收敛为 {clearance 6, maxFrames 600}，nocollider.ts 删除。验证：garment vitest 29/29、全量 70/70、tsc、vite build 全绿；引擎零改动。
- **2026-09-15 解耦定型：裤子解算弃人台碰撞，碰撞体换纸样围度撑型芯；顺藤修掉两个埋伏 bug（后片摆位反装 + collide 体内失明）**。用户方向拍板：「裤子和人台分开渲染，管什么能不能穿上——先把两个独立功能绘制正常」「内部是空的也要呈圆筒、不能叠」。**诊断链（headless 复刻钉死后动手）**：①逐站围度对照（人台精确切片：腰 69.0/臀 97.6/大腿 54.0/膝 34.5/踝 20.3 vs 纸样）证「套不上/腿松塌」皆是碰撞体选人台的结构性后果；②morph 六变体 + warm 链全扫：非 pristine fixture 全部 600 帧封顶不收敛且 entry.ts 封顶伪报 settled 静默上屏。**core.ts 定型（单一水密花生网格）**：一行一闭环、行间条带、两端封盖（盖法线必须朝外——反了把盖下粒子反推进管内，实测穿透 16cm）；腰臀圆环 + 裆下花生双瓣（ρ=2rc·sqrt(sin²θ+ε²) 平滑 + 整环缩放锁周长 = 2×腿站 girth−2×2π·skin）+ 裆带 ±3cm 混合；芯半径 = g/2π−skin → 布面恰落纸样围度。**四条死路（实测）**：腿间留槽→不可拉伸布内缝走捷径（省 10cm+）整幅堆进腿间、外侧裸管；双管相切→刀口双侧壳交替投影泵；双管交叠→符号判陷阱（「L 管外」即「R 管内」）布被钉死管内内象限无布；细龙骨→preRelax 拉链穿膛。**两真 bug**：①placement.placePoint 后片/育克 t 正向映射把侧缝端放后中、CB 端放侧面（后片 CB 在高 x、整版链序 waist 从 CB 角起算），cb 镜像对/侧缝对初始差 30~35cm 靠绕体拧转闭合 = 一直以来的扭转错位根因（前片映射本来就对；wrap/seam 金标测不出扭转——dump 初始缝误差 top 才抓到）；②collide.ts `distance≥skin→continue` 把深陷体内粒子静默跳过（无界查询只修了命中没修这个过滤，inside 分支不可达）+ 体内穿面推出需同步 prev（厘米级 teleport 否则速度踢灌全场地板 2.3 不收敛）。**标定迁移**：friction 0.5→0.8（裤筒绕瓣无重力回复，0.5 周向蠕动地板 ~9）、SEG 40→64/ε0.2→0.3（组合实测 173 帧；blend 方向曾写反——t 随高度增把花生放臀上、圆放膝下，R 场恒 8.26 暴露）、preRelaxCollideEvery 10→2（芯体细部防穿膛返工）、穿透验收换网格口径 collider.penetrationStats（hull 场口径对花生腰谷误报——谷内布在 hull 内不在网格内）；封顶 capped 上报（UI「600 帧未静止，已取终态」）。**协议瘦身**：worker init 不带人台几何（protocol/entry/useGarment），Fitting3DView autoDress/bodyStale 重挂 UI 退役；ease 读数整裤侧纸样系 st.y 切、人台侧 warp 切（各读各的坐标系）。**验证**：core/hang/solver/garment 金标重写（新链 + 网格穿透 0 断言），全量 74/74 绿、tsc 绿；直筒单（curved 回退链）端到端：缝 0.037/穿透 0/600 帧地板 0.83 波动贴线由封顶兜底（视觉静止）；正交视图渲染视检：对称、双腿成筒、腰头贴合、无穿模。**残留**：curved 回退地板贴线（抬 settleSpeed 到 0.9 已试仍波动不齐 30 连帧，回退 0.8 靠封顶）；观感 VLM 复调挂起。
- **2026-09-15（下午）用户截图三问题修复：芯体裆站掐腰（大腿比臀大的结构放大器）+ 弯腰头环带悬空 + 口袋挖削区（袋贴第 5 片 + 侧缝配对错位）**。用户反馈：①腰头和裤子没连一起；②大腿比臀大、垂坠不自然；③前口袋应缝到前片；④裤脚白色交叉线疑缝边外露。**headless 复刻钉死后动手**（临时 diag 用例跑用户真实 payload——弯腰头+yoke+口袋全开）：①弯腰头回退环带是**平环**（field+1.5、y∈[102,106]），而真实腰口弧是曲线（pin 粒子 y∈[97.1,102.0]，前后中下垂 ~5cm）→ 前后中 5cm 缝隙 + 半径错位；②芯体躯干剖面在裆站**直取单腿半径**：周长 95.4(臀)→69.2(裆+3) 掐腰再弹回 111.4(花生)，臀部塌成蜂腰（v2.1 修：裆站躯干圆 = coreRadius(max(臀, 2×腿)) 毗围代理——布在叉口连续，修后 95.4→106.5→112→花生 单调过渡）；③前片挖削后侧缝配对仍是前片全长对后片全长，P2 被硬拉到后侧链顶 = 口袋区 ~4cm 剪切错位 + 腰口侧段无布（洞）。**修复链**：（a）core.ts v2.1 裆站半径 + 金标「臀→裆带芯周长 ≥ 臀周−4 无蜂腰」；（b）`buildWaistRing` 重做为**沿腰口 pin 弧逐点成环**（各片 top_chain 摆位点 θ 排序成底圈+腰头宽顶圈，真实腰口弧高低/半径逐点跟随）；（c）**袋贴第 5 片**：引擎 fitting.py 增 front_facing（waist=top_chain/side=seam/inner=free；顺藤修真 bug——front_pocket_flow `_finish_piece` 漏登记 origin/frame，payload 原样导出局部系坐标被前端当全局系摆到脚底），前端 seams.ts 挖削款侧缝改道（前片侧缝全长只配后片**下段** 1:1 脚口端锚、后侧链余量=育克侧边+后片上端配袋贴侧边腰口端锚）+ 袋贴走 front 扇区摆位（t 用前片 x 全程归一）+ **整片钉摆位**（衬布式贴合：芯体填满筒内、垂坠动力学只会与筒面抢壳 z-fight，「缝在前片正确位置」展示语义优先）+ 袋口 mouth 边 free（口袋开口）；（d）直腰头真实布片收窄到**无挖削款**（挖削腰口弧缺段+袋布桥无片可缝，checkBandLength 必炸——旧口径直腰头+挖削=sim 报错，现一并回退视觉环带）；（e）显示层 `HANG_PRIOR.hemLift=2` 整裤抬离地面（纸样 hem 站 y=0 恰落地、裤口边与地面格网共面打架 = 「白色交叉线」观感；**缝边答复**：3D payload 消费的是净样 net_edges、本就不含缝边，白线是地面格网透过半透明布面）。**验证**：Python 1062 过（payload 增袋贴结构/挖削 mouth 两用例 + glue 全等增 facing 用例）；前端 76 过（hang 增第三 fixture「弯腰+挖削+袋贴」461 帧收敛/缝 0.023、core 增掐腰回归、buildWaistRing 改 pin 弧成环断言）；tsc、vite build、build:engine 全绿；headless 复核剪影：臀带 30.1→35.2（旧 28.9→32.5 且 76 高度带 22.0 峰谷已消）。**残留**：大腿区（2×大腿围=127 布 > 臀 100）仍比臀宽 ~40%——悬挂物理本性（两腿管并排宽度 = 2×腿围/π vs 臀单筒 直径=臀/π），属版型布量非 bug；弯腰头仍是视觉环带非真实布片（真拼合弧长 ≠ 平样腰弧合计、省道闭口差 ~4cm，PBD 无省道闭合建模，弹簧会硬拉出皱——待省道闭合机制）；袋布（front_pouch）未入 3D（袋口 opening 现为敞口）。

- **2026-09-15（晚）整裤渲染推倒重来（用户裁决「现在整体渲染裤子的效果不好，我决定一步一步来，先把之前整个裤子的渲染删掉，改为在人台旁只渲染一个前片」；AskUserQuestion 三项拍板：前片范围 = 左右一对 L+R / 整裤缝合代码 = 彻底删除文件〔**知情决定**；实施时发现 garment/ 已随 d784de1「裤子人台分开绘制」进过 git——删除后仍可 `git show d784de1:...` 找回，决策时双方按「未提交、不可恢复」认知拍板，特此更正记录〕/ 解算 = 先不解算静态上屏）**。**删除清单**（前端 webapp/frontend/src/fitting3d/garment/）：seams.ts（整裤缝合装配 11/16 缝）、band.ts（直腰头真实布片）、ease.ts+ease.test.ts（松量读数）、heatmap.ts（应变热力图）、align.ts（y-warp）、useGarment.ts（sim 生命周期 hook）、pbd/constraints.ts、worker/ 整目录（entry/protocol/solver/collide + solver.test/hang.test）、fixture_fitting_yoke.json、fixture_fitting_curved_pocket.json；依赖 three-mesh-bvh 卸载（delaunator 留）；garment.test.ts 裁至 mesh/placement 用例、core.test.ts 裁至 M1 单夹具。**保留地基**：mesh.ts（buildClothMesh 三角化+EdgeRun，约束字段为重建预留）、core.ts（撑型芯——角色从碰撞体转为静态摆位形态来源，CORE_SKIN=0.98 常数本地化）、placement.ts（BodyField/placePoint，PieceKey 收缩 'front'、去 warp 参数）、render.ts（buildGarmentView 瘦身去热力图通道）、priors.ts（去 SOLVER_PRIOR，garmentGap 移入 HANG_PRIOR、去 maxFrames）。**新增**：assemble.ts buildFrontPair（front × {L,R} 前 90° 扇区静态摆位，x→θ 均匀映射沿用旧初摆位近似）+ assemble.test.ts（镜像对称 z 差 <0.1cm——撑型芯场 θ-bin 量化比人台场旧标 0.05 略大、穿透按构造 0）；Fitting3DView 静态直出（无成形动画/缝合误差/封顶提示/松量读数卡，「悬挂」卡改「前片」卡 = 重新生成 + stale 提示）。**引擎零改动**：payload schema v1 照旧 5 片、前端只消费 front_piece（pytest 1062 恒绿；前端 tsc + vitest 57/57）。**随删除冻结的知识（重建解算必读，上两条 2026-09-14~15 条目为权威）**：yoke 16 缝整裤不收敛根因（band pinYOnly 环向零刚度 + GS 走带注入 → 全局低频摆振泵）与 collisionSkin 0.98 持续接触锚标定；丹宁增硬 0.45~0.5 助收敛（0.3 蠕动地板/0.6 磨转极限环）、preRelax 300（450 缠结吸引域）、crotchHold 60 硬释放、band rest 全保纸样矩形、friction 0.8/SEG64/ε0.3 组合；已证伪：无碰撞单帧（塌片 wrapHem 0.50）、全钉腰头 pinYOnly=false（卡腿间 18cm）、damping/iterations/摩擦单 knob 全反证、basin 彩票双跑验证法；`.claude/plans/3d试穿二期-yoke收敛交接.md` 的 yoke 收敛攻坚任务随本次裁决取消（历史参考）。

- **2026-09-15（晚二）重建二期：引力下垂（用户口径「裤子的下垂要符合地球引力」——看过一期静态前片后拍板，修订一期「先不解算」决定）**。**背景**：一期静态直出的裆部明显褶经数值定位 = 芯花生腰谷前中径向断崖（y80→y74 掉 5.5cm、谷-侧差 6~9cm）+ 均匀角度映射把裆下布平摊贴谷壁（布去向错位，非布量过剩——y68 行内余量仅 0.3cm），静态摆位无法消解，需真实引力。**设计**：**不是恢复整裤旧链**——主线程轻量 verlet（新 `garment/drape.ts`）：pin = 腰口 top_chain 全向硬钉初始位、约束 = mesh.ts 预留 dist/bend 数组（重建预留字段首次消费）、碰撞 = 撑型芯径向场推出（r < 场+CORE_SKIN 即推出）+ 接触摩擦 0.8（防周向滑转，旧教训）、重力 −980；无缝合/无 worker/无 BVH（three-mesh-bvh 不回装）；Fitting3DView rAF 实时步进（看着布垂下来），settled/封顶停走；常数 `DRAPE_PRIOR` 继承已删 SOLVER_PRIOR 标定值。**实测**：163 帧真收敛（非封顶）、质心沉降 4.66cm、穿透按构造 0、pin 恒守——旧整裤标定值在单片无缝地形直接成立，零调参。**新已知行为**：前片对未缝合，自由悬挂下前中（fly 区）敞开 ~5cm 缝（腰口钉弧采样不含前中角点、内边 rise 链荡至 θ≈−16°~−11°）——物理真实，是否前中闭合（L/R 内边配对 = 前中缝合）留用户拍板。验证：前端 tsc + vitest 58/58（新增 drape 金标）；引擎零改动。

- **2026-09-15（晚三）前中缝合（用户拍板「前片先将前中缝合在一起」——对前日「前中敞开留拍板」点的裁决）**。实现：drape 增**前中缝合对** = L/R rise 链（前裆弯，腰口端→裆点）**同号镜像配对** rest=0——两片共享同一 ClothMesh，顶点号天然一一对齐，无需旧 pairSeg 弧长配对；投影与 dist 同型（双向各移一半、刚度 seamStiffness 1.0 继承旧 SOLVER_PRIOR）。缝合范围只到裆点（真裤前中缝如此；inseam 是前后片缝留给后续加后片）；fly 连裁款 rise 链缺失则跳过缝合（敞口属连裁门襟固有形态，不拦截）。实测：158 帧真收敛、前中缝 21 对 avg=0.000/p95=0.000（同号配对闭合到零）、前中 |x|<1 粒子 0→257（原敞 ~5cm 缝全闭）、穿透仍 0。金标 drape.test.ts 增缝 avg<0.3/P95<0.8（旧整裤缝口径）。前端 tsc + vitest 58/58 + vite build 绿；引擎零改动。

- **2026-09-15（晚四）顶部折角数值定位与修复（用户截图报「前片顶部有一个折角」；数字复算出双根因）**：①**侧角下垂**——腰口边重采样不含共享端点（每边含起点采样、不含终点；共享角点 = 下一条边首采样），终点角（侧缝角）不在 pin 集内，settle 后从 98.0 急坠 95.86（−2.1cm，贴钉弧末端直坠 = 截图折角之一）；修：终点角补全向钉。②**顶中横扯缺口**——起点角（前中角）在 run 内被全向钉死在均匀映射摆位 θ≈−20°（x=∓3.89；t 归一被 crotch 鼓点 xMax=27.8 挤占角域预算，前中上段固有偏心），前中缝合把下方缝闭拢到 x=0，两者拔河把顶前中扯出 ~4cm 横向缺口（截图顶中折角）；修：前中角（环邻前点属 rise 链判定）改**只钉 Y**——高度恒守 96.86、水平交缝合协商，实测角 x −3.89→−1.67 与缝线 x=0 平缓过渡。金标补 pinY 恒守断言；缝误差仍全 0、tsc+vitest 58/58 绿。教训：均匀 x→θ 映射的角域预算挤占是前中上段固有偏心，根治要靠弧长贴合摆位（后续候选）。

- **2026-09-15（晚五）顶部折角二轮：腰口钉改「整圈 Y-only + 双角全向锚」（用户复验一轮修复后「这个折角还在啊」，并问前片来源——答：完全来自引擎 front_piece 净样 1:1）**。无头截图（Edge headless+virtual-time-budget）分析一轮后仍见右端尖角+左浅 V；但复验发现**无头模式下 drape 动画不跑**（500ms/6s/45s 虚拟时间判别：6s=45s=旧代码版字节全同=静态初始摆位帧，与 pin 代码无关）——无头截图只能验证静态渲染，动画效果验证只能靠真浏览器。二轮根因：一轮全向钉把腰口锁成**刚性折线**——图案角+侧缘陡落呈尖状凸起、前中角锁在偏心位。修法：腰口整圈只钉 Y（高度恒守、水平可滑，布自重圆化顶缘、前中缝合把顶缘滑到正中）+ L/R 侧角双全向锚；**单锚实测 600 帧不收敛**（单点锚不住环向滑移模态、无锚半边持续蠕动——capped 兜底），双锚后 162 帧真收敛。实测：前中角 x −3.89→−0.36（距正中面 3.6mm）、前中缝含顶段全闭 avg=0.000、顶缘最大水平滑动 5.66cm（顶缘真正软化）、高度恒守、穿透 0；58/58 绿。

- **2026-09-15（晚六）重建三期回落：平铺验证（用户裁决「现在感觉你裁片生成的都不对……不要悬挂渲染，先将前片渲染铺在平地」——二期悬挂+引力下垂整链暂停接线，退回裁片形状验证步）**。新增 `assemble.buildFlatPair`（现行接线）：纸样 2D 逐点等距变换平铺 y=0 地面——L 片原样归一 (u,0,v)、R 片前中镜像（u→W−u）平移到 L 右侧留 FLAT_PRIOR.gap(4)（真实排料观感：同一纸样画两次一左一右、前中边相对），无弯折无解算、网格即净样原形，与 2D 裁片 SVG 一比一；**验证分流（本步的目的）**：平铺即错 = payload 边链/网格层问题（引擎 build_fitting_payload 或 mesh.ts 上游），平铺对悬挂错 = 摆位/解算层问题。Fitting3DView 前片 effect 换平铺链：不建撑型芯/支撑场、不跑 drape、无 rAF；旁挂 Group x = 人台基础网格半宽 + FLAT_PRIOR.clearance(6)（morph 增量由净空兜底）、y = FLAT_PRIOR.lift(2)；**hangXRef 语义改组中心**（组原点在 L 片左缘 ≠ 悬挂时代的筒轴心=组原点），取景按「人台 0 ↔ 组中心」中点差量平移幂等、flyTo 同口径。悬挂链（core.buildCore / placement.buildBodyField / assemble.buildFrontPair / drape.ts）**代码保留不删**——它们尚未进过 git、删即丢失（与整裤链 d784de1 可找回不同），标注暂停接线、金标照跑（vitest 全量 62/62），形状验证通过后回归。金标 assemble.test.ts 增平铺 4 条（全片贴地 y≡0/逐点等距变换 L 原样 R 镜像/刚体镜像对称/包围域 [0,2W+gap]×[0,L]）；**坑：`Garment.pos` 是 Float32 渲染 buffer 口径，对照 Float64 `xy` 有 ~5e-7cm 量化伪差，toBeCloseTo 精度放小数 5 位（首跑 9 位 3 处全炸）**。vitest 62/62 + tsc 绿；引擎零改动。

- **2026-09-15（晚七）全裁片平铺 + 逐片分色（用户口径「把所有裁片都平铺出来，颜色区分下不要一摸一样」——三期平铺从仅前片扩为全部 payload 片）**。`buildFlatPair` 升级为 `assemble.buildFlatLayout(payload)`（单前片版逻辑被完全包含，删除不保留）：消费 payload 全部片（front/back/waistband + yoke?/facing?），**行式布局**——逐片一行、行序 = payload 片序、行距 FLAT_PRIOR.rowGap(6)；成对裁片（front/back/yoke/facing 及后续加片默认成对）L 原样 + R 前中镜像并排留 gap(4)，腰头单片（SINGLE_PIECES——整根幅样无左右之分，side 固定 'L'）；全组 z 居中（组中心 z=0，旁挂取景对称）；bbox 由网格顶点实算、不信任 payload 标量。分色在 render 层：`PIECE_COLORS`（front 蓝/back 绿/waistband 橙/back_yoke 紫/front_facing 青、未登记片灰兜底）每片一 Mesh 一材质，`pieceColor(key)` 导出供 UI 同源取色；Fitting3DView 侧栏「前片」卡改「裁片」卡 + 图例（色点 + 中文名 CN_PIECE_NAMES，随 fitting 快照更新，样式 .f3d-legend）。连带类型放宽：`GarmentPart.key` 'front'→string（payload 真实片 key），buildFrontPair 内 placePoint 改传字面量 'front'（tsc 抓的——part.key 已宽化为 string 不再匹配 PieceKey）。金标平铺 describe 重写为全裁片 5 条（parts 展开 序/贴地 + z 居中/行序行距/成对片逐点等距变换/腰头单片原样）；**新坑：buildFlatLayout 内部自建 mesh，测试侧再调 buildClothMesh 得到引用不同的新对象，toBe 引用断言必炸——内容一致性由逐点等距变换用例钉死（纯函数同输入同输出），结构对账改顶点数**。vitest 63/63 + tsc 绿；引擎零改动（payload 5 片照旧）。

- **2026-09-15（晚八）机头+后片平面缝合拼合（用户口径「先将机头和后片缝合成为一个整体」——三期平铺从验证步进入装配步，业务装配链 腰头↔育克↔后片 的第一拼）**。**几何依据（先验证后动手）**：payload 边链是整版全局系坐标，back 上边 `top`（role seam，有 yoke 时后片沿机头下口线截断）与 yoke 下边 `bottom` 在整版上**是同一条净线**——恢复 fixture（`git show d784de1:...fixture_fitting_yoke.json`，schema v1 未变直接兼容）实测两链逐点坐标重合 max 点距 0.000000cm、边长全等 19.12。**实现 = STITCH_GROUP 拼合组**（assemble.ts）：back_piece 行并入 back_yoke 同行，组内共享跨片并集 bbox 的归一变换 = 按整版原样对齐，缝合边天然贴合（净样 1:1 零缝隙）；成对镜像升级为**整组镜像**（L 侧 = back+yoke 拼合体、R 侧整组关于组中线翻转——真裁剪 yoke 也是 L/R 两片）；无 yoke 款 STITCH_GROUP filter 掉缺片自动退化单片行（向后兼容，现有金标零改动全过）。视图无需改动：绿色后片 + 紫色育克两色相接即缝合线，肉眼直接检查对齐。金标 4 条（fixture_fitting_yoke.json）：拼合行 parts 序（yoke 不再独立成行）/缝合边逐点贴合 <1e-4cm（L/R 两侧）/整组镜像/拼合行总高 = 全局 y 并集 100.141cm。**坑：行内绝对坐标断言别写死——拼合行是第二行，行基 z ≠ 0（首版 z0≈0 断言必炸），行内相对量（z1−z0）才是稳定口径**。**留后**：有省款 yoke 下边是省闭口净样，省区段与整版下口线有微错位（后片省未收的真实缝前状态），处理待用户拍板。vitest 67/67 + tsc 绿；引擎零改动。

- **2026-09-15（晚九）袋贴+前片缝合拼合 + 贴合守卫（用户口径「还需要将前口袋也平铺出来，然后和前片缝合，形成一个整体」——装配链第二拼）**。**先验证后动手的关键发现（直/弯腰头两款 fixture 实测）**：①facing 净样外边（waist+side）**不与 front_piece 重合**（直腰头款 max 3.2~5.6cm）——因为外边 1:1 复制的是**挖削前**的原大片轮廓（腰口侧段+侧缝上段，这些段已被袋口挖掉、不在前片净样上）；②真正的缝合依据 = **facing 袋口净线（marks[0]）↔ front mouth 边**，两款实测逐点重合 0.000000cm → **整版坐标即缝后位置**：按整版全局坐标摆位，袋贴自动补齐袋口月牙缺口，袋口净线即缝合线；③顺带证实晚八「留后」事项：curved fixture 的**有省 yoke** bottom 与 back top 错位实测 ~12cm（省闭口净样 vs 整版下口线）。**实现**：STITCH_GROUP 升级 `STITCH_GROUPS`（结构化 members + **贴合守卫 aligned**——缝合依据线 seamGap 双向最近点距 max <0.5cm 才拼合，不贴合/组员缺片退化独立行不错位硬拼）；front_piece 组 = [front, facing]（marks 任一条与 mouth 贴合即拼）；back_piece 组守卫拦下有省款（无省款照拼）；**组内叠层**第 j 片 y = j×FLAT_PRIOR.stackStep(0.2)——袋贴袋口条带与前片净样重叠（衬在裤身里侧）共面 z-fight，微抬分层。fixture：`fixture_fitting_pocket.json` 新生成（引擎 FlowRunner(front_pocket+front_pocket_facing) 直出、补 ok/warnings）+ `fixture_fitting_curved_pocket.json` 从 git d784de1 恢复。金标 +11（袋贴拼合 4：parts 序/缝合依据前提/**组共享变换**〔L 侧 pos = xy − Cx/Cy 逐点恒定、R 侧翻号——整版对齐的本质断言〕/叠层；守卫 2：有省 yoke 退独立行、弯腰头袋贴照拼）。**坑：TS 方法解绑——`def.aligned(byKey.get)` 里 Map.prototype.get 丢失 this 运行时炸「incompatible receiver」，改 `(k) => byKey.get(k)` 箭头包装**。vitest 73/73 + tsc 绿；引擎零改动。

- **2026-09-15（晚十）「前口袋没渲染」排障：默认参数非渲染 bug + 图例缺片可见性**。用户报袋贴没渲染——定位：`front_pocket`/`front_pocket_facing` 默认 False（webschema「前口袋绘制」组 collapsed 条件可见），payload 无第 5 片 → 平铺无片属正常；渲染链路本身 pocket fixture 金标全过。修可见性不修参数（独立原则：3D 展示不改打版参数）：图例改**固定片序**（PIECE_LEGEND_ORDER 前/后/腰头/育克/袋贴），payload 缺的片灰显「（未开启）」+ hint 标开启路径（挖削前口袋 + 袋贴开关 → 重新生成）——缺片一眼可见，不再需要来回猜。vitest 73/73 + tsc 绿；引擎零改动。

- **2026-09-16 口袋默认口径：引擎最小裸版不动、Web 产品层默认「标准牛仔裤」（用户两轮反馈「前口袋没渲染/和前片是分开的」收口）**。**先验证拼合链路无 bug**：用户典型尺寸（74/91 六组）× 直/弯腰头 × 袋贴 offset/tangent/bulge 模式矩阵，缝合依据线（marks↔mouth）逐点重合 0.000000、守卫全过——拼合必生效，「分开」的根因 = 参数默认关、payload 无第 5 片。**引擎默认改开的尝试当日回滚**（options.py/api.py front_pocket/front_pocket_facing = True → pytest 53 金标炸）：实质冲突两条——①agent/extract 探针链锚定最小版型（L0 测量守卫 `if o.front_pocket or o.back_yoke or o.belt_loop: raise` 显式拒绝口袋特征、fallback_value 表锚定 False）；②extract 测量组合下袋贴默认参数几何越界（P2 深 12.0 − 侧深 3.5 > 可用弧长 0.85 直接 ValueError 炸版——与 watch_pocket 默认几何缺口同型）。**定案口径：引擎 PatternOptions 默认 = 最小裸版（打版引擎的中性默认，金标/extract 探词语义锚定它）；Web 初始 = 产品层「标准牛仔裤」**——useDraft options state 初始化对缺失口袋键补 true（存量 localStorage 缺键 = 未曾表态自动获得；显式关过的键值 false 不动），刷新即默认挖削+袋贴、自动首挂出拼合展示；CLI/引擎 API/extract 全不动。dist 重构建。**方法论：改引擎字段默认前先全链 grep 消费方（fallback 表/探针守卫/金标锚值），「中性引擎默认」与「产品默认展示配置」分层**。
- **2026-09-16（二）袋贴 marks 缩水不同态 bug（用户报「选择了 size_female_zhitong.toml 前片和前口袋还是分开的」——真引擎 bug，与前两条「参数未开」不同）**。**排障链**：用户尺寸单逐点复现 marks↔mouth seamGap=0.927cm>守卫阈值 0.5 → 拦截退独立行；偏差全链平行 ~0.9（非端点效应）；整版上切削线/净线端点相同排除构造差；量级比对命中缩水——尺寸单袋贴专用缩水经 10%/纬 6%、前片 0。**根因**：cutter.apply_shrinkage 把 marks 随裁片同步缩放（with_shrunk 保留 net_edges 原净样但 apply_shrinkage 返回件以 smarks 覆写 marks——2D 裁片出图正确：标记线跟随裁片缩水态），fitting payload 却消费 net_edges（未缩水）+ marks（已缩水）**两态混用**，共享缝合线在两片上不同态。此前全 fixture 重合 0 的原因是造夹具没设缩水——测试盲区正好绕过该分支。**修复**：fitting payload 袋贴 marks 覆写为整版袋口净线（front_pocket_flow._collect_facing_marks 升公共 collect_facing_marks，fitting 复用同一选择逻辑保同源；marks_main 参数原样输出全局系不走局部反变换）——与 front_piece mouth 边同一条线，任缩水口径恒重合。用户尺寸单复验 marks↔mouth=0.000000000、顺带 yoke↔back=0（该款 yoke 无省但此前同样被缩水+无省双因拦下，修复后恢复拼合）。金标增缩水款防回归（test_facing_marks_net_state_with_shrinkage）；pytest 1063 + vitest 73 全绿；build:engine + dist 重构建。**方法论：跨片共享缝合线的数据出口必须单一态（净样系），消费「已变换副本」（缩水/缝边后）做对齐判据必错；造 fixture 应覆盖非零缩水组合**。
- **2026-09-16（三）拼合组叠层方向拍板（用户口径「将顺序调整下，前片在前口袋上面」）**：front_piece 组 members 换序 [front_facing, front_piece]——片序即叠层序（第 j 片 y = j×stackStep），袋贴降下层、前片叠上层：俯视袋口条带重叠区显示前片（外观主体前片正面，袋贴衬里侧）。金标 parts 序/叠层断言同步反向；vitest 73/73 + tsc 绿 + dist 重构建。
- **2026-09-16（四）四期前身缝合立起（用户口径「现在将两个前浪弧线缝合在一起，然后整体立起来」——三期平铺验证通过后悬挂链回归）**。**方案设计期两个推翻性发现**：①袋贴净样**大部分在前片净样之外**（前片开口袋后腰口被挖短，mouth 弧以上的月牙区已从前片挖除；facing 外边 1:1 复制的是挖削**前**轮廓，mouth 两端点分别落在 facing 的 waist/side 边上、投影残差 ~0.003cm）——「袋贴直接绑前片网格」不可行（月牙区顶点 locate 全 null），改为**并集宿主**方案；②前身筒**旁挂人台 +X 侧不套轴**——CORE_SKIN 只把芯半径锚纸样围度（g/2π−0.98），与人台围度无约束（独立原则），套人台轴必穿体；旁挂偏移 = 人台半宽 + clearance + 芯场包络半径。**实现（前端 fitting3d/ 四新文件两改）**：`panel.ts` buildFrontPanel——front 边链 mouth 段原位替换为 facing 月牙边（waist 部分段命名 'waist'/top_chain 与 front.waist **同名相邻、mesh runs 自动聚合**，drape 悬挂 pin 语义自动覆盖袋贴腰口段=整个前身挂腰物理正确；side 部分段命名 'side'/seam 同理聚合），守卫（frontFacingAligned 与平铺拼合同口径提炼共用 + mouth 端点投影 0.5cm）失败退化纯前片宿主+袋贴留平铺；`rider.ts` bindRider/rideRider——被载片不进粒子系，每顶点 host.locate 重心绑三角形（**重心权重对仿射变换精确**，verlet 帧间分段线性零额外误差），每帧由宿主三角形顶点插值回填，L/R 共享一份绑定（2D 绑定与 side 无关）；袋贴径向内偏 riderStep(0.2) 衬里侧（「前片在前口袋上面」口径在立体侧的对应）；**locate 缺口兜底 = 宿主边界环最近段插值**（Delaunay 无约束+质心过滤，凹弧处窄楔形三角形被丢——月牙外边恰是凹弧，实测 facing 11/76 顶点命中；段插值=弦近似弧、误差亚毫米，远优于最近角）；render.ts buildRiderView（同材质口径，每 side 一 Mesh 逐帧直写）；assemble.ts buildFlatLayout 增 exclude 参数（排除锚且守卫通过→**整组**离开平铺，缝合整体要么全平铺要么全立起；守卫不过组员照旧独立行=退化兜底）；Fitting3DView 裁片 effect 重构：panel→exclude 平铺→芯→场→buildFrontPair(panel.host)→buildDrape→双 rider view→rAF 一帧一步 settle/frozen 停、cleanup cancel+dispose；取景 hangXRef 语义改**全场景包络中心**（人台左沿~平铺右沿中点，双主体=人台+悬挂筒↔平铺组）。**测试**：panel.test（mouth 内部化/waist 聚合变长/**覆盖性 = locate 非空或贴宿主边界 <0.1cm**〔并集完备性的正确验收，locate 非 null 过强——边界三角形覆盖天然有缺口〕/面积比 ≥99%/两路退化）、rider.test（平面恒等分层容差：非兜底 1e-4、兜底弓高 <0.1/刚体跟随仿射封闭/径向内偏）、drape.test 增**并集宿主收敛金标**（本方案最大数值风险点：月牙带不破坏既有口径——600 帧真 settle/pin 恒守/缝 avg<0.3/穿透 0/质心降 >2cm，实测全过）。**坑三条**：①Float32 输出对照 Float64 期望，toBeCloseTo 9 位必炸（~5e-7 量化伪差）——分位数 4~5 位或分层容差；②测试构造「平移 facing 破坏守卫」必须**连同 marks 一起平移**（marks 是独立字段，只动 edges 守卫照过、退化原因变投影超差）；③期望行序记 payload 片序（front_facing 在 back 之后，不是直觉的在前）。vitest 89/89 + tsc + vite build 绿；dev server 起于 localhost:5173（Vite 监听 ::1，curl 127.0.0.1 探不到）。引擎零改动（无需 build:engine）。

- **2026-09-16（五）前身悬挂改自由垂（用户目检四期形态两报障：「前片裆尖缝合的点要往内拉，想象下人体的裆部，这个交点是在裆下的」+「裤子立起来为什么前片是往前凸的，如果拿着前片上部不可能是这个造型」）**。**两症状同根因：撑型芯把前身布撑成前凸筒**——裆尖（前浪缝合交点）被芯顶在筒面最前凸处（芯裆部毗围代理 2×大腿>臀 本就比臀突出 ~5cm），整体保持扇区撑开态；而真实「提着腰口的前片」没有东西撑它：前中缝竖直下垂、前浪凹弧（布不可拉伸保持纸样弧形）自然把裆尖收到裆下、布沿缝两侧垂落成对折门帘。**修法**：drape.ts `buildDrape(garment, field)` 签名放宽 `field: BodyField | null`——**null = 自由垂**（径向碰撞 collide 对 null 短路；Fitting3DView 传 null），新增 **collideGround 地面约束**（y<0 推回 + 法向速度清零 + 切向摩擦防滑转——悬挂高 = 纸样腰高 ~97、布全长 ~102，下摆拖地 ~5cm 铺地是真实提着前片的形态，不穿地）；芯/场保留调用但角色收窄为**初摆位半径 + 取景包络**（buildFrontPair 摆位、envelopeR 旁挂偏移），不进碰撞。**旧口径显式退役**：「勿去碰撞（腿间塌片 wrapHem 0.50）」是 2026-09-14 **整裤**包腿成形的要求——前身半身布垂成对折门帘正是本次预期形态，drape 头注写明勿混。**金标重写（drape.test 两 describe 改自由垂口径）**：穿透 0 断言随芯碰撞退役删除，换**不穿地 y≥0**；新增五期两条形态金标（用户口径的量化）——①**裆尖内收**：front_crotch_vertex（rise 链首点）终态轴心半径比摆位初态收缩 ≥2cm（实测 pocket fixture 14.13→11.39 = −2.74，下沉 78→73.5）；②**前向不凸**：全体粒子前向 max z 终态比初态收缩 ≥2cm（实测 16.61→11.88 = −4.73）；既有口径照旧（600 帧内真 settle——实测 181 帧、pin 恒守、缝 avg<0.3、质心降 >2cm 实测 −4.58）。两 describe 收敛共用 assertFreeHang 助手。vitest 89/89 + tsc + vite build 绿；引擎零改动。

- **2026-09-16（六）后身缝合立起（用户口径「现在把两个后片（包含机头）缝合，将后浪缝合在一起，然后像两个前片一样悬挂」——前身方案镜像复制到后身，前后两筒并排旁挂、互不缝合〔内缝/侧缝留给后续整裤期〕）**。**实现（前端 fitting3d/ 五文件，引擎零改动）**：①`panel.ts` buildBackPanel——后身并集宿主 = back_piece 边链 **top 段（机头下口线 P0→PN）丢弃内部化为缝线**，数组末尾接 yoke 余边反向闭环路径（cb 反向 P0→O + 腰口反向 O→X + 侧缝反向 X→PN；与前身 mouth 段「原位替换」不同——后身缝合依据线是两片共享线、无需部分段截取）：**边链重排的聚合关键** = back.cb（裆尖→P0）与 yoke cb 反向段（P0→O）同名相邻，mesh runs 聚合成**整条后浪**（裆尖→腰口贯通育克，yoke fixture 实测 29.0cm）——drape 后中缝合对沿 cb 配对天然缝到腰；yoke 腰口 top（反向）顶替 top_chain（悬挂 pin 自动覆盖）；守卫 = backYokeAligned（与平铺拼合同口径提炼共用：back.top ↔ yoke.bottom seamGap <0.5，无省款 fixture 逐点重合 0；**有省款 yoke 下边是省闭口净样错位 ~12cm 拦下**退化纯后片+育克留平铺；无育克 = 正常款型纯后片即后身、信息性 warning 不算守卫失败）。②`placement.ts` PieceKey 增 'back'：placePoint 后扇区 L θ∈[−90°,−180°]（侧缝端与前扇区同角 ±90°、后中端 ±180° 与前中相对——穿着拓扑同构），R 镜像 θ→−θ。③`assemble.ts` buildFrontPair 泛化 buildHangPair(key, mesh, field)（前/后身悬挂对共用）；backYokeAligned 导出。④`drape.ts` 中缝对泛化：seamIdx 取 runs 的 rise|cb（后中 cb 链同号配对 rest=0），错误信息去前片硬编码。⑤Fitting3DView：backPanel→后扇区 pair→backSim（与前身两个独立 verlet 互不相干，同一 rAF 各步进一步）、后片/育克双 rider（**径向均 0**——真裤育克与后片沿缝线相邻共面非叠层，无袋贴衬里语义）、后身筒并排 `backX = hangX + 2×envelopeR + clearance`、平铺组让位到后身筒右、exclude 增 back_piece〔hasYoke 再加 back_yoke〕、panelHint 合并前后 warnings。**金标②改口径（缝线不凸钉中缝链而非全体粒子）**：后身实测全粒 −Z 极值仅缩 0.9cm 假阴——**终态极值点是地面堆布（y≈0）**，拖地下摆径向摊开污染「不凸」判据（后片布多堆得宽、前身恰因布少余量通过），改钉中缝链本体（对折线不再被撑到摆位弧半径：front 16.61→5.72 / back 16.08→8.63，阈值 ≥2cm 照旧）+ assertFreeHang 增 seamDir（+1 前中取 z / −1 后中取 −z，镜像同构）。金标：panel.test 后身 describe（cb 单一 run 长度 = 两链和 29.0、链首 = 裆尖、top_chain = yoke top、覆盖性/面积比、无育克信息性退化 + 有省款守卫退化）、drape.test 后身并集宿主自由垂（yoke fixture 249 帧真收敛全口径过）、assemble.test 后身组 exclude 三用例 + 后片后扇区摆位（角度域/镜像/穿透零）、garment.test placePoint back 金标。vitest 106/106 + tsc -b 绿。

- **2026-09-16（七）悬挂「拉直」改整圈全向钉直挂（用户报障「缝合悬挂的前片和后片拉直一点，现在前中和后中是往里折的」）**。**根因 = 二轮 Y-only 腰口钉的环向滑移**：腰口只钉高度、水平可滑，布自重把整圈腰口滑向筒轴心——中缝跟着荡离前中/后中（前中缝外伸 16.61→5.72cm，近轴心），整幅布塌成对折门帘；五期当时把「对折门帘」当预期形态（无芯自由垂的推理），用户目检推翻。**修法两件套（互为前提）**：①`assemble.buildHangPair` 腰口弧长重参数化——均匀 x→θ 映射固有偏心（中缝腰角实测偏心 front −3.9cm ≈ 17° / back −5.8cm），顶链顶点在 x 映射后改按腰口弧长分数 s∈[0,1]（s=0 = 中缝腰角端，用中缝链腰口端顶点定向）重铺扇区：中缝腰角精确落 0°/−180°、侧缝腰角精确落 ±90°、θ 沿链单调，R 侧 θ→−θ 镜像求场，片身其余顶点照旧 x 映射；②`drape.buildDrape` 悬挂支点改**整圈全向钉（无例外）**+ 终点角全向锚。**一轮教训的根源重定位**：2026-09-15 一轮全向钉「锁成刚性折线/顶中缺口」的根因是**映射偏心而非钉本身**——重参数化后 L/R 中缝腰角镜像重合在中面，「钉与缝同意」按构造成立，无需二轮的角点 Y-only 例外（实测缝对 avg/p95 = 0）；pinY 机制（pinYIdx/pinYTarget/projectPins Y 环）整体删除（钉集全向化后无消费者）。**坑一个**：中缝链 runs 末顶点 ≠ 腰口角本体——边界重采样每边不含终点，run 末采样距角 ~1 个步长（腰口角 = 顶链首采样），按 run 末顶点找角会找错顶点（首版角例外因此未生效，靠整圈钉+重参数化恰好无症状，暴露后干脆去掉例外）。**金标随口径换代**：质心沉降 >2cm 断言退役（布全长 = 悬挂高、下摆摆位即恰拖地，直挂无可沉降空间，实测 Δ≈0）；五期「裆尖内收/缝线不凸」两条退役（直挂下中缝本就该留在外侧，收缩断言反向失效）——替**六期拉直三条**：中缝腰角居中 |x|<0.1（实测 0.000）/ 中缝不塌轴：终态中缝链缝向平均外伸 ≥ 摆位 55%（实测 front 87%、back 65%；Y-only 口径曾塌 ~5.7cm）/ 缝不外凸：终态最大外伸 ≤ 摆位 +0.5cm（实测 16.61→15.85 / 16.08→11.09）；assemble.test 增腰口重参数化金标（中缝腰角落中面 / 侧缝腰角落 ±90° / θ 单调铺满，前/后身并集双宿主）。收敛实测 front 171 / back 203 帧真 settle。vitest 108/108 + tsc -b + dist 重构建绿；引擎零改动。

- **2026-09-17 七期侧缝缝合整裤 + 当日整体还原（补记留档——实现、验收、还原均在本日完成，因未进 git 无痕，本条为唯一留档）**。**七期 = 侧缝缝合 + 前后身合并单筒**：panel.ts 边链环形重排（前身宿主侧缝边与后身宿主侧缝边按整版全局坐标拼接）+ assemble.ts mergeGarments（双宿主粒子系合并、offset 重排）+ drape.ts SeamSpec 双模式（mirror 同号镜像 / **arclen 弧长配对**——侧缝两链采样数不同步，按弧长分数配对；吃势〔前长后短差量〕按弧长均匀吸收）+ Fitting3DView 合并单筒展示 + 五处金标。**验收实测**：~170 帧真 settle、侧缝 248 对 avg 0.045 / p95 0.000——缝合本身收敛健康。**当日用户裁决整体还原**（两筒并排形态保留、侧缝不缝），全部代码还原至六期（七）态。**留档要点（后续整裤期复用）**：①arclen 配对 + 吃势均匀吸收方案可复用（248 对 p95 0.000 验证配对/吸收数学成立）；②七期失败不在缝合数值而在**合并单筒后的形态语义**——侧缝缝合把前后身拉成一个闭合筒后，无腰头/无内缝的半成品筒在自由垂下扭转、观感不如两筒并排清晰；后续整裤期应按业务装配链一次补齐（侧缝+内缝+腰头）而不是单缝逐条加；③还原后六期金标 108/108 全绿复核。

- **2026-09-17（二）悬挂两优化：不拖地（hangLift）+ 顶部圆弧（侧缝语义摆位 + 腰头刚度带）**。用户两项口径：「将悬挂高度调整，不要让裤子拖地」「前片和后片的顶部侧缝边会有折，不要折保持拼合后顶部〔腰头缝合线〕是圆弧」；第 2 点 AskUserQuestion 拍板**「保持两筒并排，顶部撑圆」**（否决缝侧缝复活七期）。**①不拖地**：布全长 > 腰口挂高（筒面斜挂+腰弧下沉+拉伸，实测余量 ~8-10cm），基线 front 102 / back 202 粒子贴地堆布；抬升扫描（+8 不足 2-44 粒仍触地 / +16 过高）定 **hangLift 12**——三款夹具零触地、下摆离地 3.3~4.4cm（`HANG_PRIOR.hangLift`，`buildHangPair` 末尾全 pos y 统一施加，场查询仍用纸样 y）；地面碰撞降级安全网。**②顶部圆弧双根因**：(a) **摆位折角**——顶链弧长重参数化把侧缝腰角精确摆在 ±90°，但其正下方的侧缝边仍用均匀 x→θ 映射——牛仔裤侧缝腰口撇势内收（弯曲边）被映射到 θ≈−76°，腰口角正下方留 **~14° 楔形折角**；(b) **自由边内摆**——无侧缝缝合的自由半身筒，侧缝自由边在自由垂中向筒轴心内摆（后身最深 ~25°，−90.7°→−115°）。**修法**：侧缝边**语义摆位**（'side' 名 run 全量整条摆 θ=±90° 竖直、r = 场+gap、y = 纸样高原样——侧缝边在穿着语义上是体侧竖直线，纸样弯曲由绕体 wrapping 吸收，与腰口重参数化同因；后宿主育克侧段与后片侧缝可能各自成 run，filter 全覆盖）+ 侧缝边顶部 **sideHold(10cm) 全向钉 = 腰头缝合线的刚度带**（真实成衣此区由腰头撑圆；run 首采样 = 腰口终点角已在钉集须去重）。**实测**：带缘出口偏差 ≤0.7°（往下自由内摆渐增至 2.9~5.5° 属自然垂）；settle front 184 / back 186 / curved 189 帧真收敛、minY 4.44 / 3.98 / 3.77 零触地。**金标换代**：drape.test 不穿地 y≥0 → **不拖地 minY>1** + 侧缝顶部刚度带终态 θ=±90°（L −X / R +X、z≈0）；assemble.test 增侧缝语义摆位金标（side run 全顶点 θ=±90° + y = 纸样高 + hangLift / 全 pos y ≥ hangLift），穿透零金标改**纸样空间查验**（hangLift 抬升后 pos y ≠ 场查询 y，直接查会把抬升位当穿透）。vitest 112/112 + tsc -b + vite build 绿；引擎零改动。

- **2026-09-17（三）八期整裤缝合：侧缝+内缝缝合、四缝汇集裆交叉点（v3 两腿分离芯 + 截面碰撞 + 腿局部摆位）**。用户三步口径：①前后片侧缝缝合形成腰圆；②一边前片+一边后片内缝缝合形成两条裤筒；③两内缝+前中缝+后中缝汇集到裆交叉点。AskUserQuestion 两拍板：**解算开撑型芯碰撞**（裤腿圆整两腿分开——2026-09-14「整裤包腿必须有碰撞体」教训的正名；CORE_SKIN 0.98 口径延续）、**本期不含腰头**（腰圆整圈钉挂撑形、sideHold 刚度带暂代腰头，腰头裁片照旧平铺；七期留档「按业务装配链一次补齐」的侧缝+内缝部分本期落实，腰头立体化留下期）。

  **架构**：七期 mergeGarments 重做并扩为单 sim 四 part——`assemble.buildFullPair(front宿主, back宿主, field, forkY, legAxis)` 产出 parts=[fL,fR,bL,bR]（offset 0/nF/2nF/2nF+nB，panel 并集宿主不变）+ `seams.buildSeamSet` 四族缝合对（rise/cb **镜像族**同宿主同号配对〔六期机制泛化〕、side/inseam **弧长族**跨宿主 runIndexAt 弧长分数配对〔side 锚腰口端、inseam 锚脚口端；吃势沿链均匀吸收，side 前后链 99.03 vs 99.6、inseam 78.2 vs 78.0；back 宿主 side 两条 run 经 mesh.mergeRuns 首采样 y 降序合链〕、**tip 补焊 ×2**——角点共享规则下 tip 本体=rise/cb 链首采样、inseam 末采样是 tip 前一步，不补焊前/后裆尖各悬 ~1cm；rise 末对+tipL+cb 首对+tipR 构成零 rest 四边形自动坍缩为裆交叉点〕+ fly 连裁 rise 缺失整族跳过/tip 走 inseam 环下一点兜底）。drape 求解循环零改动（全族 rest=0 同一扁平数组），buildDrape 增 seamGroups 分族统计/yLift/crotchHold(0 默认关的 fallback 旋钮)。

  **弯路一课（v2 花生+径向场证伪，勿回头）**：第一版把内缝边摆到花生环 ±z 腰谷、指望焊线「穿 pinch 闭拢」——实测**内缝焊线绕腿瓣外侧闭拢，整条裤腿拖成侧挂门帘**（inseam 终态 θ≈±90°、r≈腿瓣外 18+）。根因两层：①平滑花生的「腰谷」半径 ≈0.58rc，是**实心桥**不是 pinch——前后内缝边隔桥相望，径向碰撞场（星形实心：r<R(θ) 全推外）下**永远焊不上**，任何「场开槽」都救不了（槽底仍是实心）；②**径向场本质表示不了腿间空隙**（支撑函数=凸包上界）——旧链用 BVH 表面碰撞正因此。中途 carveCrotchSlot 场刻槽方案当场证伪废弃。

  **v3 芯（三实体）**：躯干管（fork+LEG_OVERLAP(4)→腰+8 圆环）+ 左右**分离腿管**（hem−1.5→fork+4，轴 ±(r+gapHalf)，gapHalf 从 fork 处 LEG_GAP_MIN(0.8，防 v1 双管相切刀口)线性张开到 LEG_GAP_TAPER(12) 以下 LEG_GAP_HALF(2.6，总 5.2)——**裆下两腿真分离**，腿间空隙给内缝焊线与裆交叉点安身（真裤两腿间有缝空隙）；腿管上延过 fork = fork 带截面只有分离腿管（躯干环出现在 fork 行会把裆尖摆位困进管内——实测 2 粒子穿透定位即此）；v1 四死路（留槽捷径/相切刀口/交叠符号陷阱/细龙骨穿膛）的八期对策：侧缝语义摆位+sideHold 钉死外侧线防抄近道、GAP_MIN 真间隙、腿管不交叠、摆位零间隙无需 preRelax 拉链。v2 花生退役（「内缝闭合在瓣内侧」设想只在表面碰撞下成立）。

  **碰撞升级（截面多边形）**：buildBodyField 留存行截面环（躯干行 1 环/腿行 2 环；SliceRing 质心+包围半径 quick reject），drape.collide 从径向推改为**截面最近边界+skin·外法线推出**（性能口径：sqrt 代 hypot、深穿兜底只在包围圆内才做射线判内）。实战修一个真 bug：bd<skin 分支原按「边界→粒子」方向推——**环内粒子越推越深自陷**（终态 80 粒子悬在环内 ~0.5cm），统一改沿外法线推到边界外 skin 距后穿透归零。

  **摆位（腿局部圆环）**：腿区 y<fork 逐高度局部归一 t=(x−xi)/(xo−xi)（xi/xo=inseam 链/side 合链的 chainXAt 插值），φ=t·π 从腿内侧经前/后到腿外侧绕腿轴铺 rAt+skin+gap——**内缝边落腿内侧线（前后宿主相邻共线，焊对初始间隙 ≈0）**、侧缝边落腿外侧线（与 θ=±90° 语义摆位同点）；fork 向上 forkBlend(8) 线性过渡（裆角布从躯干前面顺滑收进腿间；混合弦线落环内回退径向摆位保零穿透）。腰圆 360° 整圈弧长重参数化（fL −f·90°/fR +f·90°/bL −180°+f·90°/bR 180°−f·90°，四段拼闭）；**侧缝腰角前后共点 snap**（前后腰口曲线起翘差 front 97.7/back 98.1，退化款差 ~4——不 snap 则钉与缝在腰角永久拔河；角点本体=**side 合链首采样**，第一版写 top run 末端〔角点前一步〕被 Step3 侧缝摆位覆盖、测试同错顶点假绿，移到 Step3 后+改 side 链首才真生效）。内缝语义摆位=腿内侧线 cAt−rAt−skin−gap 钳 ≥0.3 不越中线（近裆收敛到 ±0.3 与裆尖焊拢一致）。有省款退化：panel degenerate 路径把 back top 边（引擎因有育克覆写 seam）角色升回 top_chain 供钉挂。

  **实测（三款夹具+退化款 vitest 全绿 119）**：settle 99~174 帧真收敛；四族缝 rise/cb/inseam/tip **全族 avg=p95=0**（镜像/相邻共线摆位起点即闭无对抗）、side avg 0.026/p95 0.176（吃势均匀吸收）；裆四尖两两最大距 <1cm；hangLift 重定标 12→**8**（整裤全长 ~99cm PBD 持续重力下拉伸 ~4cm，lift 4 时 hem 落地、8 时离地 ~3-4 不拖地）；终态截面穿透 0（穿透查验改截面环口径 pointInRings——两腿分离芯的腿间空隙是合法布位，径向口径会误判）；有省退化款 side 固有开口 avg 0.42/p95 3.66（back 缺育克矮 ~4cm，panelHint 已警示）阈值放宽 documented。浏览器目检（headless Chrome 预种 localStorage 驱动）：整裤旁挂人台 +X 侧、腰圆闭合、两腿分开圆整、分色正确、腰头平铺照旧。

  **金标换代**：drape.test 四 describe 换代整裤（assertFullPants：真收敛/四 part 钉恒守/四族分族 stats/裆四尖/侧缝首对/不拖地/截面穿透零；退化款 degradedSide 放宽）；assemble.test 增 buildFullPair 六用例（结构/腰圆四角+单调+共点/内缝内侧线/腿区圆环距离/侧缝+镜像+穿透）、删六期双筒四 describe（buildHangPair 函数连同退役）；core.test 换代三实体（fork 行恰 2 环/腿管逐站锁定/腿间隙 2×GAP_MIN→2×GAP_HALF）；新增 seams.test（纯拓扑：合链/四族配对数/锚定端/tip 顶点号/fly 分支）。**附带发现（既有问题非本期）**：fresh 浏览器 profile（无 localStorage）下 auto 首挂/本地计算 422——useDraft measurements 初始化 `saved?.measurements ?? {}` 为空、表单显示 schema 默认值但 store 空，用户存量浏览器正常、首访用户会踩；待单独修。

- **2026-09-17（四）裆圆角修复（用户报障「裆部处前片和后片有那么生硬的凸起」）**。诊断：前中缝终态剖面实测 z 从裆尖 3.2 → 仅 8cm 图纸高内冲到 **16.1**（比腰口肚子 z≈12 还凸出）再回落——生硬尖帐篷+硬折痕。根因在芯：躯干管是**全半径直圆柱捅到 fork+4 才平底截断**，前/后中布被摆位（径向场=圆柱壁）撑到 z≈17.8，又必须收回裆点 z≈3，布被撑成帐篷。修法 = 躯干管底加**裆圆角带**（真身形依据：骨盆底耻骨/会阴区前后收拢、左右髋仍宽）：`CROTCH_FILLET(6)` 带内截面 z 向压缩成椭圆（zScale 从底行 `CROTCH_ZMIN(0.28)` smoothstep 到全圆、x 保持全宽），躯干管底从 fork+4 降到 fork+`TORSO_BOTTOM(2)`（fork..fork+2 仍是腿管口袋，裆尖不困管内）；摆位半径走场表**自动跟随圆角卷进裆**（placePoint/碰撞零改动）。实测修复后前中剖面单调顺滑（2.8→5.2→7.7→9.6→11.3→13.3→15.1 臀位→12，无过冲尖峰），截图前后对比裆部帐篷与折痕消失。金标换代：掐腰口径改「左右侧向宽度不掐 + 圆角带前后收拢存在 + 臀站恢复全圆」（旧周长口径在圆角带必炸——前后收拢属设计）；121 绿。

- **2026-09-17（五）整裤改自由垂：腰部圆形撑开 + 真实物理垂挂**。用户口径「只保留腰部圆形撑开，其他地方就真实的物理垂挂」（看（四）裆圆角修复后的芯碰撞穿着形后提出；另明确**不要代为 git 提交**）。实现极小：Fitting3DView 解算 `buildDrape(pair, null)`（五期自由垂口径复活）——腰口整圈全向钉 = 圆形撑环 + sideHold(10) 腰头代形刚度带，其下布自重下垂出自然褶皱、腿筒前后压扁（真挂裤观感）；芯/摆位全保留（腰圆半径 + 初始形态动画起点仍由 v3 芯给），仅不参与碰撞。**芯碰撞机制整体保留**（drape 传 field 即生效，机制与金标门控都健在——穿透查验门控改跟 sim.field 走，摆位用的场不参与解算不能拿来查）。hangLift 重定回 12（自由垂褶皱收拢余量比碰撞形态大）。已知取舍：无布-布碰撞，两腿相贴处可能轻微互穿（用户未反馈前接受；备选变体 = 只给两根腿管保留碰撞）。金标适配：drape.test 四 describe 照旧全绿（四族缝/裆汇集/腰圆闭合/不拖地在自由垂下全部达标），穿透查验 sim.field 门控跳过；120 绿。截图 out/freehang_front.png / freehang_drag.png。代码停工作区未提交（用户口径）。

- **2026-09-17（六）九期腰头立体化：腰头与裤身缝合（挂腰头）**。用户口径「把腰头和裤身缝合，**腰头两边是前中线、腰头中点是后中线**」。发现 payload 腰头片本就带完整边链（frame='local'，bottom×2+left_end/right_end+top，**以中点为原点左右对称**）且直款 bottom 70.10 = 前后腰弧和 70.10 **严格相等**（腰长不变量）——两端=前中/中点=后中的映射与四段腰弧自然环序 fL(前中→左侧)→bL(左侧→后中)→bR(后中→右侧)→fR(右侧→前中) 完全吻合（左右半弧镜像相等 ⇒ 环 s=P/2 精确落后中）。**实现**：新 garment/band.ts——`ringWalk`（四段 top_chain 环序行走，逆序链行走弧 = run.length−原弧）+ `bandBottomChain`（两条 bottom run 按端点衔接排成端→中点→端整链；角点共享规则两坑：衔接端点差一步长须放宽判定、链端缺的角点在 end run 首/末采样上须**共线延展**判定补回——否则端头投影内缩 1.5cm）+ `bandTargets`（每带顶点 (u,v) 正交投影到底边链：u=弧长分数、v=带法向距离——弯腰头底边是曲线，v 不能取局部 y；**摆位与配对同源**，带底顶点落在配对环顶点原位=初始间隙 0）+ `bandEndPairs`（两端 weld **按等高配对**——两 end 边采样高度集错开一步长，索引一正一反配错 1.33cm）；assemble.buildFullPair 增第 5 可选 part（坏链降级无腰头不炸主展示）；seams 增 bandWaist（带底↔环，吃势=带底与纸样腰弧差沿环均匀吸收——弯款差=省道开口，呈腰部轻微软褶=未收省真实缝前语义）+ bandEnds（前中会合=扣好的闭合环）；drape 挂腰头口径：**只钉带顶（belt line）整圈**——身片顶弧经缝挂不直钉（钉与缝二选一）、sideHold 随真腰头退役；腰头为 sim 参与片直渲（render.buildSimView，非贴层）；平铺 exclude 腰头。**收敛一课**：挂腰头后育克款前片裆区持续蠕动 ~1.5cm/s 600 帧不衰（阻尼 0.98 无效=GS 泵非衰减问题；丹宁增硬 span6/0.5 旧链口径只压一半 vmax 19→11）——根因=**裤身绕竖轴扭摆失去方位锚**（无腰头直钉时腰环本身就是刚性方位锚）；修=四个**侧缝腰角**（snap 共点角）并入钉集，190 帧真收敛（语义仍在「腰部圆形撑开」内）。实测四款夹具 bandWaist/bandEnds avg=p95=0、125 测试全绿；截图 out/band_front.png / band_drag.png。金标：assemble 腰头五用例（**用户口径量化**：u=0/1 环伙伴 θ≈0(+Z) 前中、u=0.5 伙伴 θ=−180(−Z) 后中 / 对账 runLength<0.5% / bandWaist 初始距离=0 / v∈[0,width] / bandEnds 共位）。

- **2026-09-17（七）腰头前后中开洞修复（用户报障「腰头缝合之后，前中和后中附近处会有洞」）**。量化诊断（缝度量 vs 渲染对照）：bandWaist 缝对距离全部 <0.3cm——缝没开；剖面实测腰口下 0cm 处前中 r=10.2 vs 侧 11.9（放位圆 11.36）：**只钉带顶时自由垂挂的对折趋势把腰口前中/后中往里拖 ~1.2cm，腰头内倾 + 布面继续内折（腰口下 4cm 处 r=9.0）= 前后中透光楔形洞**；另有后中接缝孪生角（bL|bR 两个重合腰角只配到一个）悬空 ~1cm 小洞。**修法三件**：①钉集改**腰口区全钉**——身片腰口环整圈照旧钉（「腰部圆形撑开」全义）+ 带顶/带底两缘钉（带 = 两缘被持的真实布）+ sideHold 退役；中间试过只加钉带底——缝对与身片网格 GS 冲突平衡留 ~0.5cm 均匀缝口（缝拉一半、网格每迭代再拽开一半，约束冲突平衡的典型形态），全钉后两侧钉位本就重合、缝恒 0。②seams bandWaist 增**接缝孪生角补配对**（walk 弧距接缝弧 0/P/2 <0.6 的配对追加同接缝另一重合角；弧距按环形回绕算——0 与 P 是同一前中接缝，先按绝对差找不到前中孪生是坑）。③金标换代：钉数对账 = Σ身片(top+1)+带 top+1+带底链；bandWaist 阈值收紧 avg<0.1/p95<0.3（全钉后实测≈0）。125 全绿；截图对比 out/fix_zoom.png vs out/hole_zoom2.png——前后中楔形口消失、腰头全线贴合、缝线以下自然垂褶保留。**教训**：悬挂刚体的「软传力」（缝对拉两半）与网格持续张力在 GS 定序下有均匀平衡缝口——要零缝口必须有一侧硬钉；「提着腰头上沿」的纯物理口径与「腰部圆形撑开」的展示口径冲突时按用户本义取后者。

- **2026-09-17（八）侧缝缝口凸起摊平（用户报障「前片后片侧缝缝合的线有很明显的凸起，是不是可以向两边拉直」）**。诊断：自由垂挂前后片近乎平行贴合，侧缝 = **180° 对折的刀锋卷边**（缝尖 r 剖面 11.4→15.2→~14.8 全程 ≈C/2 = 前后片宽度和=自然摊平宽度，周长守恒所迫非多余布量；全局 bend 0.3→0.45 实测**无效**——重力压平镜头截面缝尖不动，刚度拗不过）。修法 = **缝口摊平窗**（「向两边拉直」的物理实现）：mesh.bendKArr 复活（drape.stampSeamFlatten——距 side/inseam 链纸样距离 <seamFlattenSpan(3cm) 的弯曲约束刚度升到 seamFlattenStiffness(0.5，旧链丹宁稳定窗)，折痕从刀锋摊成 ±3cm 缓坡；drape 弯曲求解消费逐约束刚度）；缝尖半径不动（自然宽度，动了反而错）——软掉的是卷边视觉。实测 settle 191 帧照旧、125 全绿；截图对比 out/flat_zoom.png vs out/side_zoom.png 缝口卷边明显软化。若后续想把**横向轮廓**也收窄/拉直（缝尖 ~15 → 更贴腿筒），自由垂下只有两条路：引导线钉或局部碰撞（芯碰撞版已有机制），届时找用户拍板。

- **2026-09-17（九）腰臀段侧缝山脊修复：混合形态（腰臀芯碰撞 + 腿段自由垂）**。用户报障「腰头到臀部部分的侧缝像山脊一样凸出，尤其快到腰头处感觉前后片叠合；前片后片相连整体应该是顺滑的弯曲线」。诊断：钉圆的腰口往下布塌得太快且**不对称**——后片腰下即塌到 z≈0（垂平）、前片被前中缝撑住保持 z≈10，**D 形截面**把前后折角全堆在缝口几厘米内成脊、缝口下前后片贴叠；缝尖 r≈15=C/2 自然摊平宽度（周长守恒，非布量问题）。两轮无效修法留档：①全局 bend 0.3→0.45——重力压平镜头截面缝尖纹丝不动（同（八）课）；②腰臀过渡刚度带（bendKArr y 窗 waistTransitionSpan=15 内 0.5）——分离剖面数值不动（刚度拗不过悬挂张力）。**有效修法 = collideAboveY 混合形态**：buildDrape 增碰撞纸样 y 下限（= 裆站 + TORSO_BOTTOM = 躯干管底，其下本无躯干环）——腰臀段恢复 v3 芯截面碰撞（布依**自身纸样围度**的隐形形体撑开 = 顺滑圆曲线、前后片同步、缝口折角消失），腿段（躯干管底以下）照旧真实自由垂（用户认可的臀下顺滑垂坠保留）；摆位/钉集/缝合全不动。金标：穿透查验只在碰撞段查（腿段摊平布本就穿过腿管环位——混合口径）；125 全绿；截图对比 out/hybrid_zoom.png vs out/ridge_zoom.png 山脊消失。**形态学定位**：用户要的「前片后片相连顺滑弯曲线」= 依形截面；纯自由垂给不了（前中缝结构不对称注定 D 形），芯碰撞是唯一稳定给圆曲线的机制——（五）自由垂口径在腰臀段被本条收敛为混合形态。

- **2026-09-17（十）腰头侧缝不平整修复（用户问诊「是因为后片高前片低的缘故么」——是）**。前后腰口在侧缝高差 ~0.4cm（实测 front 97.7 / back 98.1），（六）的 corner snap 只平均了角点本体，缝口两侧边沿各持己高，腰头底边跟出台阶。修法 = **侧缝腰口高差渐变**（assemble：角点两侧 waistBlendSpan(4cm) 弧内顶链 y 向 snap 高度线性渐变 = 真实缝头吸收高差的展示等价；整体腰线前低后高的自然翘保留）。**坑**：渐变只改 y 不改半径 → 被调低的钉点半径仍是旧 y 的场值，陷进碰撞壳 ~0.07cm 与 collide 打架（有省款金标「钉恒守」0.071 实测抓住）——渐变必须**按新 y 重算半径**（field(yNew,θ)+gap，θ 不动）。金标 125 全绿；带底 y 环剖验证侧缝 ±90° 两侧 ~3cm 内缓坡（109.58→110.00→110.40）；截图 out/wb_zoom.png。

- **2026-09-17（十一）回归全域自由垂 + 宽摊平窗（用户口径「腰头保持一圈，下面保持真实物理悬挂，侧缝不要像山脊一样凸出」）**。撤（九）腰臀段芯碰撞（collideAboveY 机制保留在 drape 备用——传 field+下限即混合形态），摊平窗 seamFlattenSpan 3→5：缝条足够宽才像熨过的缝头硬条，折痕移到窗缘、缝身留在布面中间平顺——±3 时缝本身仍是折尖（（八）后用户仍见山脊的根因）。**度量纠错**：（九）诊断的「后片塌平/前片撑住的 D 形不对称」是度量 bug（「后中 z」取了行内最大 z=侧缝区顶点，不是后中）——正确度量（θ 最近 180 的后片顶点）证实自由垂截面**前后基本对称**（腰下 2cm：前 +11.2/后 −9.8，渐收到腰下 18cm +5.6/−3.9），山脊从来就是缝尖折痕本身而非不对称；教训：**度量口径先于诊断**（D 形结论直接导向了（九）的碰撞方案，多绕一轮）。125 全绿；截图 out/wide_zoom.png。

- **2026-09-18 十期穿台：整裤穿人台 + 落位/合身读数（旁挂/穿台双视图）**。用户行为模型口径：「正常人穿裤子拉到腰或盆骨位置，裆部不舒服就会一点点往下，直到裆部比较舒服」→ 仿真协议翻译：向上唯一硬止点 = 裆（浪长不可伸长）、向下无几何卡点（真人不掉裤靠摩擦/腰带/胯骨，人台刚体无摩擦 → 支撑问题换成钉扎问题）→ **最终穿着高度 = min(锚定腰位, 裆零穿透最高位)**，单程搜索（向下无反向力学信号，掉裆是观感读数）；前后裆独立探针（前浪顶耻骨/后浪贴尾骨）→ 俯仰自然涌现。**AskUserQuestion 四口径拍板（全取推荐）**：① 碰撞体 = 人台切片环场（否决纸样芯——芯锚纸样围度与人台围度无约束、套人台轴必穿体；否决并集碰撞体——芯∪人台双重保守，布被两层壳夹持出非真实中面）；② 下降机制 = 单次仿真内准静态缓释钉高（否决外层二分腰高多次重跑——每跑 150+ 帧 ×N 次太贵，且重跑间 basin 彩票破坏读数可比性）；③ 起始腰位 = 人台腰地标（「拉到腰」是人的腰不是纸样的腰）；④ 旁挂保留双视图（平铺验证通道与旁挂对照是排障基础设施）。
  **实施（引擎零改动，前端 garment/ + Fitting3DView）**：placement.ts 穿台三原语（shiftPositionsY / nearestRingBoundary 自 drape.collide 内联提取共用·迭代序不变 / buildLegAxisFromRings）+ priors.ts DRESSING_PRIOR（holdFrames 240/dropRate 0.08/deadZone 0.3/maxDrop 15/confirmFrames 30/settleMaxFrames 480/maxTotalFrames 1800/forkSearch 6/tooSmallPen 0.5）+ assemble.ts buildFullPair lift 参数化（**TS 坑：HANG_PRIOR 是 as const，缺省参 `lift = HANG_PRIOR.hangLift` 会把参数类型窄化成字面量 12，穿台传 anchorLift 报 TS2345——须显式 `lift: number =`**）+ drape.ts per-sim maxFrames（wake 续预算的挂点，默认同值 = 回归字节等价）+ seams.ts tipAfterInseam export + **settle.ts 落位控制器（新建）**：hold→lowering→settle→done 状态机、裆探针（rise/cb 链自裆尖 probeSpan 4cm 弧内采样 + inseam 链末段 + 裆尖本体〔fly 连裁兜底〕，接触量 v = 环内 ? skin+d : skin−d 与 collide 平衡位同口径）、frontness f=|atan2(x,z)|/π 的 pinTarget.y = baseY + lerp(hF,hB,f) 下放写回（X/Z 钉不动——全钉保 XZ 刚度）、单侧 maxDrop fault 停放、wake() 清假 settled 续帧预算、report 出 DressReport；Fitting3DView 双模式大效应（deps 增 viewMode/dressEpoch，滑杆经 ref 冻结读取——滑杆变化不重跑、只 stale 提示「人台已调，穿着待更新」+「重新试穿」按钮，独立原则）+ Segmented「旁挂|穿台」+ 读数卡（掉裆前/后、前后裆接触三态、最差穿透@高度、tooSmall「偏小信号——读数提示，不改版型」、capped）。
  **初摆位 142 点穿透两缺陷（计划风险 4 实发；诊断方法论 = 穿透点按 part/run 归属分类 + y 带/深度分布直方 → 精确归因，scratch 诊断测试用后即删）**：(A) **叉口越界**——纸样裆深（~78）可低于体叉口（70 = 首个双环行），inseam 链顶段 y∈(70,77] 被 step4 钳位线（x=±0.3）摆进骨盆环（frontL/R:inseam 各 7 点、最深 6.69cm）→ 修 = assemble step4 叉口守卫 `if (y > fork) continue`（这些行归 step1 躯干/混合摆位含穿芯回退；旁挂纸样叉 = 内缝链顶，严格 > 不触发，旁挂回归零改动）；(B) **腿环质心 z 偏移**——大腿环质心 z 偏前 ~2cm，LegAxis 轴心 z=0 近似使绕管摆位前侧浅穿（frontL/R:body 各 58 点、d≤0.9；back 侧因摆位相位不受影响）→ 修 = buildLegAxisFromRings rProf 取 max(环包围半径 + |质心 z|)（包围圆按 (cx,0) 圆心放大 |cz|，保证环 ⊂ 圆(轴心, rAt)、绕管半径按构造在环外）。复测初摆位 pointInRings 计数归零。
  **minY 及地判读**：穿台集成首测 minY=0 触发「不拖地」断言失败——判**真实行为非 bug**：腰锚 7.644 − 前掉 ~7 后下摆恰及地被 collideGround 钳 0，真人掉裆裤脚拖地的对应；旁挂 hangLift 12 才有离地余量。断言放宽 minY ≥ −1e-6（只把门不穿到地下）。
  **测试（三新文件 20 例；全量 14 files / 145 passed；旁挂 drape/assemble 回归零改动全绿；tsc 干净）**：dressfield.test.ts（三原语合成场单元；断言坑：16 边形顶点恰在 +x 向，环外点 (6,0) 最近边界是顶点 d=1.0 精确而非边中点 1.05，toBeCloseTo(1.0, 6)）/ settle.test.ts（stub sim 字面量 + opts.probe 注入迹线——frontness lerp 一次下放后三钉 y=99.5/100.75/102 精确；−0 坑：dropB = −hB 得 −0，toBe(0) 严格比较失败须 toBeCloseTo(0, 10)）/ dress.test.ts（真 base.bin + fixture 全管线 = Fitting3DView dress 分支的无渲染镜像：anchorLift canary ∈(7.0,8.3)/θ 对齐 canary〔前中 z>0 后中 z<0〕/初摆位零穿透/落位 done 无 NaN 裆四尖 <1.0/dropF ≥2.0 值域 [0,maxDrop]/minY ≥ −1e-6/**同输入双跑 DressReport 逐字段相等 + pos 数组相等**（确定性红线）/hips+ 负松量宽松断言；**harness 坑两条：vitest 无配置文件默认 5s 超时，长集成须 `it(..., 180_000)` 第三参；console.log 默认被拦截，数值诊断须 --disable-console-intercept 且首跑输出被吞要重跑才见**）。实测：fixture 基础款 dropF ~7cm 量级、ph=done 真静止、裆四尖 <1.0、forkIn 71.0/forkY 70.0（首个双环行）；红线全数未动（friction 0.8/SEG 64/ε 0.3/启动序列/丹宁窗 seamFlattenStiffness 0.5）。规格现行口径入 §10.11「穿台链」条；fresh profile 首挂 422 既有 bug（2026-09-17 发现）未在本期修仍待办。代码停工作区未提交（用户口径）。

- **2026-09-18（二）穿台两后补：脚碰撞修复 + 热力图双通道**。用户原话「1. 脚感觉是不是没有碰撞检测 2. 穿台模式热力图需要支持下，方便看各个部位的合适程度」。
  **① 脚碰撞（诊断成立）**：穿台场锚定 anchorLift≈7.644 把人台网格下移回纸样系——脚在世界 y∈[0,7.6)、纸样系成**负 y**，而 BodyField 行表从 y=0 起建 = 负 y 域全夹行 0 无碰撞环，脚背以下裤脚穿脚无推出（diag 实测终态 56 粒子 pointInRings 在脚环内）。**修法（placement.ts 三处 + 穿台调用点，引擎零改动）**：BodyField/buildBodyField 增末参 `yMin`（行 r 高 = yMin + r·rowStep、行号换算 (y−yMin)/rowStep、行数 floor((maxY−yMin)/rowStep)+1；缺省 0 = 旁挂老口径字节等价，dressfield mkField 五参构造保绿）；buildLegAxisFromRings 增第 3 参 `ankleY` 止踝——yMin 下探后负 y 行进来的是脚环（包围半径含脚全长 ~18、前伸 cz 13+），混进 rProf 会让裤脚摆位喇叭张开（r+|cz| 放大口径，合成场实测 rAt(0) 2.04→31）；穿台调用点 `fieldYMin = floor(−anchorLift/rowStep)·rowStep`（= −8.0 对齐格点）+ `ankleY = 踝地标×sf − anchorLift`（世界 12.5 → 纸样 4.856）；腰/裆/踝三地标缺一 throw。**验证**：初摆位 initLeg=0/initFoot=0（dress.test 原断言 `inside===0` 原样保持）；终态 inFoot 56→8、深度 ≤+0.28cm（行离散化/两场行相位差 0.144cm 量级，渲染 ~4px/cm 下 1px 不可见，判网格固有残差接受）；forkY 70 不变、rAt(0)=4.52（踝行，原盲区口径 7.66）。**保留不修**：脚尖 zMax 18.91 在布筒（z ±6.7）外戳出裤口前缘 = 真人掉裆裤脚前伸观感。**diag 两坑**：初摆位扫描首版拿世界 y 查场（漏减 anchorLift，错位 ~7.6 行 → initLeg 假象 249），pair.pos 是世界系含 lift、纸样 y = pos.y − anchorLift；dressfield 新用例首版两处测试设计错——合成场 forkY 必须传**场系坐标**（yMin 下移后腿区 y∈[−8,−2]）、脚环期望值是 r+|cz|=31 不是 18（包围圆放大口径）。
  **② 热力图（AskUserQuestion 拍板双通道切换）**：**gap 间隙** = 布离身体距离（贴身≈0/适中余量/松垮悬空，看松量分布）/ **strain 应变** = 布料网格被拉伸程度（绷紧=偏小信号/压缩堆布，看偏小），互补、一个开关切换。实现：`garment/heatmap.ts`（新建，纯函数）——gap 逐粒子 nearestRingBoundary − CORE_SKIN（与 collide 推出位同口径，负=穿透；无环行/超 margin 钳 gapMax(4) 松端）、strain 逐顶点参与 dist 约束 (len−rest)/rest 的 signed 最大值（NaN 哨兵兜底）；priors 增 HEAT_PRIOR（gapMax 4 / strainMax 0.06〔丹宁缝后正常 <2%、绷紧 3~6%〕/every 6）；渲染 = render.ts rider/sim 视图顶点色 attr + vertexColors 材质切换（贴层顶点按绑定重心权重插值三宿主粒子、null 恢复分色）；**开/切只重着色当前帧不重跑仿真**（heatOn/heatMode refs 镜像不进大效应 deps，解算中每 6 帧刷、终态定格）；UI 侧栏卡 = Switch + Segmented 间隙|应变 + 渐变图例（CSS 渐变停靠色与 heatColor RED/GREEN/BLUE 同源）。**首版反向 bug（金标抓住）**：strain 色带 t=(s+max)/2max 把拉伸绷紧映到蓝〔松〕端、压缩堆布映到红〔紧〕端——与「红紧/蓝松」两通道同向语义相反，修为 t=(max−s)/2max（+6% 红/0 绿/−6% 蓝），heatmap.test 方向用例钉死。
  **测试**：dressfield 增 yMin/ankleY 两用例（10/10）、dress.test 管线同步（2/2，234s）、heatmap.test.ts 新 8 例（gap 环心 5·cos(π/16)−CORE_SKIN 精确/钳松端/yLift 行换算/field null 全 0；strain signed max/rest 0 守卫/局部号+offset——首版用例误传全局号即炸，dist 存 part 局部号与 drape 同口径由用例钉死；heatColor 端点/中点/越界钳）。**超时坑**：dress.test 双跑用例单跑 ~230s、全量并行负载实测 286s 超 180s 门槛（负载性超时非回归）——超时第三参放宽 480_000（探索例 300_000），复跑全量 15 files/155 全绿。红线全数未动；引擎零改动。规格入 §10.11 穿台链（yMin 场域下探/止踝/热力图段）与测试条；代码停工作区未提交（用户口径）。
