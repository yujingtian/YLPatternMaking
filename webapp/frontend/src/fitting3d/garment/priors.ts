// 3D 展示先验常数表（唯一收敛点）。2026-09-15 重建一期起解算链已删
// （整裤缝合/worker/PBD 全退役），本表只剩网格、支撑场、静态展示三类；
// SOLVER_PRIOR 族随解算链删除，skin 壳口径移 core.ts（CORE_SKIN）。
// 体型估计（EASE/BODY_*/BODYMESH_PRIOR 标定族）随一期 BodyProfile 链
// 退役不复活——独立原则下人台围度读 bodymesh 切片、衣服尺寸读引擎，
// 互不反推。改这里不需要重打引擎 zip（引擎零体型先验）。

export const MESH_PRIOR = {
  spacing: 1.5,          // cm：内部栅格间距
  boundaryStep: 1.5,     // cm：边界弧长重采样步长
  seamStep: 1.0,         // cm：缝合角色边的重采样步长（缝合链已删，边
                         // 角色表仍在 payload/mesh 层保留，后续重建复用）
  markStep: 1.0,         // cm：结构线重采样步长
} as const

// ---- 支撑半径场（R(y,θ) 查表；从网格截面预计算） ----
// 每行取全部切片环在方向 θ 上的支撑距离（凸上界——腿间/多环并集取
// 最大），保证初始摆位不穿体。
export const FIELD_PRIOR = {
  thetaBins: 256,        // θ 分辨率（查询双线性，环形回绕）
  rowStep: 0.5,          // y 行距 cm
} as const

// ---- 静态旁挂展示（2026-09-15 重建一期：前片 L+R 一对） ----
export const HANG_PRIOR = {
  clearance: 6,          // cm：前片筒与人台之间净空（视图层偏移量由此推导）
  hemLift: 2,            // cm：显示层整体抬高（历史防裤脚与地面格网共面打架；
                         // hangLift 抬升后下摆已离地 3~4cm，本值仅余显示量）
  garmentGap: 1.2,       // cm：摆位径向松量（placePoint 半径 = 场 + 本值）。
                         // 解算时代实测值原样沿用：贴皮起摆会让大量粒子
                         // 立即进入接触壳（下垂解算起步即接触，见 DRAPE）
  hangLift: 10,          // cm：悬挂解算整体抬升（2026-09-19 strain limiting
                         // 落地后重定标）：原 12 = PBD 残余拉伸 8~10（现已
                         // 硬钳 ≤1%）+ 离地余量；不可伸长布实测基础款下摆
                         // 落 world y≈0.5@lift7（腿区绕管摆位世界 y 比纸样
                         // y 低 ~6.5，几何量非拉伸），抬 10 离地 ~3cm 不拖地
                         // （金标 minY>1 实测标定）。作用于摆位 y（含钉
                         // 目标），地面碰撞安全网
  sideHold: 10,          // cm：侧缝边顶部全向钉深度 = 腰头缝合线的刚度带
                         // （2026-09-17 用户口径「顶部侧缝边不要折、拼合后
                         // 顶部〔腰头缝合线〕是圆弧」，AskUserQuestion 拍板
                         // 保持两筒并排撑圆）：无侧缝缝合的自由半身筒，侧缝
                         // 自由边在自由垂中向筒轴心内摆（后身实测最深
                         // ~25°）；配 assemble 侧缝边语义摆位（θ=±90° 竖直）
                         // 后顶部扇区完整、带缘出口偏差实测 ≤0.7°（往下自由
                         // 内摆渐增属自然垂）——摆位与布料度量一致后布不再
                         // 对抗（真实成衣此区由腰头撑圆）
  riderStep: 0.2,        // cm：贴层径向内偏步进（2026-09-16 四期前身缝合
                         // 立起：宿主不渲染、前片/袋贴双 rider 贴层——前片
                         // 偏移 0、袋贴 −本值衬里侧，语义同平铺 stackStep，
                         // 防与宿主面共面 z-fight；口径「前片在前口袋上面」）
  legReparam: true,      // 八期整裤：腿区（fork 以下）腿局部圆环绕管摆位
                         // 开关——内缝边落腿内侧线（前后宿主相邻共线，焊
                         // 对初始间隙 ≈0）、侧缝边落腿外侧线；false 回退
                         // 全局 x→θ（旧口径，仅诊断用）
  waistBlendSpan: 4,     // cm：侧缝腰口高差渐变带半宽（2026-09-17（十）
                         // 用户报障「腰头在侧缝处不平整，是后片高前片低
                         // 的缘故么」——是：前后腰口在侧缝高差 ~0.4cm
                         //〔实测 front 97.7 / back 98.1〕，corner snap 只
                         // 平均了角点本体、缝口两侧边沿仍各持己高，腰头底
                         // 边跟出台阶；角点两侧本值弧内顶链 y 向 snap 高度
                         // 渐变 = 真实缝头吸收高差的展示等价）
  forkBlend: 8,          // cm：fork 向上的躯干↔腿摆位线性过渡带高（裆角
                         // 布从躯干前面顺滑收进腿间；0 = 硬切换——fork 高
                         // 度折痕时的对照旋钮，单 knob 单变量）
} as const

// ---- 平铺验证展示（2026-09-15 重建三期回落：用户判定悬挂渲染下裁片
// 形状异常，先平铺对照 2D 裁片 SVG 定位问题层；悬挂链常数保留上方 ----
export const FLAT_PRIOR = {
  clearance: 6,          // cm：平铺片与人台净空（旁挂偏移由此推导）
  gap: 4,                // cm：L/R 两片间距（前中边相对，拼近整前片观感）
  rowGap: 6,             // cm：裁片行距（每种裁片一行，行式布局）
  stackStep: 0.2,        // cm：拼合组内叠层步进（组内第 j 片 y = j×本值）——
                         // 前片+袋贴净样有重叠区（袋口条带衬在裤身里侧），
                         // 共面 z-fight，按片序微抬分层（真布叠放观感）
  lift: 2,               // cm：显示层离地高度（防与地面格网共面打架，
                         // 同 hemLift 理由）
} as const

// ---- 引力下垂解算（2026-09-15 重建二期：用户口径「裤子的下垂要符合
// 地球引力」+ 同日拍板「前中缝合」。主线程轻量 verlet——距离/弯曲约束
// + 前中缝合对（L/R rise 链镜像配对）+ 撑型芯径向碰撞 + 腰口全向 pin，
// 无跨片装配缝合/无 worker/无 BVH。数值继承已删 SOLVER_PRIOR
// （git d784de1，整裤标定值）——单片地形更温和，旧值直接起步；
// 若不 settle 按旧方法论调 damping/friction，勿动 dt/substeps 结构） ----
export const DRAPE_PRIOR = {
  gravity: -980,         // cm/s²（地球引力 9.8 m/s²，纸样系 Y 向上）
  damping: 0.985,        // 速度衰减/步
  dt: 1 / 60,            // 单步时长 s
  substeps: 3,           // 帧内子步
  iterations: 6,         // 每子步约束迭代（Gauss-Seidel）
  bendStiffness: 0.3,    // 弯曲刚度（dist 全刚 1.0 对照；全局提 0.45 实测
                         // 无效——重力压平镜头截面，缝尖 r 仍 ≈C/2 不动，
                         // 缝口凸起要靠 seamFlatten 局部摊平窗）
  seamStiffness: 1.0,    // 前中缝合约束刚度（L/R rise 链镜像配对 rest=0，
                         // 2026-09-15 用户拍板「前片先将前中缝合在一起」；
                         // 值继承旧 SOLVER_PRIOR.seamStiffness）
  friction: 0.8,         // 接触摩擦：碰撞推出时 prev 向 pos 混合的比例——
                         // 旧链教训：摩擦 0.5 在撑型芯上周向慢转不锚（地板
                         // ~9 永不收敛），0.8 + SEG64 芯实测收敛
  settleSpeed: 0.8,      // cm/s：均速低于此视为静止
  settleFrames: 30,      // 连续静止帧数 -> settled
  maxFrames: 600,        // 帧数封顶（兜底）：超限取当前帧出 settled
  strainLimit: 0.01,     // 应变限幅 ε（2026-09-19 用户口径「牛仔裤布料厚实、
                         // 不会被这么拉伸」）：每子步末硬投影 dist 边长
                         // ≤ rest×(1+ε)。审计实测 PBD 平衡态残余 mean 4.3%
                         // /p95 16%（续跑 2400 帧不退；旁挂 hangLift 旧值 12
                         // 即为藏它）全是数值假象——rest=纸样净长本义不可
                         // 伸长。丹宁缝后正常 <2%（HEAT 口径）取 1%；只限拉
                         // 不限压（压缩=褶皱=真布自由度）
  seamFlattenStiffness: 0.5,  // 缝口摊平窗弯曲刚度（2026-09-17（八）用户
                              // 报障「侧缝缝合的线有明显凸起」：自由垂挂
                              // 前后片近乎平行贴合，侧缝=180° 对折刀锋卷边
                              // ——缝口 ±seamFlattenSpan 窗内弯曲刚度升到
                              // 本值，折痕摊开成缓坡（「向两边拉直」的物理
                              // 实现；值取旧链丹宁稳定窗 0.45~0.5 下沿以上）
  seamFlattenSpan: 5,         // cm：缝口摊平窗半宽（距 side/inseam 链的
                              // 纸样距离。（十一）3→5：缝条足够宽才像熨过
                              // 的缝头硬条，折痕移到窗缘、缝身留在布面中间
                              // 平顺——±3 时缝本身仍是折尖（用户仍见山脊））
  waistTransitionSpan: 15,    // cm：腰臀过渡刚度带高（腰缝以下本值内弯曲
                              // 刚度同样升到 seamFlattenStiffness——
                              // 2026-09-17（九）用户报障「腰头到臀部的
                              // 侧缝像山脊凸出、快到腰头处前后片叠合」：
                              // 钉圆的腰口往下布塌得太快（后片腰下即塌到
                              // z≈0、前片保持 z≈10 的 D 形不对称截面），
                              // 折角全挤在缝口几厘米内成脊；过渡带提刚度
                              // = 截面缓慢张开、前后片同步、折痕摊成顺滑
                              // 曲线；臀以下照旧自然垂）
  hemBandStiffness: 0.5,       // 脚口环带弯曲刚度（2026-09-19（二）用户报障
                              // 「脚口穿模」——脚碰撞本身干净，观感 = 前缘
                              // 从脚背脱扣滑到脚后；根因链 = 钉环贴体 → 掉裆
                              // 加深 0.6~0.8（dropF 6.48→7.12）→ 额外钉降把
                              // 挂在脚背上的前缘拖过脚背冠。真实牛仔裤脚口
                              // 是双折卷边+车缝硬圈，抗剪切压折——hem 链
                              // hemBandSpan 内弯曲刚度升到本值，环口保持
                              // 圆形不折，前缘挂扣全程保持到 done（实测 +7.9，
                              // 诚实掉裆不钳制）。k 窗口地图（fixture 基础款
                              // DIAG 扫描）：0.8/1.0 顶撞脚碰撞（脚区穿透
                              // 0.92~0.95）取 0.5；span 3/3.5 不足（前缘
                              // 2.9/3.3 滑脱）、4✓最干净（穿透 0）、4.5 中
                              // 带孤点 0.95 穿透、5✓（footIn=0）、6 上探小腿
                              // 杠杆反害（前缘 3.2）——span 只对本 fixture
                              // 对成立（collisionSkin 先例口径），换体型/裁片
                              // basin 重排需重验
  hemBandSpan: 4,              // cm：脚口环带高（真实双折卷边 ~3-4cm 取上限）
  topSupportScan: 3,           // 行：上表面竖直支撑向下扫行数（2026-09-20
                               // 长裤盖脚：支撑壳驻留位在交叉带上方
                               // skin/rowStep≈2 行，扫 3 行覆盖 + 余量；
                               // 墙面/陡坡零改动，详见 drape.collide 头注）
  topSupportScanUp: 8,         // 行：埋点救援向上扫行数（同日二轮：脚口周长
                               // 37.5 < 脚底切片周长 ~45-55，褶皱压进坡面以下
                               // = 自身行环内+上行行环也内，需向上扫到头顶坡
                               // 面出口抬回；8 行 = 4cm 埋深覆盖脚背/脚趾上
                               // 侧褶皱下扎深度，墙内深埋出口更远扫不到、
                               // 零改动交水平推出）
  topSupportSlope: 0.6,        // 坡度阈 g=tanθ ≥ 本值判「上表面」（0.6 ≈ 31°。
                               // g=0 垂直墙 →∞ 平顶；墙/陡坡走原水平推出，
                               // 脚背/脚尖/脚跟、大腿上侧等朝上面才有竖直
                               // 支撑——水平推出撑不住向下变宽的坡，布粒被
                               // 侧向射出再下坠 = 沿坡滑到底、脚渐次穿出裤筒。
                               // 初版 1=45°：趾盒冠实测 g≈0.8 被判陡坡漏支撑、
                               // 脚口前缘在趾盒顶滑脱，降到 0.6 = 趾盒全坡面
                               // 接管〔腿侧竖直墙 g≈0 不受影响〕）

  crotchHold: 0,         // 帧数：裆四尖短时硬钉后硬释放（八期 fallback
                         // 旋钮，默认 0 关——四尖零 rest 闭环拓扑自动坍缩；
                         // 不 settle/裆形态差时先试 60，先例旧链硬释放口径）
} as const

// ---- 穿台落位（2026-09-18：整裤穿人台 + 穿着位 settle）----
// 真人穿裤行为模型：「拉到腰 → 裆不舒服一点点往下 → 前后裆零穿透即停」
// （向上唯一硬止点 = 裆〔浪长不可伸长〕、向下无几何卡点〔支撑换成钉扎〕
// → 最终穿着高度 = min(锚定腰位, 裆零穿透最高位)；前后裆独立探针 =
// 俯仰自然涌现）。四口径拍板：碰撞体 = 人台切片环场 / 单次仿真内缓释
// 钉高（前后独立）/ 起始 = 人台腰地标 / 旁挂保留双视图。独立原则不变：
// 试穿结果是读数，不是自动调版闭环
export const DRESSING_PRIOR = {
  holdFrames: 240,       // 帧：上身后持锚先松弛（布贴身、缝对闭拢）再开始
                         // 下放（启动序列勿盲调红线——先让布静止再动钉）。
                         // 上限而已（2026-09-23 提速 1c）：settledFrames 先达
                         // 全局静止判据即提前转 lowering（零新常数，「静止」
                         // 定义与 settle 相位同源；实测基础款 ~80 帧即静）
  dropRate: 0.08,        // cm/帧：钉目标下放速率（准静态 ≈4.8cm/s @60Hz；
                         // 过快 = 下放瞬态与碰撞拔河）
  deadZone: 0.3,         // cm：裆接触死区（探针 |穿透/间隙| ≤ 本值视为贴合
                         // ——壳口径下 0 = 恰接触；对齐旧链 ε 0.3 判据）
  probeSpan: 4,          // cm：裆探针弧长窗（rise/cb 自裆尖、inseam 自脚口
                         // 端各取本值内采样——裆接触是一段不是一个点）
  probeMargin: 2,        // cm：探针量间隙的 quick reject 余量（全落空按
                         // 本值钳为净空）
  maxDrop: 15,           // cm：单侧最大掉裆（到顶仍穿 = 偏小 fault 停放，
                         // 不与碰撞无限拔河——偏小是读数不是错误）
  confirmFrames: 30,     // 帧：双侧入区（或 fault）确认帧数 → 进 settle
  settleMaxFrames: 200,  // 帧：settle 相位独立预算（wake 续 maxFrames 用）。
                         // 480→200（2026-09-23 提速 1c）：挂胯判据 P1 后
                         // settle 段主要在消缓释残差，200 帧实测足够收敛；
                         // 预算尽 capped 读数如实呈现（「预算尽收束」提示）
  maxTotalFrames: 1800,  // 帧：控制器硬顶（hold+lowering+settle 总计兜底）
  forkSearch: 6,         // cm：自裆地标向下搜首个双腿分离行的窗口
  jamMargin: 0.02,       // 挂胯判据余量（2026-09-23 P1）：候选档行周长 ≥
                         // 环长×(1+本值) → 卡停不下放——不可伸长闭环（总长
                         // C）套不进周长 > C 的截面是精确几何事实，余量只对应
                         // 布的弹性（丹宁实穿下限；0% 过紧、4% 偏松，1% 差 ≈
                         // 卡停行 ~0.4cm 位移不敏感）
  tooSmallPen: 0.5,      // cm：终态全粒子壳穿透最差 > 本值 → 偏小读数
  waistRingStep: 0.25,   // cm：腰圈钉环等弧长重采样步长（buildWaistRing；
                         // 弧长映射精度——步长越短钉间距=纸样边长越精确，
                         // 0.25 时弦/弧差 <0.1%）
} as const

// ---- 穿台热力图（2026-09-18：用户拍板双通道切换——间隙/应变互补，
// 一个开关切换；间隙看松量分布、应变看偏小绷紧）----
export const HEAT_PRIOR = {
  gapMax: 4,             // cm：间隙通道满值（布离体 ≥ 本值 = 最松蓝端；
                         // 负值 = 穿透红端）
  strainMax: 0.06,       // 应变通道满值（应变通道 2026-09-19 移除暂留——
                         // 通道恢复成本低，参数口径先保 |拉伸| ≥ 6% 上界；
                         // 丹宁无弹布缝后正常 <2%，绷紧带 3~6%，超 6% 已肉眼
                         // 可见）
  every: 6,              // 帧：解算中重算间隔（性能——每帧全粒子 nearest
                         // 环边界扫描 ~5ms，6 帧一次无感；终态/切换即时）
} as const
