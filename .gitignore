# 打版产物（SVG / 追踪记录 / 报表）
out/

# Python 缓存
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 前端
node_modules/
dist/

# 本地引擎生成物（build:engine 产出，随 dist 构建链带上）
webapp/frontend/public/engine/
webapp/frontend/public/pyodide/

# VLM 接入配置（真实 key 只进本地，仓库只留 vlm.toml.example）
vlm.toml

# 照片提取金标集照片（描述与真值入库，照片不进 git）
tests/_extract_golden/cases/*/photos/

# MakeHuman 原始网格直显（诊断用 ?bodymesh=raw；vendor/makehuman/base.obj 的本地副本，不进 git）
webapp/frontend/public/bodymesh/raw.obj
